# TCG Pipeline Tracker — Roadmap & Current Build Plan

> **This is the active build plan.** It supersedes the build plan (Section 7) in `ARCHITECTURE.md`.
> For detailed source specifications, field inventories, matching strategy, and data model definitions, refer to `ARCHITECTURE.md`. That file remains the reference for *what the system is*; this file is the reference for *what to build next and what has been built*.

**Last updated:** 2026-05-07
**Maintained by:** Nate Goldstein + Claude Code / Codex agents

---

## How to Use This Document

This is a living document. It should be updated continuously as work progresses.

**For Claude Code / Codex agents:** Read this file at the start of every session. When you complete a build step, mark it `done` in the table and add a date + brief note. When the plan changes (new steps, reordered priorities, removed items), update the tables and add an entry to the Decision Log at the bottom. Use commits as checkpoints.

**For researchers and contributors:**
- **Status markers:** `done`, `in_progress`, `not_started`, `deferred`, `blocked`, `flexible`
- **When completing a step:** Change status to `done`, add completion date and a one-line note summarizing what was built and where the code lives.
- **When priorities shift:** Move steps between phases or reorder them, but always record why in the Decision Log.
- **When adding new work:** Add it to the appropriate phase. If it doesn't fit, create a new phase. Keep phase numbering stable — insert sub-phases (e.g., Phase 3a) rather than renumbering everything.
- **When deferring work:** Change status to `deferred` and note why. Don't delete — deferred work often comes back.

**Principles for this document:**
1. The build plan reflects what we *actually intend to build next*, not an idealized sequence.
2. Every step should be small enough to complete in 1-3 days of focused work.
3. Dependencies between steps should be explicit.
4. If a step has been `in_progress` for more than a week without progress, it needs to be broken down or re-evaluated.
5. The Decision Log is append-only. Don't edit old entries — add new ones that supersede them.
6. New sources, matchers, extractors, resolvers, and UI workflows should be market-agnostic by default. If market-specific behavior is unavoidable, put it in market/jurisdiction/source config and make it easy to extend during new-market onboarding; avoid growing hardcoded `if market_slug == ...` maps in application logic.

---

## 1. Project Overview

An automated system for building and maintaining comprehensive real estate development pipeline data across US markets. See `ARCHITECTURE.md` Section 1 for the full overview.

**Core workflow (with evidence layer):**
```
Seed (once) → Collect (scheduled) → Match → Insert Evidence → Resolve Project → Diff → Review (human) → Update
```

The system uses an **append-only evidence store** where all incoming data is captured as immutable evidence rows. The Project record is a **derived view** computed by per-field resolution rules. This ensures full provenance, supports confidence scoring, and allows the resolution logic to be rerun or refined without losing any historical data.

---

## 2. Current System State

**As of 2026-05-07.** Phase D is in progress (D.6 is the last open item from the deterministic news pipeline). The AGENT sprint has completed AGENT.1 foundation work: schema/cost prerequisites, the multi-provider news LLM abstraction, the slim no-glossary default-extraction prompt, the A/B harness scaffold, and accepted-reference article embeddings are committed and production-smoked. Live harness runs are complete on the five-article D.6 smoke set, including the supplemental Opus 4.6 / GPT-5.5 comparison; the researcher selected Opus 4.7 as the default extraction model. The AGENT.1 migration has been applied to the single production Supabase database after a logical backup; Render API/worker are deployed on matching AGENT.1 code; one accepted news reference indexed two article chunks; and an immediate rerun skipped both chunks with zero embedding spend. AGENT.2 audit migrations, the workforce-units migration, and the semantic Pass 2c runway migrations are now applied to the same database, DB-backed agent/schema tests have run unskipped, and the narrow news agent routes for `new_candidate`, `possible` / ambiguous existing-project matches, and `low_confidence` references are implemented and live-smoked. The output-quality retry path (`extract_retry_v1`) is code/test complete. AGENT.2 step 7 now has the Pass 2c news interpreter, audit persistence, jurisdiction-policy context, evidence/review integration, status resolver promotion path, and broader controlled smoke suite complete. AGENT.2 step 8 moved legacy Pass 3a/3b re-extraction into `src/tcg_pipeline/news/extraction_legacy.py` behind the existing `news_use_legacy_pass3` fallback flag. Render API/worker target the Pass 2c path with `NEWS_USE_LEGACY_SEMANTIC=false`; explicit kill switches remain in place. Phase tables below are authoritative for detailed status.

### What's Built and Working

| Component | Status | Key Code |
|-----------|--------|----------|
| **Database schema** | Complete + active Phase D extensions | `db/models.py` - core project/evidence/review schema plus markets/jurisdictions, source registrations, review staging/decision cards, worker jobs/heartbeats, and news research tables |
| **Alembic migrations** | Repo current through `202605070033`; production applied through `202605070033` | `alembic/versions/` - core schema, evidence layer, RLS/read views, review workflow, worker jobs, Phase D news schema, cache-creation token accounting, AGENT.1 foundation schema, AGENT.2 agent-run audit schema, workforce-units schema, semantic review-item enum runway, and Pass 2c semantic-interpretation audit table runway. `202605040028` was applied to the single production Supabase database on 2026-05-05 after a `pg_dump` logical backup. `202605050029` and `202605050030` were applied to the same database on 2026-05-06 after fresh logical backup `supabase_pre_agent2_20260506_033558.dump` (SHA256 recorded in chat and AGENT.2 notes). `202605060031` was applied on 2026-05-06 after `supabase_pre_workforce_units_20260506_193803.dump` (SHA256 `f2f9bf32787bb759df1544010f2bb197a3e7b4b58fdf6ef01c2ca2344a602e01`); Render API/worker deployed matching commit `cdb4519`. `202605070032` and `202605070033` were applied on 2026-05-07 after `supabase_pre_semantic_pass2c_20260507_101243.dump` (SHA256 `bdd9839b7d9ce10c0492047fe7e5e3e55442c0e7c090ac7cecf15b7ebe423b16`); Render API/worker deployed matching commit `ab10cf7`. |
| **Pipedream ingester** | Complete | `ingesters/pipedream.py` — parses .xlsm DataStorage tab, 81 fields |
| **CoStar ingester** | Complete | `ingesters/costar.py` — parses .xlsx exports, header-based mapping |
| **Seed persistence** | Complete | `db/seed.py` — Pipedream + CoStar merge with dedup, relationship resolution |
| **Socrata collector** | Complete | `collectors/socrata.py` — async paginated, incremental mode, hash-based change detection |
| **LADBS source adapters** | Complete (6 adapters) | `source_adapters/ladbs.py` — permits + permit activity (pi9x-tg5x), inspections (9w5z-rg2h), CofO (3f9m-afei), 2 legacy frozen-dataset adapters |
| **Matcher** | Complete (3-tier) | `matching/matcher.py` — source_record → identifier → address |
| **Address normalizer** | Complete | `matching/normalizer.py` — usaddress parsing, directional/suffix/ordinal normalization |
| **Differ** | Complete | `matching/differ.py` — field change detection between project and incoming record |
| **Status rules** | Complete | `status_rules.py` — forward-only with evidence types |
| **Collect + persist pipeline** | Complete (evidence-aware) | `db/collect.py` — match → write evidence → upsert PSR → resolve project → diff → review items |
| **Evidence write layer** | Complete | `db/evidence.py` — write_raw_record_evidence, write_source_record_evidence, write_pipedream_snapshot_evidence, hash-based dedup |
| **Resolution engine** | Complete | `resolution/engine.py` — orchestrates 6 field resolvers, computes likelihood + confidence, writes ResolutionLog, appends StatusHistory |
| **Field resolvers** | Complete (all 6) | `resolution/fields/` — status, units, delivery_year, developer, product_type, age_restriction |
| **Likelihood engine** | Complete (v1) | `resolution/likelihood.py` — base rate per status + signal adjustments |
| **Confidence rollup** | Complete | `resolution/confidence.py` — field-level → project-level aggregation |
| **Source tier config** | Complete | `source_tiers.py` + `config/source_tiers.yaml` — logical source types, 4-tier hierarchy |
| **Developer canonicalization** | Complete | `developer/registry.py` — rapidfuzz matching, alias management, registry merging |
| **Review workflow** | Complete | `db/review_workflow.py` — accept/reject/defer with evidence relinking, resolution re-run |
| **Frontend app** | Complete for Phase B/C workflows | `app/`, `components/`, `lib/` - project list/detail, map, Coverage, Review Queue, project create/edit/relation workflows |
| **FastAPI write boundary** | Complete for Phase C writes | `src/tcg_pipeline/api/` - JWT/allowlist guarded writes for review decisions, project edits, notes, relationships, manual create, and Phase D paste-a-link/admin reads |
| **Worker runway** | Complete for Phase D queueing | `src/tcg_pipeline/workers/` - Redis/RQ scrape worker, worker heartbeat, scheduler leader loop, health endpoints, and registered news job kinds |
| **News research runway** | Partial (D.1-D.5 + D.7b done; D.6 in progress) | `src/tcg_pipeline/news/`, `app/(app)/research/`, and Review Queue news context - paste-a-link Pass 0, structural extraction, Haiku triage, Opus extraction/re-extraction, matcher/integrator, resolver filtering, review context rendering, article admin, and retry/refetch UX. Scheduled collection/backfill still pending. **Pass 3a/3b deterministic re-extraction will be replaced by the agentic escalation layer in the AGENT sprint — see `docs/specs/agentic_escalation_design.md`.** |
| **Backfill scripts** | Complete (not yet run on prod) | `scripts/backfill_evidence.py`, `scripts/backfill_developers.py` |
| **CLI** | Complete | `cli.py` — doctor, preview/seed pipedream/costar, preview/collect source, resolve-all, canonicalize-developers |
| **Market config** | Complete (LA + SM stub) | `config/markets/los_angeles.yaml`, `config/markets/santa_monica.yaml` |
| **Tests** | Solid | Broad pytest coverage across collectors, evidence/resolution, review workflow, API, workers, and Phase D news modules |
| **Agent escalation layer** | In progress (AGENT.1 done; AGENT.2 first three news triggers live-smoked) | `docs/specs/agentic_escalation_design.md` is the implementation contract. AGENT.1 delivered the generalized cost/retrieval schema prerequisites, multi-provider news LLM clients, the no-glossary default-extraction prompt, the non-mutating A/B harness scaffold, and the accepted-reference article embedding/indexing pipeline. Production smoke indexed one accepted news reference into two active chunks and proved idempotent rerun behavior. Live A/B smoke-set reports exist for Opus 4.7 / Sonnet 4.6 / GPT-5.4 and the supplemental Opus 4.7 / Opus 4.6 / GPT-5.5 comparison. Researcher decision: keep Opus 4.7 as default extraction. AGENT.2 has applied `agent_runs` / `agent_run_review_items` schema, added the runner/client/tool registry, shipped DB-backed `get_project_state`, `search_projects`, `search_articles_similar`, and `get_article_body` tools, and live-smoked `new_candidate`, `possible` / ambiguous existing-project, and `low_confidence` news routes while preserving deterministic fallback. Next: output-quality retry, then workforce-units integration, semantic interpretation, and Pass 3a/3b replacement. |

### What's NOT Built

- No scheduled news collection or source backfill yet. Paste-a-link, Pass 0 fetch, Pass 1 structural extraction, Pass 2 triage/extraction, Pass 3 re-extraction, matcher/integrator, resolver filtering, Review Queue news context, and article admin UX exist in Phase D.
- No broad production agent rollout yet. `new_candidate`, `possible` / ambiguous existing-project, and `low_confidence` news agent routes are implemented, schema-applied, and controlled-smoked, but AGENT.2 is still intentionally narrow. `extract_retry_v1` output-quality retry is implemented/tested; semantic field interpretation and full Pass 3a/3b replacement are still pending. Render kill switches remain the fast rollback path while AGENT.2 is monitored and extended. See `docs/specs/agentic_escalation_design.md`.
- Article retrieval schema prerequisites are live from AGENT.1 (`pgvector`, article chunks, per-reference gates, PostGIS GIST index). The embedding pipeline is implemented as `tcg-pipeline news index-articles` plus the `news_backfill_chunk` worker task; retrieval tools are consumed in AGENT.2.
- No LAHD affordable housing collector (defined in config, no code)
- No PDF parser collector (la_case_reports — defined in config, no code)
- No ZIMAS/PDIS scraper (defined in config, no code)
- No CEQAnet collector
- No output formats (Excel export, exhibit appendix, CMA block)
- No delivery year freshness threshold (6-month rule, UC exemption)
- No auto-stall detection (12+ months no evidence → flag)
- No likelihood config externalization (base rates hardcoded in Python)
- No seed ingester evidence writes (seed path doesn't create evidence rows yet)
- No Santa Monica market implementation
- No multi-market generalization

---

## 3. Evidence Layer Architecture

> This section summarizes the evidence layer design. For the full specification, see `docs/specs/EVIDENCE_LAYER_INTEGRATION_GUIDE.md` and `docs/specs/EVIDENCE_LAYER_DECISIONS.md`.

### Core Concept

All incoming data — from collectors, seed imports, news articles, researcher input — is stored as **immutable evidence rows** in the `evidence` table. The `Project` record is never written to directly by collectors. Instead, a **resolution engine** reads all evidence for a project and computes the canonical field values using per-field rules.

After the AGENT sprint, evidence rows produced as a result of agent reasoning will be linked (via the `agent_runs` audit table keyed by `(intake_source_type, intake_record_id)`) to a reasoning trace, an `evidence_consulted[]` list, and a `tool_calls_summary[]` — preserving full audit context for any agent-mediated attribution decision. Evidence row schema is unchanged; the agent metadata lives alongside in `agent_runs`. See `docs/specs/agentic_escalation_design.md` §5.6.

### Source Tiers

| Tier | Sources | Role |
|------|---------|------|
| **Tier 0** | Researcher overrides | Highest authority, but review-protected rather than permanently sticky. New contradicting evidence creates a review item; it never silently replaces or silently yields to the override. See EVIDENCE_LAYER_DECISIONS.md §22. |
| **Tier 1** | Government sources (LADBS, LAHD, ZIMAS, SM permits) + Pipedream | Authoritative public record + human-verified TCG research |
| **Tier 2** | News articles (Urbanize LA pilot; LA YIMBY, The Real Deal LA, and BizJournals LA deferred) | Timely intelligence, moderate confidence |
| **Tier 3** | CoStar, developer websites | Broad coverage, sometimes stale or inaccurate |
| **Tier 4** | Social media, forums | Weak signal, requires corroboration |

### Resolution Rules (per-field)

| Field | Rule | Notes |
|-------|------|-------|
| **status** | Forward-only progression, evidence-type gated | CofO→Complete (direct), inspection→UC (direct), permit alone→Approved (requires review), UC needs corroboration |
| **total_units** | Most recent evidence wins (any source) | |
| **affordable/workforce/market_rate units** | Most recent from allowlisted sources only | Pipedream, LAHD, SM Dev Tracking, news |
| **product_type** | Most recent explicit value wins | |
| **age_restriction** | Most recent explicit value wins | |
| **delivery_year** | Explicit source date > existing project value > estimation formula | Provenance tracked: explicit_government, explicit_tcg, etc. |
| **developer** | Most recent wins, then custom source priority as tiebreak + canonicalization | pipedream > news > developer_website > costar > ladbs (tiebreak only — newer evidence always wins over older regardless of source priority) |

### Data Flow

```
Source → Collector → RawRecord → match_raw_record()
                                       ↓
                              write_raw_record_evidence()  (append-only)
                                       ↓
                              _upsert_source_record()      (PSR cache, for matcher compat)
                                       ↓
                              resolve_project()            (recompute all fields from evidence)
                                       ↓
                              diff → ReviewItem            (flag changes for researcher)
```

---

## 4. Build Plan

### Phase A: Backfill & Validation
> **Goal:** Prove the evidence layer produces correct results against real data before building anything on top of it.
> **Priority:** Immediate — this is the prerequisite for everything else.
> **Depends on:** Nothing (all code exists, just needs to be run and validated).
> **Required reading:** `docs/specs/EVIDENCE_LAYER_INTEGRATION_GUIDE.md` (resolution rules, schema), `docs/specs/EVIDENCE_LAYER_DECISIONS.md` (edge cases, thresholds). For status evidence semantics: `ARCHITECTURE.md` Section 8 Decision Log entries from 2026-04-16 (LADBS permit → Approved not UC, CofO only with real date → Complete, inspections only recent+substantive → UC).

| Step | Task | Status | Notes |
|------|------|--------|-------|
| A.1 | Run `backfill_evidence.py --dry-run` against production DB | `done` | 2026-04-22: Dry-run succeeded on live DB. Would insert 1520 PSR evidence rows + 572 Pipedream snapshots, skip 572 Pipedream PSRs, 0 duplicate skips. Preflight found 2 existing orphan evidence rows, neither matched a current PSR. |
| A.2 | Run `backfill_evidence.py` (commit mode) | `done` | 2026-04-22: Committed successfully on live DB. Evidence table now totals 2094 rows (including 572 Pipedream snapshots and 2 pre-existing orphan rows). Post-commit dry-run was idempotent: 0 inserts, 2092 duplicate skips. |
| A.3 | Run `backfill_developers.py --dry-run` against production DB | `done` | 2026-04-22: Dry-run succeeded on live DB. Would insert 1 missing registry row (`Helio / UCLA`) and skip 313 existing developer names. |
| A.4 | Run `backfill_developers.py` (commit mode) | `done` | 2026-04-22: Committed successfully on live DB. Added 1 missing canonical developer (`Helio / UCLA`); registry now contains 319 rows. Post-commit dry-run was idempotent: 0 inserts. |
| A.5 | Run `resolve-all --market los_angeles` (shadow mode) | `done` | 2026-04-23: Full-market shadow sweep completed with the batched runner. Current `resolution_log` is de-duplicated per project and contains 7,519 rows across all 1,362 LA projects. Field counts: 1,362 each for confidence_reason, delivery_year_provenance, last_evidence_date, likelihood, likelihood_breakdown; 292 confidence; 253 date_delivery; 128 developer; 27 total_units; 9 pipeline_status. |
| A.6 | Review resolution discrepancies, tune if needed | `done` | 2026-04-23: Phase A review packet regenerated from the final LA shadow rerun (`docs/notes/phase_a_review/reshadow_run_postfix.log`). Final packet counts: `status_review.csv` (9), `units_review.csv` (27), `delivery_review.csv` (34 explicit overwrites), `delivery_estimate_spotcheck.csv` (10 sampled from 218 estimated fills), `developer_review.csv` (161 judgment rows), `developer_category_cleanup.csv` (84 FYI rows), `developer_canonical_cleanup.csv` (59 exact alias/canonical cleanup rows), `developer_helio_ucla_cluster.csv` (10), `developer_alias_candidates.csv` (0 after alias adds + rerun). Review decisions: accept all 9 status rows; accept all explicit delivery rows; accept all estimated delivery fills as policy; accept unit deltas `<= 5`, hold larger deltas with conditional overrides; accept Category/canonical developer cleanup rows; accept remaining developer rows except architecture-firm and raw-value override exceptions captured in the apply profile. Validation memo: `docs/notes/phase_a_validation.md`. |
| A.7 | Run `canonicalize-developers --market los_angeles` (shadow mode) | `done` | 2026-04-23: Shadow run completed with no writes. After deleting the polluted `Category` registry row and its aliases in production, the rerun scanned 318 registry rows and 967 projects with non-null developer values; would change 0 project values in this mode. Match summary: 1,134 exact, 2 fuzzy-auto, 13 fuzzy-review, 0 new registry-entry candidates. `--apply` now skips fuzzy-review registry merges and project developer rewrites so ambiguous matches remain shadow-only until reviewed. Later reruns also split exact alias/canonical cleanup rows out of manual review into `developer_canonical_cleanup.csv`. |
| A.8 | Run `resolve-all --apply` + `canonicalize-developers --apply` | `done` | 2026-04-23: Bucket-level decision profile `phase_a_2026_04_23` dry-ran cleanly on 384 CSV rows (`360` accept, `20` defer, `4` override) and wrote 24 conditional overrides before apply. LA apply completed across all 1,362 projects using the stable batched runner (`scripts/run_phase_a_resolve.py`): an initial 76-project apply plus a resumed 1-project batch sweep recorded in `docs/notes/phase_a_review/phase_a_apply_resume.log`. Resume summary: 1,286 projects resolved, 7,234 resolution-log rows written, changed fields `confidence` 280, `date_delivery` 233, `developer` 281, `pipeline_status` 9, `total_units` 6. `canonicalize-developers --apply` then scanned 316 registry rows, merged 1 row, created 172 rows, created 5 aliases, and changed 26 project developer values. |
| A.9 | Spot-check 10-20 projects post-apply | `done` | 2026-04-23: Post-apply verification passed on representative status, units, delivery-date, and developer cases. Held overrides stayed intact for 20 large-unit defers and 4 developer exceptions; current DB now shows 23 projects with researcher overrides and 0 override/value mismatches. Spot checks confirmed: Miles at Highland is `Complete` with a non-future delivery date, large-unit defer and small-unit accept cases landed correctly, and architecture-firm developer exceptions (`NOW`, `Pico Gateway Apartments`, `2023 WESTWOOD BOULEVARD`) kept the current developer. One project (`Lake on Wilshire`) was re-resolved after `canonicalize-developers --apply` clobbered a raw developer override; code now prevents that path from recurring. |

### Phase B: Frontend — Read-Only Explorer
> **Goal:** A read-only interface over the validated pipeline data. Researchers can browse, search, and inspect projects + evidence but cannot yet edit. Sets expectations: this is not yet the Pipedream replacement — it's the preview that validates the data.
> **Priority:** High — informs all subsequent design decisions.
> **Depends on:** Phase A validated data, plus the three schema prerequisites below (B.0a, B.0b, B.0c).
> **Required reading:** `docs/specs/ui_requirements.md` (primary UI spec), `docs/specs/data_model_changes.md` (schema changes required), `docs/specs/field_inventory.md` (field class audit — prerequisite), `docs/specs/EVIDENCE_LAYER_INTEGRATION_GUIDE.md`, `ARCHITECTURE.md` Section 3d (Pipedream field inventory), Section 3e (master project record), Section 6 (matching strategy).

| Step | Task | Status | Notes |
|------|------|--------|-------|
| B.0 | **Prerequisite:** take a production DB snapshot before schema migrations | `done` | 2026-04-24: `pg_dump` custom-format schema+data snapshot written to ignored local path `data/output/db_snapshots/prod_snapshot_20260424_184916.dump` (3,155,000 bytes). SHA256 `32840FCEE59A4784963A6CC329160A799DEB62A8D388E999486520DC887DAF6D`. `pg_restore -l` read the archive TOC successfully. |
| B.0a | **Prerequisite:** complete the field inventory audit | `done` | 2026-04-24: Audited `docs/specs/field_inventory.md` against live SQLAlchemy models and source ingesters/adapters. Added missing live `Project` fields (`applicant`, `description`, CoStar physical fields, lifecycle/planning fields, geocode/status/review metadata). Verification script confirmed 0 unclassified live `Project` columns remain. |
| B.0b | **Prerequisite:** data model migrations for jurisdictions + markets | `done` | 2026-04-24: Added `markets` and `jurisdictions` tables, seeded `los_angeles` / `city_of_los_angeles`, added nullable `projects.market_id` and `projects.jurisdiction_id`, and applied migrations through `202604240005` to production. Backfill verification: 1,362 `los_angeles` projects, 0 missing `market_id`, 0 missing `jurisdiction_id`. Existing `projects.market` / `projects.jurisdiction` string columns remain in place until all CLI, collector, matcher, and UI reads have migrated. |
| B.0c | **Prerequisite:** source_runs expansion + source_registrations table | `done` | 2026-04-24: Added `source_registrations` and seeded 7 City of LA sources from `config/markets/los_angeles.yaml`. Expanded `source_runs` with `jurisdiction_id`, `trigger_type`, user/job metadata, finish time, row counts, and error text. Production verification: 7 source registrations, 7 historical source runs, 0 null `trigger_type` values. |
| B.1 | Next.js scaffolding + Supabase + shadcn + auth + nav shell + one live read path | `done` | 2026-04-25: Vercel project `the-concord-group/tcg-pipeline` is linked and deployed to production at `https://tcg-pipeline.vercel.app`; production env vars are set and Supabase Auth redirect URLs were configured. Production smoke passed: `/login` 200, `/coverage` 307 to `/login?next=%2Fcoverage`, disallowed email submit 303 to `/login?error=not_allowed`, allowed `ng@theconcordgroup.com` submit 303 to `/login?sent=1`, real inbox magic-link click lands on authenticated Coverage with jurisdiction rows visible. Added `.vercelignore` so local secrets/data are not uploaded by CLI deployments. Preview env vars are not set yet because the CLI required branch-scoped Preview variables in this session; set them from the dashboard before relying on preview deployments. Original B.1 implementation checks passed: `npm run typecheck`, `npm run lint`, `npm run build`, and `npm audit --audit-level=moderate`. |
| B.2 | Coverage view | `done` | 2026-04-25: Coverage view implemented and visible in production after authenticated magic-link smoke. Includes Supabase-backed jurisdiction/project/source/review-item aggregation, filter bar, session-sticky filters, local-only pins, optional columns, queue priority counts, source freshness, expandable source detail, and disabled Phase C actions. Added read-only authenticated RLS for `review_items` so Coverage can show queue/deferred counts without opening writes. `npm run typecheck`, `npm run lint`, `npm run build`, `npm audit --audit-level=moderate`, and `pytest -q` passed. Refresh and Upload buttons remain disabled until Phase C. |
| B.3 | Pipeline list view (table + map) | `done` | 2026-04-25: Implemented read-only Pipeline with Supabase-backed project/latest-evidence/APN data, dense sortable table, collapsible filter sidebar, sticky filters, local saved views, row hover preview, keyboard navigation (`j`/`k`, Enter, `/`, Escape), command search, read-only preview drawer, and tokenless MapLibre map view with configurable raster tiles and status-colored clustered pins. Added `project_latest_evidence` Postgres read view so Pipeline no longer fetches the full `evidence` table. New Project remains disabled until Phase C. Verified with `npm run typecheck`, `npm run lint`, `npm run build`, `npm audit --audit-level=moderate`, and `python -m pytest -q`. Deployed to production at `https://tcg-pipeline.vercel.app`; logged-out `/pipeline` redirects to `/login?next=%2Fpipeline`. |
| B.4 | Project Detail — Snapshot tab | `done` | 2026-04-25: Implemented direct Project Detail route at `/pipeline/[projectId]` and wired Pipeline table rows, command search, keyboard Enter, and map popups to open it. Snapshot is read-only and renders Core, Source Facts, Identity, Notes, Relationships, and Computed sections with field-class badges, source badges, hover provenance popovers, and amber highlights for fields in open review items. Added `project_field_resolution` Postgres read view over latest `resolution_log` rows for per-field source/rule/confidence data. Verified with `npm run typecheck`, `npm run lint`, `npm run build`, `npm audit --audit-level=moderate`, and `python -m pytest -q`. |
| B.5 | Project Detail — Evidence tab | `done` | 2026-04-25: Implemented read-only Evidence tab at `/pipeline/[projectId]?tab=evidence`. It renders the project's evidence rows chronologically with month dividers, native collapsible rows, generic snippets, extracted-field summaries, raw JSON expansion, source URL links from `project_source_records`, and server-rendered filters for field, source, and date range. Snapshot source badges now link into the Evidence tab filtered to that field. Source-specific snippet renderers and suspect-row writes remain deferred. Verified with `npm run typecheck`, `npm run lint`, `npm run build`, `npm audit --audit-level=moderate`, and `python -m pytest -q`. |
| B.6 | Project Detail — Resolution + Changes + Overrides tabs | `done` | 2026-04-26: Added read-only Project Detail tabs for Resolution, Changes, and Overrides. Resolution reads latest per-field rows from `project_field_resolution`, separates changed resolver output from collapsed unchanged tracked output, and shows current/resolved values, rule, confidence, linked evidence, and evidence drill-through only when evidence rows are linked. Changes reads both `change_log` review-commit rows and `status_history` lifecycle rows so the tab has useful pre-Phase-C activity. Overrides initially read the legacy `projects.researcher_override` JSONB payload; C.tail.2 moved the tab to active `researcher_overrides` rows. Alternatives considered and superseded override history are not currently stored, so the UI states those Phase C limitations honestly. |
| B.7 | Dashboard (5 tiles) | `done` | 2026-04-26: Implemented the read-only Dashboard at `/dashboard` with five tiles from `docs/specs/ui_requirements.md` §18: Needs Attention, Stalled Candidates, Contradictions, Pipeline By Status, and Recent Activity. Tiles use existing Phase B read access only: projects, review_items, recent evidence, source_runs, and markets. Pipeline status tile links now initialize Pipeline status filters from `?status=` query params. Stalled candidates are a read-only heuristic for Approved/U/C projects with real evidence dates older than 12 months; formal stall review-item generation remains Phase E. Contradictions renders as a Phase C inactive state until contradiction-type review items exist. Needs Attention links to Coverage until the Review Queue is built. |
| B.3.1 | Pipeline page tab-crash on production with full dataset | `not_started` | 2026-05-04: `/pipeline` on `https://tcg-pipeline.vercel.app` triggers the browser's tab-crash recovery UI ("This page couldn't load. Reload Back") with no console errors. Server-side `getPipelineData` succeeds — the streamed RSC payload contains all 1,362 hydrated `PipelineProject` rows. Crash happens during client render: `pipeline-client.tsx` hydrates the full dataset, builds 1,362 MapLibre features, and renders the table without server-side limit/pagination or row virtualization. `/coverage` and `/dashboard` work fine. Fix options: (a) add server-side limit/cursor to `getPipelineData` and stream additional pages on demand; (b) virtualize the results table with `@tanstack/react-virtual` (already in the dep tree via react-query); (c) cluster the map at low zoom and lazy-load features in viewport. Probably want all three eventually. Independent of Tier 2 deployment work. |

### Phase C: Write Path + Review Queue
> **Goal:** Researchers can edit any field, create new projects, review the system's proposed changes, and commit decisions in batch. This phase is where the tool replaces Pipedream.
> **Priority:** High — core value proposition. Follows Phase B.
> **Depends on:** Phase B (read-only surfaces give researchers context), FastAPI backend scaffolding, contradiction detection service.
> **Structure:** Split into two sequenced halves. C-early ships editing (unlocks Pipedream replacement workflow). C-late ships the review queue (unlocks system-proposed-change review).
> **Required reading:** `docs/specs/review_workflow.md`, `docs/specs/ui_requirements.md`, `docs/specs/EVIDENCE_LAYER_DECISIONS.md` §22 (review-protected override semantics).

#### C-early: Inline editing + new project creation

| Step | Task | Status | Notes |
|------|------|--------|-------|
| C.a | FastAPI backend scaffolding | `done` | 2026-04-26: Added FastAPI write-path scaffold under `src/tcg_pipeline/api/` with health/readiness, Supabase JWT verification plus auth-server fallback, allowed-email enforcement, DB session dependency, C.a protected route stubs, API tests, env/docs updates, and Render runbook. Verified `pytest`, `npm run lint`, `npm run typecheck`, `npm run build`, and scoped ruff for the new API files. |
| C.a-bis | Staging write environment gate | `done` | Before any real Phase C write path is used from Vercel previews, stand up and verify staging Supabase + staging FastAPI/Render or explicitly re-decide the preview write policy. Production Supabase previews were acceptable for Phase B read-only; Phase C writes need an isolated target or a documented exception. 2026-04-26: Added repeatable `researcher_overrides` migration verification commands/runbook for the pre-C.d gate, including metadata mismatch checks, index/RLS/policy/grant checks, pre/post resolution snapshots, and verbose drift output. 2026-04-27: Took a DB snapshot of the configured database, applied Alembic `202604260010`, verified summary counts/artifacts (`24` active rows, `0` drift, RLS/index/policy/grant present), and passed post-migration resolution compare against the pre-migration snapshot. Preview writes are blocked by default; previews may use read-only Phase B Supabase access, while mutation testing requires an explicitly approved staging target or temporary production write session. |
| C.a.1 | Developer registry + apply-script hardening | `done` | 2026-04-26: Moved forward from E.3a to unblock Phase D news ingestion. Added `scripts/audit_developer_registry.py` shadow/apply-delete tooling over the shared meaningful-token overlap guard; destructive apply now prunes unsafe aliases for safe canonical rows and only deletes whole canonical rows when the canonical name itself is unsafe. Shadow audit against the configured DB returned 0 flagged canonical rows with `>=3` aliases. A one-time looser `--min-aliases 1` shadow audit found and then pruned two confirmed-bad aliases: `Marmar Group` under `The Panorama Group`, and `Realm Group LLC` under `The Remm Group`; follow-up `--min-aliases 1` and `--min-aliases 3` shadow audits both returned 0 flagged rows. Refactored Phase A architecture-firm exceptions from raw-value-only to scoped `(project_id, raw_value, expected_current_developer)` rows. Hardened developer registry persistence so generic-only canonical names are not inserted and unsafe aliases are ignored/dropped during exact matching, alias creation, and registry merges. |
| C.b | Source snippet renderers per source_type | `done` | 2026-04-26: Added backend `SnippetPayload` models and renderer registry under `src/tcg_pipeline/review/snippets.py`, with source-specific renderers for LADBS permit/inspection/CofO, CoStar, Pipedream, news articles, developer websites, researcher overrides, computed evidence, and generic fallbacks for future source types. Wired protected `GET /evidence/{id}/snippet?field=...` to load evidence rows and return snippets; missing auth returns `401` before DB access and missing evidence returns `404`. News article renderer reads stored highlights without re-extraction for Phase D. |
| C.c | Promote `researcher_override` to a table | `done` | 2026-04-26: Added `researcher_overrides` table migration and SQLAlchemy model with active `(project_id, field_name)` uniqueness, legacy JSONB backfill, authenticated read-only RLS, and no direct client write grants. Added shared override helpers that originally merged/dual-wrote legacy JSONB during transition. Resolution now reads table-backed overrides; C.i replaced silent supersession/clearance with contradiction review items. Review workflow writes overrides through the shared helper; developer canonicalization respects active table overrides. 2026-04-28: C.tail.2 removed the legacy JSONB column and legacy read/write branches after table-backed UI reads were verified. |
| C.d | Inline editing on Core fields (Evidence-derived) | `done` | 2026-04-27: Implemented FastAPI set/clear override endpoints for evidence-derived Core fields (`pipeline_status`, unit counts, `developer`, `product_type`, `age_restriction`, `date_delivery`). Writes go through `researcher_overrides` with Supabase user IDs, re-run `resolve_project(apply=True)`, update project edit metadata, and write `change_log` rows with `researcher_override`. New manual Core edits use `mode = review_protected`, not sticky: the manual value remains current, but any newer auto-resolved value that differs for that field creates an `override_contradiction` review item. Project Detail Core rows now show an inline edit control and override info cue; Vercel preview server actions remain blocked by default unless `ENABLE_PREVIEW_WRITES=true` for an approved session. |
| C.e | Inline editing on Identity + Notes fields | `done` | 2026-04-27: Implemented FastAPI direct-field writes through `POST /projects/{project_id}/field` for researcher-authored identity/workflow fields (`project_name`, aliases/raw addresses, city/state/county/ZIP, region, source URLs, planner contacts, inclusion flags/note). Added append-only `project_notes` table with authenticated read-only RLS and `POST /projects/{project_id}/note`; C.tail.3 removed the legacy latest-note project columns and Project Detail now derives latest-note previews from `project_notes`. Project Detail now uses the same inline editor shell for Core overrides, direct Identity fields, and append-only Notes; direct field and note writes go through Next.js server actions with preview writes blocked by default. |
| C.f | Relationship picker UI | `done` | 2026-04-27: Implemented Project Detail relationship picker in the Relationships Snapshot section. The picker searches existing projects by name/address through a Next.js server action, then writes links through FastAPI `POST /projects/{project_id}/relationship` into `project_relationships` with audit `change_log` rows. Supported relationship types are `phase`, `master_plan`, `counterpart`, `duplicate`, and `supersedes`; duplicate submissions are idempotent, and re-linking with a changed non-empty note updates the existing relationship note instead of silently discarding it. Incoming and outgoing links are shown on Project Detail. C.tail.5 later added outgoing relationship unlink/retype and explicit note clearing. Verified with `npm run typecheck`, `npm run lint`, `npm run build`, scoped `ruff check`, `python -m pytest tests/test_project_relationship_api.py tests/test_api_scaffold.py -q`, and `python -m pytest -q`. |
| C.g | New project creation flow | `done` | 2026-04-27: Enabled `[+ New project]` in Pipeline. The modal requires canonical address, market, and jurisdiction, with optional project name/city/county/ZIP. Next.js submits through FastAPI `POST /projects`; the API normalizes the address, runs the existing conservative matcher against the normalized address, and returns duplicate candidates before creating unless `force_create=true`. Confirmed creates write a `projects` row, initial Proposed `status_history`, edit metadata, and a `change_log` audit row, then the UI opens Project Detail for the new project. Duplicate handling offers Open existing or Create anyway; true merge into an existing project remains out of scope. Geocoding and DB-level duplicate locking landed in C.tail.4/C.tail.6; creation-time identifiers/source evidence remain future work. Verified with `npm run typecheck`, `npm run lint`, `npm run build`, scoped `ruff check`, `python -m pytest tests/test_project_creation_api.py tests/test_api_scaffold.py -q`, and `python -m pytest -q`. |

#### C-late: Review Queue + contradiction detection

| Step | Task | Status | Notes |
|------|------|--------|-------|
| C.h | `ReviewItem` / `ReviewDecision` staged/committed state machine | `done` | 2026-04-27: Added `review_items.state`, staged/committed `review_decisions` fields, and `change_log` user/email audit columns via Alembic. Refactored `review_workflow.py` into stage/revise/unstage/commit helpers, preserving legacy `status` values for Dashboard/Coverage counts. `/review/queue`, `/review/{id}/decide`, `/review/{id}/revise`, `/review/{id}/unstage`, and `/review/commit` are real FastAPI routes. Follow-up hardening translates staged-decision unique races to 409, wraps commit application in a savepoint, keeps staged non-deferred items visible as `In review`, and preserves staged discovery dismiss reasons. C.h requires `202604270013` to be applied before DB-backed tests or deployment. |
| C.i | Contradiction detection service | `done` | 2026-04-27: Added `tcg_pipeline.review.contradictions` with `detect_contradictions(...)` batch entrypoint and resolver-level `detect_project_contradictions(...)`. `resolve_project(apply=True)` now creates or updates one active `override_contradiction` item per `(project_id, field_name)`, invalidates stale open/staged contradiction items, and no longer treats `until_newer_evidence` as silent override supersession. Per-field thresholds follow §22: unit deltas must exceed 5, delivery dates must differ by more than 30 days unless recent article evidence is involved, developers compare normalized/registry-canonicalized names, and pipeline status / Tier 1 / large unit deltas escalate priority. Review commits skip invalidating the contradiction item currently being applied. Follow-up hardening added dry-run/apply `detect-contradictions`, a filtered `--only-with-overrides` audit mode, direct backfill contradiction scanning, baseline-less legacy override documentation, and integration coverage for accept-new contradiction commits. |
| C.j | Review Queue UI | `done` | 2026-04-27: Added `/review` as the researcher queue workspace backed by FastAPI `/review/queue` and review staging/commit server actions. The UI groups active items by project, sorts by priority with a deferred section, shows staged-by-me/staged-by-other state, supports A/S/D/F row decisions, project-level accept/keep actions, unstage/revise flows, filtered jurisdiction entry from Coverage, dashboard links to Review, and a persistent batch commit bar. Follow-up hardening scopes commits to the visible jurisdiction, lets staged `new_candidate` accepts create projects on commit, and replaces invalid discovery `candidate_N` staging with explicit possible-match project choices. C.k detail view, reviewed tab, richer source grouping, and multi-select bulk shortcuts remain later scoped items. |
| C.k | Review Item detail view | `done` | 2026-04-27: Added `/review/[itemId]` backed by FastAPI `GET /review/queue/{item_id}` plus Supabase project/source hydration. Queue rows now link to detail, and Custom routes to the detail form instead of using `window.prompt`. The detail view shows current vs proposed values, project/source context, possible-match candidate project cards, staged state, accept/keep/defer/unstage actions, and a custom value form with notes/source URL. Reviewed tab remains later scope; processed-items context and bracket-key navigation landed in C.k.1. |
| C.k.1 | Review detail hardening | `done` | 2026-04-27: Extracted shared review payload/coercion helpers into `lib/review/payload.ts` and added Vitest fixture coverage for status suggestions, contradictions, new candidates, possible matches, change fallbacks, candidate values, staged ownership, and payload flattening. Detail links preserve jurisdiction context, `/review/[itemId]` now computes previous/next active queue navigation with `[` / `]`, and the page shows recent committed `change_log` entries for the same project field. Staged banners now require staged state, include timestamps, payload rows flatten one nested level without silent truncation, and actor fallback labels are more readable. |
| C.l | Coverage — scrape kickoff + CoStar upload | `done` | 2026-04-27: Added `scrape_jobs` and `costar_uploads` via Alembic `202604270014`, SQLAlchemy models, and FastAPI Coverage endpoints. `/coverage/{jurisdiction_id}/scrape` validates active non-CoStar source registrations and creates queued user-initiated jobs; `/scrape_jobs/{id}` returns pollable status. `/coverage/{jurisdiction_id}/costar-upload` accepts multipart `.xlsx` uploads, records uploader/file audit data, runs the existing CoStar seed importer, creates a jurisdiction-scoped `source_runs` freshness row, and preserves failed upload audit rows. Coverage source rows now enable Refresh/Upload actions through server actions with bounded job polling. External scraper worker consumption of queued jobs remains a deployment/collector integration task. |
| C.l-bis | Coverage scrape hardening | `done` | 2026-04-27: Added Alembic `202604270015` partial unique index for one active `scrape_jobs` row per jurisdiction/source and idempotent enqueue behavior. Supported Socrata/LADBS sources (`ladbs_permits`, `ladbs_permit_activity`, `ladbs_inspections`, `ladbs_cofo`) now run through a FastAPI background task using incremental cursors and `persist_collected_records`; unsupported sources fail clearly instead of queueing jobs no worker consumes. Scrape job polling is jurisdiction-scoped, Coverage disables unsupported Refresh buttons, and polling backs off over a longer window. CoStar uploads are capped at 50 MB in Next.js and FastAPI, with direct tests for success/failure audit behavior. C.tail.1 later added the durable RQ/Redis worker path for production. |
| C.m | Inclusion flags UI | `done` | 2026-04-27: Promoted `inclusion_in_analysis`, `inclusion_in_exhibit`, and `inclusion_note` into a dedicated Project Detail Snapshot inclusion panel. Binary fields now use toggle controls backed by the existing FastAPI direct-field write path, and the note editor supports explicit clearing through the same `/projects/{id}/field` endpoint. Removed duplicate generic inclusion rows from the Notes section and added backend regression coverage for string boolean coercion plus note clearing. |
| C.n | ChangeLog view | `done` | 2026-04-28: Upgraded Project Detail → Changes into a filtered audit view over `change_log`, append-only `project_notes`, and lifecycle `status_history`. The tab now filters by actor, date range, field, source, and change type; displays full email identities when C.h audit columns are present; keeps older legacy actor labels visible; links ChangeLog rows back to review items when available; and surfaces project note history rather than only the latest synchronized note column. Direct Phase C audit writers now populate `reviewed_by_user_id` and `reviewed_by_email`, and shared Project Detail change-filter helpers have Vitest coverage. |
| C.o | Reviewed tab in Review Queue | `done` | 2026-04-28: Added a Reviewed tab to `/review` backed by committed `ReviewItem` / `ReviewDecision` state. `GET /review/queue?state=committed` now returns the latest committed decision summary for each committed item, ordered by commit time for audit use. The frontend loads active and reviewed items together, preserves jurisdiction scoping, and provides reviewed filters for search, field, outcome, and decider plus sorting by date, decider, or project. Reviewed rows link to the review item detail and the project ChangeLog filtered to the decided field. Added backend serialization coverage and Vitest coverage for reviewed-row filtering/sorting helpers. |

#### Phase C Tail Cleanup (non-blocking)
> **Goal:** Close deferred Phase C work without reopening Phase C completion. These steps are either Phase D runway, transitional-storage retirement, safety hardening, or UX/policy polish.
> **Sequencing:** C.tail.4 and C.tail.1 are complete Phase D runway. C.tail.2/C.tail.3 retired transitional storage. The remaining rows can be scheduled opportunistically.

##### Phase D runway

| Step | Task | Status | Notes |
|------|------|--------|-------|
| C.tail.1 | Durable Render worker (RQ + Redis) | `done` | 2026-04-28: Added Redis/RQ-backed scrape job dispatch with `tcg-pipeline worker` for a dedicated Render worker service. Coverage Refresh now writes the existing `scrape_jobs` row, queues RQ execution when `REDIS_URL` is configured, and keeps the FastAPI background-task fallback for local/dev. Added authenticated worker health and per-source scrape-job history endpoints plus a Coverage "History" expansion. RQ workers process one job at a time, so concurrency is controlled by Render worker instance count. |
| C.tail.4 | Geocoding for manually-created projects | `done` | 2026-04-28: Added a reusable FastAPI-side Geocodio-first / Esri-fallback geocoder based on the `tcg-project-search` approach. Manual `POST /projects` now geocodes after duplicate checks, accepts high-confidence Geocodio results, automatically retries with Esri when Geocodio is not high-confidence, writes reliable coordinates to `lat`/`lng`/`location` with `geocode_confidence`, and records provider/fallback metadata in `change_log`. Geocoding failures do not block project creation; D.7 paste-a-link can reuse the same backend module. |
| C.tail.4-bis | Geocoding remediation path | `done` | 2026-04-28: Added FastAPI `POST /projects/{project_id}/geocode`, which derives a query from the stored canonical address/city/state/ZIP, reuses the Geocodio-first / Esri-fallback geocoder, updates `lat`/`lng`/`location`/`geocode_confidence` only for accepted results, and audits accepted/skipped/failed/low-confidence attempts through `change_log` source `manual_geocode`. Project Detail Snapshot now shows a Map Location panel with a Re-geocode action for missing or non-high-confidence coordinates. |
| C.tail.11 | Backend decision-card consolidation | `done` | 2026-04-28: Added Alembic `202604280018` for `review_items.field_name`, `review_items.winning_evidence_id`, `review_items.updated_at`, and partial unique index `uq_review_items_active_project_field_type` enforcing one active field-based card per `(project_id, field_name, item_type)`. Added `src/tcg_pipeline/review/decision_cards.py` to upsert same-proposal cards, merge `payload.evidence_ids`, refresh representative source metadata, translate proposal flips into invalidation plus a fresh item, and drop staged decisions whose proposal changed. `persist_collected_records` now uses this path for matched status/field review items; discovery items remain per-record. Override contradictions now use the same card shape with `field_name`, `winning_evidence_id`, and combined supporting/candidate `evidence_ids`. Added `scripts/collapse_duplicate_review_items.py` dry-run/apply preflight for existing duplicate rows; it aborts on staged duplicate groups unless `--migrate-staged` is provided. Deployment requires running the collapse script before `202604280018` on databases with existing review items. |
| C.tail.12 | Decision-card UI on Review Queue | `done` | 2026-04-28: Review Queue and Review Detail now render consolidated decision cards with one action group and collapsible Supporting/Against evidence sections. FastAPI `GET /review/queue` and `GET /review/queue/{item_id}` hydrate `evidence_summaries[]` server-side from `payload.evidence_ids`, classify stance with the existing contradiction threshold rules, render source-specific summary text through `review/snippets.py`, and mark `winning_evidence_id`. Frontend maps the new response shape, adds `supportingEvidenceForItem`, `dissentingEvidenceForItem`, and `winningEvidenceForItem` helpers with Vitest coverage, highlights winning evidence with a star, and keeps discovery item behavior unchanged. |

##### Transitional storage retirement

| Step | Task | Status | Notes |
|------|------|--------|-------|
| C.tail.2 | Drop legacy `projects.researcher_override` JSONB | `done` | 2026-04-28: Moved Project Detail Overrides and Coverage override counts to active `researcher_overrides` rows, removed legacy JSONB read/write/normalization branches from `db/researcher_overrides.py`, deleted the retired verifier script/runbook, and added Alembic `202604280017` to drop the column. The migration includes a preflight guard that fails if any legacy override value lacks a matching active table row or diverges from the table value. |
| C.tail.3 | Drop legacy project note columns | `done` | 2026-04-28: Project Detail Snapshot latest-note previews now derive from `project_notes`, note appends no longer mirror into `projects.researcher_notes`, `personal_notes`, or `change_notes`, Pipedream imports now create `project_notes` rows for imported notes, and Alembic `202604280017` drops the three legacy note columns after checking that non-empty legacy values match latest `project_notes` rows. |

##### Safety and correctness hardening

| Step | Task | Status | Notes |
|------|------|--------|-------|
| C.tail.6 | DB-level duplicate lock for project creation | `done` | 2026-04-28: Added transaction-scoped Postgres advisory locking around manual `create_project` duplicate checks, keyed by `(market_id, canonical_address)`. This serializes same-address manual create races so the second request re-runs the matcher after the first commits and returns the existing `created = false` duplicate-candidate response. Alembic `202604280016` is a no-op marker revision; a unique index on `projects(market_id, canonical_address)` was rejected because legitimate multi-phase projects can share one address. |

##### UX and audit polish

| Step | Task | Status | Notes |
|------|------|--------|-------|
| C.tail.5 | Relationship unlink, retype, and note clear | `done` | 2026-04-28: Added FastAPI `PATCH /projects/{project_id}/relationship/{relationship_id}` and `DELETE /projects/{project_id}/relationship/{relationship_id}` for outgoing relationship maintenance. Retypes, note edits, explicit note clears, and unlink operations update project edit metadata and audit old/new relationship payloads through `change_log` with Supabase user ID/email. Project Detail now exposes inline controls for outgoing links while keeping incoming links read-only from the target project. |
| C.tail.7 | Reviewed tab date range and history scaling | `not_started` | Low priority. The active/reviewed fetch parallelization landed in C.o; remaining work is API-backed `committed_after` / `committed_before` filters, an explicit newest-N cap message or pagination for committed history beyond the current 500-row limit, and optional date dividers for skimability. |

##### Operational policy decisions

| Step | Task | Status | Notes |
|------|------|--------|-------|
| C.tail.8 | Cleared override retention policy | `not_started` | Low priority and mostly a decision. After C.tail.2 removes the dual-write era, decide whether cleared `researcher_overrides` rows are retained indefinitely, archived after N years, or moved to cold storage. If indefinite is chosen, document it explicitly to avoid future re-litigation. |
| C.tail.9 | Symmetric relationship semantics | `not_started` | Lowest priority. Decide whether symmetric relationship types (`phase`, `counterpart`, `duplicate`) should auto-create reverse rows or remain one-way with incoming/outgoing display. Asymmetric types (`master_plan`, `supersedes`) stay one-way. Document the decision before relationship volume grows. |
| C.tail.10 | Repository line-ending policy | `not_started` | Lowest priority repository hygiene. Add `.gitattributes` line-ending rules to remove recurring CRLF warning noise from future diffs. |

### Phase D: News Scraping & Deep Research
> **Goal:** Automated collection of news articles about development projects. First scheduled pilot source: Urbanize LA. Enriches the evidence layer with Tier 2 signals.
> **Priority:** High — fills critical intelligence gaps that government sources and CoStar miss (developer announcements, project milestones, community opposition, timeline updates).
> **Depends on:** Phase A (evidence layer must be validated), C.tail.1 (RQ/Redis worker), C.tail.11/12 (decision-card consolidation — Phase D runway).
> **Required reading:** `docs/specs/news_research_design.md` is the implementation contract for this phase. Also: `config/source_tiers.yaml` (Tier 2 = news_article), `ARCHITECTURE.md` Section 5 (collection workflow), Section 6 (matching strategy).
> **Sequencing:** Paste-a-link and extraction runway shipped first. The scheduled-source path now pilots on Urbanize LA through a generic polite collector. LA YIMBY, The Real Deal LA, BizJournals LA, and full advanced fetch implementation are explicit Phase D-late items after the Urbanize end-to-end path is proven.
> **Effort note:** Deferring full advanced-fetch implementation makes revised Phase D several days smaller than the advanced-fetch/day-one proposal. That work is rephased into D.late.ADV/D.late.C, not removed.

| Step | Task | Status | Notes |
|------|------|--------|-------|
| D.1 | News research schema + dependency install | `done` | 2026-04-29: Added news research schema, safe summary views/RLS posture, seed rows, source-type mappings, `evidence.superseded_at`, `scrape_jobs` news extension, and Phase D dependencies. Configured development DB migrated to Alembic head `202604290020`. |
| D.W | Extend C.tail.1 RQ worker for news job kinds | `done` | 2026-04-29: Registered the four design news job kinds, added worker heartbeat writes, `/healthz`, scheduler-leader loop, scheduler dedup, alert upsert hardening, and placeholder task handlers that fail clearly until their later Phase D implementation steps. |
| D.7a | Paste-a-link minimal vertical (Pass 0 only) | `done` | 2026-04-29: Added `POST /research/articles` and `GET /research/articles/{id}` behind FastAPI auth, seeded the fallback `news_paste_a_link` source, canonicalized/deduped pasted URLs, queued `news_paste_a_link` scrape jobs with local background fallback, and implemented worker Pass 0 via httpx + trafilatura metadata/body extraction. Force re-extract remains deferred to D.3d/D.4. |
| D.3a | Pass 1 structural extractor library | `done` | 2026-04-29: Added the Pass 1 structural extractor library with regex/date/address/status/delivery/product/age/affordable-split signals plus Aho-Corasick developer/project dictionary scans. Paste-a-link worker now runs Pass 1 immediately after successful Pass 0 fetches and stores `news_articles.structural_signals` / `structural_signals_at`. |
| D.3b | Pass 2a triage (Haiku 4.5) | `done` | 2026-04-29: Added versioned `triage_v1` prompts/config, Anthropic Haiku triage client, broad-net JSON parser with uncertain-negative override, `news_extractions(pass='triage')` persistence, article `triage_status` updates, and race-protected cost-cap reservation/rollup writes in `news_extraction_costs` with separate cache-creation token accounting. Paste-a-link worker now runs triage after successful Pass 0/1 and skips with a system alert when `ANTHROPIC_API_KEY` is missing. |
| D.3c | Pass 2b extraction (Opus 4.7) | `done` | 2026-04-29: Added versioned `extract_v1` prompt/schema, shared LLM pricing/usage helpers, Anthropic Opus extraction client, strict output parser, glossary + signal-flag registry rendering in cached system prompt blocks, `news_extractions(pass='extraction')` persistence, pending `news_project_references`, article `current_extraction_id`/version updates, and paste-a-link worker wiring after triage-relevant articles. Matching, evidence integration, review items, re-extraction triggers, and materially-changed gating remain with D.3d/D.4. |
| D.3d | Extraction-time Pass 3a re-extraction | `done` | 2026-04-29: Added extraction-time Pass 3a trigger detection for Pass 1/2 load-bearing conflicts, low-confidence references, and parse/schema/refusal/truncation outcomes. Added `reextract_v1` prompt/schema, re-extraction cost accounting under `pass='reextraction'`, `supersedes_extraction_id`, current-extraction pointer update only on clean re-extraction parse, worker progress fields, and focused tests. Match-triggered Pass 3b remains with D.4. |
| D.4 | Article matcher + integrator + Pass 3b | `done` | 2026-04-30: Added market-agnostic `news_matcher` Tier 1/2/3/4 matching with live developer/project registry UUID validation, match-triggered Pass 3b re-extraction on `new_candidate`, and the news evidence integrator. Paste-a-link now flows extract -> match -> optional Pass 3b -> evidence write -> `resolve_project(apply=True)` -> post-apply review items. Confirmed project changes create one `STATUS_CHANGE` item per changed field with a single-element `payload.changes` list; possible/new-candidate items write orphan news evidence and payloads compatible with `_link_orphan_evidence` accept paths. Follow-up hardening supersedes stale article evidence before resolver reads, marks superseded extraction references terminal, adds an active article-evidence index, and aligns change rows with the frontend/spec evidence-id contract. Integration records/updates `SourceRun` counters. C.a.1 blocker was cleared by completed C.a.1 before implementation. D.4-resolver remains separate. |
| D.4-resolver | §21f recent-article delivery-date priority + supersession filter | `done` | 2026-04-30: Resolver reads now exclude superseded evidence rows in the project evidence loader and contradiction supporting-evidence query. `delivery_year.py` implements §21f by allowing recent `news_article` delivery-date evidence within 180 days to outrank CoStar while preserving higher-priority TCG/government winners. Added focused resolver tests for superseded evidence filtering and recent/stale news delivery-date priority. |
| D.5 | Review-queue news context rendering | `done` | 2026-04-30: Review queue rows and detail pages now render D.4 `news_context` with article links, extraction-confidence chips, reference index, prompt metadata, and structural-disagreement chips/payload detail while keeping news evidence inside the existing consolidated decision-card evidence sections. Added frontend payload helper coverage plus backend serialization coverage proving news context and `render_news_article_snippet` summaries flow through review API responses. |
| D.7b | Paste-a-link UI + article admin view | `done` | 2026-04-30: Added `/research` paste-a-link form, `/research/articles/{id}` article admin view with job/extraction/reference/body details, app navigation entry, and a guarded retry/refetch endpoint + server action for terminal fetch failures. Retry reuses active paste-a-link jobs, preserves any original `force_project_id`, resets failed articles to `pending`, and requeues through the existing RQ/background fallback. |
| D.2v | Urbanize/source validation pass | `done` | 2026-04-30: Validated `la.urbanize.city` robots/RSS/sitemap behavior, confirmed RSS incremental and sitemap backfill paths, and ran five representative Urbanize articles through `POST /research/articles` in the development DB with Pass 0/1 enabled. All five fetched cleanly as open articles; Pass 1 exposed follow-up tuning needs for comma-formatted unit counts, title-only addresses, and completion-date phrases. Added `docs/sources/README.md`, `docs/sources/news/urbanize_la.md`, and a design-doc validation addendum. Light LA YIMBY/TRD reconnaissance recorded for D.late.E1/E2. |
| D.2-docs | Revise Phase D news design for Urbanize pivot | `done` | 2026-05-01: Updated `docs/specs/news_research_design.md`, `docs/sources/news/urbanize_la.md`, and this roadmap so Phase D reads Urbanize-first rather than BizJournals-first. Locked in market-unscoped `urbanize_la`, per-source backfill-window configuration, D.6 staging smoke-test gate, and D-late source expansion boundaries. Reconciled stale Pass 3 `triggered_by` examples so `pass2_parse_error` is documented. |
| D.2a-prep | Pass 1 Urbanize validation tightening + fixtures | `done` | 2026-05-01: Tightened structural extraction for comma-formatted unit counts (`2,250 residential units`), title/headline address scanning (`2101 W. 8th Street`), numbered street names, and completion-date phrases (`Completion is expected in Fall 2027`). Added sanitized Urbanize validation fixtures plus LA YIMBY-like RSS/article fixtures for D.2a collector tests. Verified with `python -m pytest tests/test_news_structural.py -q` and `ruff check src/tcg_pipeline/news/structural.py tests/test_news_structural.py`. |
| D.2a | Generic polite NewsCollector + Urbanize LA pilot | `done` | 2026-05-01: Added `PoliteNewsCollector` with config-driven RSS incremental discovery, sitemap-index/backfill discovery, robots.txt cache/crawl-delay handling, per-host rate limiting, `Retry-After` surfacing for block-like statuses, conditional GET headers, and hard failure for deferred `fetch_path='advanced'`. Added migration `202605010025` to disable/unschedule `bizjournals_la` and seed market-unscoped active `urbanize_la` with host/RSS/sitemap/robots/source-doc config and cron candidate `30 7 * * *` in `America/Los_Angeles`. Paste-a-link source routing now reads active `news_sources.config.hosts` through a 5-minute in-process cache, so Urbanize URLs are stored against `urbanize_la` while unknown hosts keep the fallback `news_paste_a_link` source. Added `urbanize_la -> news_article` logical source mapping plus RSS/sitemap/article fixture tests using Urbanize/LA YIMBY-like fixtures. D.6 still owns the Anthropic-key staging rerun of the five D.2v URLs through the host-routed `urbanize_la` path before scheduled cron goes live. |
| D.2b | Advanced fetch routing interface, implementation deferred | `done` | 2026-05-01: Added shared fetch-path constants and worker-side guardrails for source configs requesting `fetch_path='advanced'`. Paste-a-link jobs now carry `fetch_path` in start/final progress diagnostics; `advanced` sources fail before HTTP fetch with a `news_advanced_fetch_deferred` system alert, source strategy doc detail, and article/job terminal failure. `PoliteNewsCollector` still hard-fails `advanced` directly. Full browser/proxy/TLS/paid-source implementation remains deferred to D.late.ADV/D.late.C and requires human approval before any source config flips to `advanced`. |
| D.2 | (Superseded) BizJournals-first fetcher + cookie-injection auth | `superseded` | Replaced by D.2a Urbanize-first generic polite collector and D.late.C paid-source capability. Keep `bizjournals_la` mapped to `news_article`, but make it inactive and unscheduled until D.late.C. |
| D.6 | Scheduled Urbanize scrapes + minimal source health | `in_progress` | 2026-05-01: Worker-side scheduled scrape implementation is in place: `news_scrape` jobs instantiate `PoliteNewsCollector`, run RSS incremental discovery, persist new `NewsArticle` rows under `urbanize_la`, fetch article bodies through the polite path, and reuse the existing triage/extraction/integration pipeline. Added transient 5xx retry/backoff, structured block-like 401/403/429/503 tracking with active-source auto-pause/system alerts, cost-cap skip counters, deterministic per-source scheduler jitter, source-run/job counters, article URL hash dedup, and a Coverage news source health panel showing active/paused state, fetch path, last run, discovered/fetched/failed/cost-cap counts, and last alert. `last_scrape_started_at` remains derived from `source_runs` per §12.5. Anthropic smoke against the configured development environment passed for the five D.2v Urbanize URLs through the `urbanize_la` scheduled-source path after raising `NEWS_EXTRACT_MAX_TOKENS` to 5000. First staging smoke proved the full path but exposed 100% Pass 3a fire rate; D.6 tightened Pass 3a structural-conflict detection and reran smoke, reducing Pass 3a to 1 trigger. A clean 5/5 staging smoke on 2026-05-02 under an audited temporary `$50` cap (`d6-smoke-staging-20260502a`) completed with 0 fetch/block-like/transient/cost-cap failures, 5 extracted articles, 1 Pass 3a trigger, 3 Pass 3b triggers, 8 review items, and `$7.27` measured LLM cost. Still pending before production cron sign-off: cleanup of smoke artifacts, deployed env var confirmation, `NEWS_SCHEDULER_LEADER=true` on exactly one worker, first-cron observation, and Redis/shared per-host limiting evaluation before multiple concurrent news workers hit the same host. |
| D.M | Monitoring, alerting, cost-cap admin UI | `not_started` | SMTP email dispatcher to `ng@theconcordgroup.com`. `system_alerts` reader for the Coverage banner. Rich news ops dashboard in Coverage: discovery/fetch/extraction health, fetch-path distribution, robots status, articles per dollar, review acceptance by source, last alert, and manual cost-cap bump UI. Add a newest-first, table-format source-run/scrape ledger for Phase D news runs with filter/sort by source, trigger, status, date, and cost; columns should include source, run/job ids, discovered/fetched/extracted counts, review/re-review/conflict/new-candidate counts, Pass 3a/3b counts, failure counters, token/cost totals, and drill-through links to articles/extractions/review items. This is the news-specific predecessor to the broader platform admin ledger in Phase J. Add per-source health cards; when a source is auto-paused for repeated `401/403/429`, show a "Request advanced fetch" action that records a senior-approval request. Approval may flip `fetch_path` only after D.late.ADV ships; before then the request is informational and prioritizes advanced-fetch work. **Source admin controls (in-app schedule editing):** add a `/research/sources` admin page (or Coverage admin tile) with FastAPI `PATCH /research/sources/{id}` for editing `schedule_cron` (with cron-expression validation), `schedule_timezone`, and active/paused state, plus a `POST /research/sources/{id}/run-now` force-trigger. Pair with the `/research/sources/{id}/pause` and `/resume` routes from spec §18.6. Every mutation writes a `news_admin_actions` audit row. This replaces today's "edit via Alembic migration" flow so frequency tuning during Phase D rollout doesn't require a deploy. Worker monitoring should document stable production `WORKER_NAME` values, clean up stale heartbeat rows, fail stale RUNNING jobs whose worker heartbeat has expired, sweep stale `_reservation_` cost rows left by worker crashes, measure extraction body-length/offset accuracy before any truncation policy, and add graceful shutdown signaling if long-lived scheduler/heartbeat threads need draining. Depends on D.6. |
| D.sec | Phase D summary-view ownership audit | `deferred` | Audit `news_*_summary` owner/grant behavior in environments where the Alembic role can alter view ownership. Current migrations use `security_invoker=false` and authenticated grants, but owner normalization to `news_summary_reader` needs a dedicated privilege-aware migration/test rather than a one-off downgrade patch. |
| D.G | Discarded-articles graveyard | `not_started` | `/research/graveyard` browseable list per §14. Manual relink endpoint `POST /research/articles/{id}/references/{ref}/relink` writes evidence + audit (`news_admin_actions`). Depends on D.5. |
| D.B | 8-week Urbanize LA backfill (per-geo configurable) | `not_started` | `tcg-pipeline news backfill --source urbanize_la` using the D.2a sitemap/archive discovery path and `news_sources.config.backfill_window_days` (Urbanize LA defaults to 56 days). Run a dry-run first that reports deterministic URL count plus ranges for estimated relevant article count, projected LLM cost, cache-hit assumptions, and runtime; do not present a single optimistic point estimate for cost. Price against the 8-week URL slice (~150 URLs at current Urbanize cadence), not a market-filtered subset. The 8-week window is LA-specific. Backfill window length is configured per news source and may be longer for newer geographies that need more historical context to bootstrap pipeline awareness. Reprice the approval range before each apply run, using the active `extract_v2` no-glossary default with the AGENT.1-chosen model AND the AGENT.2 agent-trigger fire rates observed in production after step 9 / Case B; the older D.6 Opus/glossary estimate is no longer the approval basis. Respect robots.txt, crawl-delay, per-host rate limits, `Retry-After`, and source pause state. Observable via Coverage queue depth/minimal source health. Benchmark Pass 1 dictionary scans during backfill and decide whether to cache Aho-Corasick automatons per worker/batch. Measure Pass 3a false positives after D.6 smoke tightening and decide whether further fuzzy date comparison or tighter developer-window logic is worth the cost/complexity. Cross-source article dedup is deferred until D.late.E1 introduces LA YIMBY. Depends on D.6, D.M, D.2a-prep. **Sequenced as `AGENT.reset` step R.6.5** (immediately after R.6 re-enables news cron + agent path live), so the backfill becomes the inaugural production-quality news load on a clean post-reset DB. **During iterative `AGENT.reset` stabilization cycles, default to a 4-week window** (`backfill_window_days = 28`, ~75 URLs at current Urbanize cadence) for cost discipline — each reset cycle is expected to be wiped and re-run as UI/review/process improvements land. Pair each cycle's backfill with a small set of curated paste-a-link smokes that deliberately trigger rarer agent paths (`material_contradiction`, `override_contradiction`, structural conflicts) so the review queue exercises the full UI surface area regardless of what the 4-week organic window happens to contain. **The final-settle reset cycle uses a longer backfill window sized by explicit cost-vs-comprehensiveness analysis** before the cycle runs (see `AGENT.reset` row). 8 weeks is the initial planning baseline; the production-ready window may extend to 12, 26, or more weeks depending on cost telemetry from stabilization cycles, project-attribution coverage targets, glossary-gap rates per market, AGENT.7 cost optimizations shipped, and operator preference. The pre-final-settle dry-run report must include a cost-coverage curve at multiple candidate windows (8 / 12 / 26 weeks at minimum) so the operator picks an explicit point on the curve rather than a default — URL count, projected LLM cost (extraction + Pass 2c semantic + agent fires), prompt-cache assumptions, and runtime per candidate. |
| D.7 | (Renumbered) Paste-a-link covered by D.7a + D.7b above | `superseded` | The original D.7 paste-a-link is split across D.7a (worker-side vertical slice) and D.7b (Next.js UI). Marker preserved here so cross-references in the Decision Log resolve. |
| D.8 | (Superseded) Additional news sources | `superseded` | Replaced by explicit D.late.E1/D.late.E2/D.late.C source-expansion rows. Adding a publisher now requires a `news_sources` row/config, `LOGICAL_SOURCE_TYPE_BY_SOURCE_NAME` entry, source strategy doc, migration, and validation notes. |
| D.late.E1 | LA YIMBY expansion | `deferred` | First real second-source pressure test for the polite collector after Urbanize. Expected to be config-only if D.2a holds; reserve 0.5-1 day for abstraction adjustment. Add source row/config/doc/migration, host mapping, source-tier mapping, feed/archive validation, fixture tests, and cost/quality notes. |
| D.late.E2 | The Real Deal LA expansion | `deferred` | Likely first advanced-path candidate and section-filtering pressure test because of commercial leasing/noise. Validate robots/feed/Cloudflare behavior and complete a source strategy doc before enabling. Any advanced fetch use requires human approval and a compliance/TOS review. |
| D.late.C | Paid-source capability + BizJournals LA | `deferred` | Cookie/session auth for authorized paid-source access. Prefer Playwright/browser-session storage state for BizJournals; integrate with `service_credentials`, add manual relogin CLI, source-specific login/paywall markers, and paid-source runbook. First concrete consumer: `bizjournals_la`, currently mapped but inactive/unscheduled. |
| D.late.ADV | Advanced fetch implementation | `deferred` | Implement only when a real approved source needs it or polite access is disrupted and human review approves escalation. Candidate tools: Playwright/browser-session first for authorized paid-source access; FlareSolverr kept as a roadmap option but deprioritized and requires explicit approval. Residential proxy support is not a config flip and requires separate compliance/operational approval. |
| D.late.S | Self-healing source diagnostics | `deferred` | Narrow scope only: auto-diagnose alerts into the alert record, auto-pause unhealthy sources on block-like signals, and later auto-PR for narrow config fixes (RSS URL drift, tracking-param additions, paywall-marker updates). No auto-merge. No automatic polite-to-advanced escalation; the health UI may expose a simple human-approved switch/request action. |
| D.late.A | Auto-apply for high-confidence article matches | `deferred` | Per `news_research_design.md` §25.1. Activates after Phase D shows ≥80% reviewer acceptance over 3 months. `confirmed` matches with `candidate_confidence='high'` and clean Pass 1/2 agreement skip the Review Queue; daily auto-apply summary email to the researcher. |
| D.late.B | Eval set bootstrap from real reviewer decisions | `deferred` | Per `news_research_design.md` §21.2. **Now superseded in scope by the three-layer eval methodology in `agentic_escalation_design.md` §6** (continuous reviewer accept signal + quarterly Pipedream auto-comparison + targeted hand-grading on disagreement + spot-check sampler). The original bootstrap-from-50-reviewed-articles approach remains valid for an initial seed; the long-term eval set grows via the three-layer methodology rather than a curated golden set. Prompt-version-bump gating at `--eval-pass-rate >= 0.90` still applies, now scoped to both default-extraction prompts and per-source agent profile prompts. |

#### AGENT sprint — agentic escalation layer
> **Goal:** Replace Pass 3a/3b deterministic re-extraction with an Opus 4.7 agent loop with tool access. Source-agnostic architecture: news + permits ship in initial sprint; CoStar + Pipedream are deferred follow-ons.
> **Priority:** High — addresses dense-submarket duplicate creation and contradicting-state articles that the deterministic path cannot reason about, and removes the in-prompt glossary that blocks 25-market scaling.
> **Depends on:** D.6 stable production cron (provides agent-side production data baseline and the smoke article set for the model A/B harness).
> **Required reading:** `docs/specs/agentic_escalation_design.md` is the full implementation contract. Pre-build deliverable per §5.5.0: `docs/specs/ci_contradiction_impact_assessment.md` (Claude Code-authored, researcher-reviewed before any C.i code is touched).
> **Sprint structure:** All three items below build as one continuous workstream and ship as one cutover event. Permits live from day one. Cost guardrails (scoped daily caps + runtime kill switches per design doc §5.8) replace staged-rollout production observation as the safety mechanism.

| Step | Task | Status | Notes |
|------|------|--------|-------|
| AGENT.1 | Default-extraction infrastructure + retrieval prerequisites | `done` | 2026-05-05: Foundation schema/cost slice committed (`202605040028` migration): PostGIS GIST index on `Project.location`, `pgvector` article chunks, per-reference indexing gates, generalized `cost_caps` / `cost_cap_overrides` / `llm_cost_usage`, and runtime kill switches. 2026-05-05: Multi-provider news LLM layer committed in `news/llm.py` and wired through triage/extraction with Anthropic defaults preserved; OpenAI/Vercel-compatible Responses JSON client, provider-aware cost attribution, strict `AI_GATEWAY_API_KEY` requirement for Vercel AI Gateway, Sonnet 4.6 / Opus 4.7 / Opus 4.6 / GPT-5.5 / GPT-5.4 pricing aliases, pricing assumptions, stale missing-key alert cleanup, and focused tests. Current routing policy is direct provider APIs for built/current AGENT work: native Anthropic for Claude candidates and native OpenAI for GPT candidates; Gateway remains supported code but deferred until an explicit sweep/activation decision. 2026-05-05: Active default `extract_v2` prompt now omits the developer/project glossary, asks for raw candidate names/developers, no longer requires registry IDs in its schema, explicitly bans outside knowledge/guessing, and includes a TCG status rubric for `candidate_status_signal` before A/B testing; `extract_v1` is retained as the legacy glossary prompt for pre-cutover audit clarity, and `reextract_v1` keeps the glossary until AGENT.2 moves Pass 3a/3b to `news/extraction_legacy.py`. 2026-05-05: A/B harness scaffold added as `tcg-pipeline news ab-extract`; it runs configured provider/model candidates over the D.6 fixture with active `extract_v2`, records parse/reference/matcher/agent-trigger/cost metrics, projects review-item counts inside a rollback-only transaction, and writes a JSON report for researcher spot-grading. 2026-05-05 follow-up guardrails: rollback non-mutation is now covered by a DB-backed test, CLI prints redacted DB target + planned LLM call count before live calls, candidate preflight pings fail fast on unavailable models/providers, and report metadata states harness LLM spend bypasses `llm_cost_usage` cost-cap accounting. 2026-05-05 live A/B: primary smoke-set report `ab_extract_20260505_174623.json` measured Opus 4.7 `$0.202427`, Sonnet 4.6 `$0.107957`, and GPT-5.4 `$0.054534` adjusted to current OpenAI cached-input pricing; supplemental report `ab_extract_20260505_180014.json` measured Opus 4.7 `$0.201802`, Opus 4.6 `$0.233970`, and GPT-5.5 `$0.292948`. Researcher decision: keep Opus 4.7 as the default extraction model. 2026-05-05: Article embedding/indexing pipeline implemented using direct OpenAI `text-embedding-3-small`, `pgvector.sqlalchemy.Vector`, gated committed-accept / auto-applied references, per-reference chunks plus one whole-article chunk per accepted article, cost-cap reservation + `llm_cost_usage` true-up, `tcg-pipeline news index-articles` dry-run/apply CLI, and `news_backfill_chunk` worker task. 2026-05-05 production application: the single Supabase database was logically backed up (`pg_dump` custom format, SHA256 recorded in chat), upgraded to `202605040028`, verified for new/legacy tables and indexes, and Render API/worker were deployed at commit `c7946b9`. Final smoke: one committed-accepted Urbanize news reference planned 2 chunks, `--apply` indexed 2 chunks via one OpenAI embedding call (`883` input tokens, `$0.000018`), wrote `llm_cost_usage` for `news/article_embedding`, and an immediate rerun skipped both unchanged chunks with zero API calls. |
| AGENT.2 | Agent on news | `in_progress` | **Pre-build deliverable complete:** `docs/specs/ci_contradiction_impact_assessment.md` written and reviewed; researcher decisions recorded in §I of that doc. 2026-05-05: first foundation slice added `202605050029` with source-agnostic `agent_runs` full-observability schema, `agent_run_review_items` authoritative join table, default-deny RLS posture, ORM metadata, and `NewsExtractionPass.EXTRACT_RETRY`; follow-up `202605050030` tightens `evidence_consulted` / `tool_calls_summary` to required JSON arrays, requires `completed_at`, documents the news `intake_record_id` convention, and adds DB-backed schema contract tests. 2026-05-06 production application: after fresh logical backup `supabase_pre_agent2_20260506_033558.dump` (SHA256 `97f8674a9a8474a947865431def097247eacfa98020fbedac3586dbc42821519`), AGENT.2 migrations `202605050029` and `202605050030` were applied to the single Supabase database and DB-backed agent routing tests were rerun unskipped. 2026-05-05: added `tcg_pipeline.agents` with `news_v1` source profile, source-agnostic `IntakeRecord`, trigger validation, `AgentClient` injection seam, per-run cost reservation/true-up under `agent.news_v1`, kill-switch/budget/failure/success terminal audit-row persistence, and profile/runner tests. 2026-05-05 guardrail follow-up: the runner now owns wallclock timeout enforcement (`failed_timeout`), post-hoc tool-call-count and per-run-cost checks, cost-overshoot `SystemAlert`s, reservation release on timeout/failure, and required news `extraction_id` validation. 2026-05-05: Anthropic tool-loop client shell and bounded `AgentToolRegistry` landed: prompt loading, Messages API tool specs, tool-result loop, usage aggregation, final structured JSON parsing, and hard tool-output truncation are tested with fake Anthropic responses/tools. 2026-05-05 follow-up before DB-backed tools: `AgentRunRequest` now carries runner-resolved `session_factory` / `settings`, client final outcomes are validated before persistence, and news intake payloads are documented as lean context only (body/retrieval via tools). 2026-05-05: DB-backed retrieval tools landed: `get_project_state` reads project snapshot, `project_field_resolution`, `project_latest_evidence`, and referenced evidence metadata; `search_articles_similar` searches accepted active article chunks with optional `published_after` freshness filtering and returns matched project/evidence IDs when available; `get_article_body` fetches stored article text after chunk search narrows candidates. Default Anthropic client builder registers these tools; prompt now teaches search chunks → fetch body. Query-embedding cost for `search_articles_similar` is intentionally ignored as negligible. 2026-05-06: first narrow news integration route is wired for `new_candidate` matcher outcomes; Render safe defaults keep the path disabled until controlled smoke intentionally enables `AGENT_ENABLED_FOR_NEWS=true`, `AGENT_ALLOW_LIVE_LLM=true`, and `NEWS_USE_LEGACY_PASS3=false` on both API and worker. Core tools + news-specific tools must enforce **§5.4.1 hard output budgets** so per-run cost cap can't be consumed by tool returns. News source profile per design §5.9. **Shared semantic field interpretation layer** per design §5.1.1: news references pass through source-profile-owned mapping before writing canonical evidence for status, product type, age restriction, delivery date projection, and unit buckets. Single build (no slice A/B split) — `workforce_units` integration (step 6 of the AGENT.2 sub-sequence below) precedes the semantic layer build (step 7) so all five field domains land in one coherent refactor. For news, Pass 2c is the primary prose interpreter; structured sources remain deterministic through their own source profiles. 2026-05-06: output-quality retry path for parse/refused/truncated is implemented as `extract_retry_v1` with at most two non-agent retry attempts. Replace Pass 3a (a)+(b) and Pass 3b with agent loop. **Pass 3a/3b code preserved as `news/extraction_legacy.py`** for one release; emergency settings flag `news_use_legacy_pass3` re-routes if AGENT.2 has a regression. Move contradiction detection earlier (per Q5 in design doc). Cutover migration backfills synthetic `reasoning_trace` for legacy `pass='reextraction'` rows. Worker model: one job per article needing agent escalation. Agent-produced contradictions use `payload.proposed_alternatives: list[...]` (multi-alt schema) and log under distinct `"Agent contradiction detection"` actor. Per-reference review queue + retrieval index gating. Activity / Audit Log view (subsumes spot-check sampler dashboard). Pipedream coverage compare job (LA window first). Runtime kill switches: app setting `agent_enabled_for_news` defaults true, but Render blueprint defaults false until controlled smoke; `news_use_legacy_pass3` defaults false; `news_use_legacy_semantic` defaults false in code while Render keeps it true until controlled semantic smoke. Depends on AGENT.1. **Build sequence: see "AGENT.2 sub-sequence, 2026-05-06" note below the AGENT table for the authoritative 15-step ordering. Steps 1-10 are done; step 11 (Activity / Audit Log view) is next.** |
| AGENT.3 | Agent on permits | `not_started` | Add permit source profile (per design §5.9) to the source-agnostic agent runner: permit-specific tools (`get_permits_for_parcel`, `get_permits_for_project`), cross-stream tools (`get_articles_about_parcel_or_address`, `get_permits_for_parcel_or_address`), permit-specific failure-mode prompts (with §5.4.1 output budgets). Wire permits into the shared semantic interpretation layer using deterministic LADBS/source-profile rules first; reserve LLM/agent interpretation for ambiguous permit descriptions, conflicting signals, or cross-stream exceptions. Wire LADBS adapter integration through the agent on the **uniform 10% unit-delta trigger** (Q9 narrow set: `new_candidate`, >10% unit change, product-type change — revised from earlier >50%). Scoped `cost_caps` row for `bucket='permits'` (`daily_warn_usd: $50`, `daily_hard_usd: $75`). Runtime kill switch `agent_enabled_for_permits` (default `true` at launch — researcher direction is "build with it on, monitor closely, dial back if needed"). Depends on AGENT.2. |
| AGENT.6 | Agent on leasing sites — Pre-Leasing detection | `not_started` | New source profile (`leasing_v1`) on the source-agnostic agent runner. Per-project leasing-site discovery and rent-listing classification, with a scheduled bi-weekly sweep over `Under Construction` projects plus trigger-driven immediate checks when the news interpreter emits `topped_out` / `framing_complete` / `concrete_pour` / `first_move_ins` / `ribbon_cutting` / `now_open` signal flags (per `docs/specs/semantic_interpretation_layer_design.md` §4.2 and §9). New logical source type `leasing_site` declared in `config/source_tiers.yaml` with field-specific authority for `pipeline_status` only (Pre-Leasing detection). Detection: search Apartments.com / StreetEasy / Trulia / community microsite / developer portfolio for the project; classify via Haiku-cheap LLM whether rent prices for this specific community are publicly listed. Strong detection (specific units + specific monthly prices) → HIGH confidence Pre-Leasing evidence. Marketing-floor only ("starting at $X") → MEDIUM confidence with explicit note. "Sign up to be notified" / "coming soon" → diagnostic only, no status change. Sweep stops once project hits Complete (first move-ins or CofO). New `cost_caps` bucket `leasing` (estimate ≈ $0.20-0.50/day at LA scale; ≈ $20/day at 25-market scale). Runtime kill switch `agent_enabled_for_leasing`. Tools: existing core tools + `search_web` / `fetch_page` (cross-stream). Depends on AGENT.3 stable in production and AGENT.2 step 7 (semantic layer must emit the trigger signal flags). |
| AGENT.4 | Agent on CoStar uploads | `deferred` | Add CoStar source profile (per design §5.9). Trigger profile: contradiction-only (>10% unit delta per uniform threshold, status regression, developer mismatch) — no `new_candidate` triggers because CoStar's stable `costar_property_id` short-circuits identifier matching for the common case. Allowed tools: core + cross-stream `get_articles_about_parcel_or_address`. New `cost_caps` bucket row sized for bulk-upload spike (CoStar uploads can contain hundreds of rows). Runtime kill switch `agent_enabled_for_costar`. Wire CoStar upload path (currently `db/seed.py` and `costar_uploads`) through the agent runner for in-scope rows. Depends on AGENT.3 stable in production. |
| AGENT.5 | Agent on Pipedream uploads | `deferred` | Add Pipedream source profile (per design §5.9). Conservative trigger profile: contradiction-only (>10% unit delta) AND contradiction must be against Tier 1 evidence (newer government source). Pipedream is human-curated Tier 1; agent escalates rather than overrides. Type 2 (downgrade matcher's confirmed) NOT in scope for Pipedream profile. Allowed tools: core only. New `cost_caps` bucket row. Runtime kill switch `agent_enabled_for_pipedream`. **Includes refactor of Pipedream upload path** — currently flows through `db/seed.py`, not the scheduled-collector or news-integrator paths. Refactor is real work owned by AGENT.5. Depends on AGENT.3 stable in production. |
| AGENT.7 | Pass 2c semantic cost review and optimization | `not_started` | Optimize per-call cost of the Pass 2c news semantic interpreter after AGENT.2 step 7 cuts over and production telemetry is large enough to evaluate. **Baseline measurement (2026-05-07 staging smoke, n=1 production-shaped article):** `$0.198341` per Pass 2c call for an article with one reference matched to an LA project plus three candidate projects, emitting `news_status_uncorroborated_high_quality_permit_jurisdiction`. Roughly `2x` the original design-doc estimate of `$0.05-0.10`; the dominant cost is uncached per-article input (article body + project context + recent evidence + Pass 2b output), not output and not the cached system prompt. At LA scale (~150 Urbanize articles/day) ≈ `$30/day`; at projected 25-market scale (~1,000 articles/day) ≈ `$200/day`. The broader controlled smoke produced a smaller synthetic-article sample (2026-05-07, 9 live calls, `$0.402036` total, mean ≈ `$0.045`/call), so keep `$0.20/article` as the scheduled-article planning baseline until production telemetry is larger. Cost is bounded and per the 2026-04-28 decision quality remains the primary constraint, but optimization headroom is real. **Pre-work — capture the production cost distribution.** Do not optimize on smoke-only data. Instrument prompt-cache effectiveness by tracking `input_tokens_cache_creation` vs `input_tokens_cached` distribution from `news_semantic_interpretations` rows once a few hundred production calls are observed; cache hit rate depends on call cadence and queueing/batching mitigations may reduce cost without architectural change. **Tier 1 — easy wins, no architectural change.** Ship first; expected to drop per-call cost from `~$0.198` to `~$0.08-0.10`. (a) **Pre-filter at integration level** — skip Pass 2c entirely when Pass 2b extracted no load-bearing fields (`candidate_status_signal IS NULL AND all unit candidates IS NULL AND candidate_developer IS NULL AND candidate_address IS NULL`); informational/tangential coverage probably cuts call volume `10-30%`. (b) **Compress `recent_evidence` representation** from full `extracted_fields` JSON to a single line per row (e.g. `"YYYY-MM-DD <source_type>: <field>=<value>; ..."`); cuts per-row tokens ~5x. (c) **Compress `project_state`** to fields relevant to what Pass 2b actually extracted (status-related fields when Pass 2b extracted status, unit-related fields when units, etc.) instead of the full 14-field payload per project; cuts per-project tokens ~3-5x. (d) **Trim article body cap from `30,000` to `15,000` chars** after verifying production length distribution; >95% of news articles fit comfortably. (e) **Slice Pass 2b output passed in the prompt** to just the candidate references rather than the full extraction `output_json`. **Tier 2 — model swap evaluation.** Evaluate other LLMs (Sonnet 4.6, GPT-5.4, future Anthropic/OpenAI/Google releases as they become A/B-eligible) against Opus 4.7 on the existing A/B harness pattern (`tcg-pipeline news ab-extract`, adapted as `interpret_v1` A/B) using the smoke fixture set + accumulating production articles. The semantic-interpretation task operates on Pass 2b's already-extracted candidates rather than raw prose, which makes lighter-weight models a real possibility in principle. **Researcher hypothesis is that Opus will likely remain the best fit for ambiguous-prose interpretation, especially on glossary-gap edge cases**; the A/B is to test that hypothesis with measured data, not displace Opus speculatively. Quality bar must include parse_status distribution, reason-code distribution match against Opus baseline, glossary-gap rate per field, multi-tenure detection accuracy, and reviewer-acceptance rate when historical data is available. The AGENT.1 multi-provider abstraction makes any swap a config change (`NEWS_SEMANTIC_PROVIDER` / `NEWS_SEMANTIC_MODEL`), so evaluation cost is harness LLM spend, not engineering work. **Tier 3 — tools-on-demand (architectural change, last resort).** Convert pre-loaded `project_context`, `recent_evidence`, and `jurisdiction_policy` into LLM-callable tools (e.g. `get_project_state`, `get_recent_evidence_for_field`, `get_jurisdiction_policy`) so the interpreter only loads context relevant to its decision. Real cost saving on cases that don't need full context (strong physical signals where the article alone supports the verdict, forward-looking signals, no-match articles) but adds round-trips, non-determinism, harder testability, and crosses the `docs/specs/semantic_interpretation_layer_design.md` §2 principle 11 boundary that Pass 2c is a single-shot classifier. Reserve for "Tiers 1 + 2 were not enough." Includes hard tool-output budgets per `agentic_escalation_design.md` §5.4.1. **Acceptance:** ship Tier 1 first after production telemetry supports it; quantify reduction; run Tier 2 A/B; re-evaluate need for Tier 3 against measured remaining cost. Update `docs/specs/semantic_interpretation_layer_design.md` revision history with the measured baseline, optimizations shipped, and post-optimization cost distribution. Depends on at least 30 days of production cost data across multiple article types. |
| AGENT.batch | Batch API dispatch for scheduled extractions | `deferred` | Implement Anthropic Batch API path in `ExtractionDispatcher` for cron-driven extractions only. Paste-a-link stays synchronous; agent runs stay synchronous. Cost-savings target: ~50% off scheduled-scrape extraction line item. Explicitly NOT in initial sprint per design §0.1; architecture must support clean swap-in later. |
| AGENT.reset | Controlled production database rebuild / reset | `not_started` | **Required before any post-AGENT phase work (E, F, G, H, J) or new-market onboarding.** Production has been the tuning environment since Phase A; nothing in the database has been authored under final-system conditions, so the reset is truncate-and-reseed rather than snapshot-and-replay. Drop everything in the data tables: projects, evidence, source_runs, news_articles, news_extractions, news_project_references, news_article_chunks, news_reference_auto_applied, agent_runs, agent_run_review_items, llm_cost_usage, review_items, review_decisions, change_log, status_history, project_notes, project_relationships, project_identifiers, researcher_overrides, resolution_log, scrape_jobs, costar_uploads, system_alerts. Preserve config tables only: markets, jurisdictions, source_registrations, news_sources, news_signal_flag_registry, cost_caps, cost_cap_overrides, alembic_version, developer canonical registry seed. **Sequence:** (R.1) fresh `pg_dump` logical backup as recovery path; (R.2) truncate the data tables listed above; (R.3) re-seed from a fresh CoStar export pulled at reset time + the most recent available Pipedream workbook (Pipedream typically refreshes June, so ahead of June use whichever is latest); (R.4) re-run scheduled collectors (LADBS / Socrata / etc. — deterministic from public sources); (R.5) re-run `resolve-all` + `canonicalize-developers` under the final semantic layer + workforce_units; (R.6) re-enable scheduled news cron + agent path with kill switches set to live; **(R.6.5) run D.B Urbanize backfill at the configured window — 4 weeks for stabilization cycles; the final-settle cycle uses a longer window sized by explicit cost-vs-comprehensiveness analysis (8 weeks as initial planning baseline; production-ready may extend to 12, 26, or more weeks; see D.B row) — supplemented with curated paste-a-link smokes that deliberately trigger rarer agent paths so the review queue exercises the full UI surface area**; (R.7) once the June 2026 Pipedream refresh is available, run the AGENT.2 Pipedream coverage compare job as the inaugural eval baseline of the post-reset system vs. seasoned researcher work. **Posture during AGENT development:** because everything pre-reset is dummy data, the developer can smoke aggressively and write evidence/review/agent rows during AGENT.2/3 without worrying about cleanup. The only constraint is **do not break config tables** — schema migrations to `markets`, `jurisdictions`, `source_registrations`, `news_sources`, `news_signal_flag_registry`, `cost_caps` need to be conservative and non-breaking because those rows survive the reset. Everything else is fair game to mutate freely until R fires. **Treat as iterative stabilization cycles, not a single-day cutover.** Each cycle (R.1 backup → R.2 truncate → R.3 reseed → R.4 collectors → R.5 resolve → R.6 news cron live → R.6.5 backfill → R.7 eval) is sized at one to two days of focused work. **Expect to run multiple cycles during stabilization** as UI improvements, review-process changes, and pipeline tuning land — every cycle wipes the data tables and re-runs the bootstrap so each test starts from clean state. Backfill cost stays bounded by the 4-week-per-cycle default. **Final-settle criteria** before the last cycle: `UI.QA` complete; review queue UX validated end-to-end on real data; trigger fire rates and review-acceptance rates measured against the AGENT.2 step 11 Activity / Audit Log view; cost telemetry within expected range per AGENT.7; all known bugs/improvements from prior cycles addressed. After the final-settle cycle (backfill window sized by explicit cost-vs-comprehensiveness analysis, see D.B row), the post-reset state becomes the new production baseline and Phase E/F/G/H/J unblock. **`UI.QA` slots into every stabilization cycle** — each cycle's clean post-reset state is a fresh test surface; mark `UI.QA` `done` only after the final-settle cycle confirms no unresolved layout/flow/design issues. |

AGENT.2 routing note, 2026-05-05: the first news integration slice is intentionally narrow. `new_candidate` matcher outcomes run through the news agent before final evidence/review writes unless `news_use_legacy_pass3=true`; only `promote_existing_project` can revise deterministic output in this slice. No-change, escalated, failed, budget-rejected, timeout, and killed-by-switch outcomes fall back to the deterministic new-candidate review item and link it through `agent_run_review_items`. Real agent-client construction is blocked unless `agent_allow_live_llm=true`; injected fake/test clients are still allowed. Malformed promotion confidence falls back to deterministic review instead of being silently replaced.

AGENT.2 Render deployment reminder: before deploying API/worker on AGENT.2 agent-routing code, choose and set one explicit env mode. For a live controlled smoke, set `AGENT_ENABLED_FOR_NEWS=true`, `AGENT_ALLOW_LIVE_LLM=true`, and `NEWS_USE_LEGACY_PASS3=false`. To hold agent routing disabled, set `AGENT_ENABLED_FOR_NEWS=false` and leave `AGENT_ALLOW_LIVE_LLM=false`. Do not deploy with `AGENT_ENABLED_FOR_NEWS=true` and `AGENT_ALLOW_LIVE_LLM=false` unless the desired behavior is fail-fast on any `new_candidate` path.

AGENT.2 status correction, 2026-05-06: the AGENT.2 audit migrations are applied and the first narrow news `new_candidate` agent route is wired in code. The remaining operational checkpoint is a controlled live smoke, not implementation of the route itself. Render safe defaults keep the path disabled until both API and worker are intentionally switched to `AGENT_ENABLED_FOR_NEWS=true`, `AGENT_ALLOW_LIVE_LLM=true`, and `NEWS_USE_LEGACY_PASS3=false`.

AGENT.2 smoke-monitoring note, 2026-05-06: do not block the sprint for a 24-hour observation window after the first controlled live agent run. Run one narrow `new_candidate` smoke, immediately inspect `agent_runs`, `agent_run_review_items`, produced review/evidence behavior, and `llm_cost_usage`, then continue AGENT.2/3 implementation while scheduling a same-day follow-up check on the early agent rows. If the smoke shows malformed decisions, unexpected cost, missing linkage, or unsafe mutation behavior, flip `AGENT_ENABLED_FOR_NEWS=false` / `AGENT_ALLOW_LIVE_LLM=false` on both API and worker and continue development against injected/fake clients until fixed.

AGENT.2 live-smoke finding, 2026-05-06: the first true live-agent smoke reached Anthropic, executed two `search_articles_similar` tool calls, wrote `agent.news_v1` cost, and failed safely to deterministic review because the final model response was not bare JSON. The immediate hardening is to keep the prompt explicit about returning a single JSON object and make client parsing accept fenced JSON or JSON with minimal surrounding text while still rejecting responses with no JSON object.

AGENT.2 worker smoke finding, 2026-05-06: a follow-up paste-a-link smoke exposed a transient PostgreSQL SSL failure after fetch/triage/extraction had already persisted. The worker correctly failed the job, but late generic job-failure handling also marked the article `fetch_failed`, which obscured the successful fetch state. The worker should only set `news_articles.fetch_status='fetch_failed'` for failures before Pass 0 fetch is durably recorded; post-fetch/integration failures leave article fetch state intact and fail the scrape job for retry/ops review.

AGENT.2 live-smoke success, 2026-05-06: a controlled Urbanize paste-a-link smoke for `380 S. Rosemead Boulevard` reached the live `news_v1` agent after the JSON parser and worker-state hardening deploys. The agent completed with `decision=no_change`, executed two `search_articles_similar` calls, linked the agent run to the deterministic `new_candidate` review item, recorded about `$0.072` under `agent.news_v1`, and left deterministic review behavior intact. This validates the first narrow `new_candidate` live route while broader rollout remains gated by same-day monitoring and Render kill switches.

AGENT.2 smoke tooling note, 2026-05-06: use `tcg-pipeline news paste-link-smoke <url>` for frontendless paste-a-link smoke tests when running from a trusted environment with the intended database and LLM credentials. The command uses the same article/job creation path as the FastAPI endpoint and runs the paste-a-link worker inline, so it is useful for local or Render-shell validation without requiring a browser. It does not replace a deployed RQ-worker smoke when queue dispatch itself is under test.

AGENT.2 Render deploy ops note, 2026-05-06: after a push to `main`, Render autodeploy can already be building the new commit. Before manually creating a deploy through the Render API, list recent deploys for both API and worker; if an autodeploy for the same commit is `queued`, `build_in_progress`, or `update_in_progress`, wait for that deploy instead of creating a duplicate. Duplicate deploys are cancellable, but the dashboard can make the canceled duplicate look like the current deploy if the operator is viewing the wrong deploy ID.

AGENT.2 promotion-smoke prerequisite, 2026-05-06: before spending a live promotion smoke, add the missing `search_projects` core tool already declared in the source profile. `get_project_state` can inspect a known UUID, but a deterministic `new_candidate` with no matcher candidates needs a bounded registry lookup by article-observed address/name/developer before the agent can produce an auditable `promote_existing_project` verdict. `search_articles_similar` should also surface the attributed project/evidence through accepted evidence rows when `news_project_references.matched_project_id` is empty, so accepted-new chunks can lead the agent to real project IDs.

AGENT.2 RQ-worker smoke success, 2026-05-06: deployed queue dispatch has now been validated for the narrow news `new_candidate` agent path. Because Render Redis uses a private hostname that local Windows cannot resolve, use the Render API one-off job pattern to enqueue existing DB scrape jobs from inside the worker service network when browser/API auth is not the boundary under test. The RQ smoke job `55259d67-e421-4ee6-bd25-eefeb67b716a` was enqueued through Render, picked up by the deployed worker, completed article `30c88b7b-1dd0-45b1-8468-ac1b182cd94e`, and promoted Rosa's Place to project `7c19d98a-b735-4815-9ddc-6e5a5d0de640` with evidence `f892b0ab-6788-4693-8a5d-1fad21659d03` and no fallback review item.

AGENT.2 possible-match routing note, 2026-05-06: broadened news agent routing from deterministic `new_candidate` only to `possible` / ambiguous existing-project matcher outcomes. The `possible_multi_candidate` trigger lets the agent inspect candidate project IDs and retrieval context, then either confirm one existing project, leave the deterministic possible-match review in place, or escalate without mutation. Mutation is deliberately narrower than `new_candidate`: `confirm_existing_project` is accepted only when the selected `project_id` is one of the deterministic matcher `candidate_project_ids`; off-list IDs, malformed confidence, no-change, escalation, and failures fall back to the deterministic possible-match review item and link it through `agent_run_review_items`. Code/tests and controlled live smoke are complete.

AGENT.2 possible-match live-smoke success, 2026-05-06: controlled paste-link smoke for the Downtown Women's Center / Rosa's Place Urbanize article (`?tcg_possible_smoke=20260506a`) triggered `possible_multi_candidate`. The deterministic matcher produced `possible` via `address_composite` with candidate project `7c19d98a-b735-4815-9ddc-6e5a5d0de640`; the agent called `get_project_state`, returned `confirm_existing_project` with confidence `0.95`, and integration wrote `match_type='agent_confirmed_possible_match'`, evidence `152a0b3e-eabd-4698-8121-4a9da2822272`, no fallback review item, and cost `$0.079148` under `agent.news_v1`. This validates Type 3 mutation on live LLM/tooling; step 4 (`low_confidence`) is next.

AGENT.2 smoke convention, 2026-05-06: use a fresh no-op query parameter to bypass URL dedupe when reusing a known article for deterministic trigger smokes. Pattern examples: `new_candidate` smoke = paste-link an article about a project not yet in the database; possible-match smoke = re-paste the same article with cache-bust after the project exists so the matcher now sees candidate IDs; `low_confidence` smoke needs a known article or fixture where Pass 2 emits `candidate_confidence='low'` on populated fields.

AGENT.2 Anthropic 400 note, 2026-05-06: the 400 visible in the local possible-match smoke stdout was from the news extraction call's known temperature-deprecation retry path (`create_anthropic_message` retries without temperature), not from the agent tool loop. The agent client already omits temperature and the two agent Messages turns completed with 200 responses. If a future smoke shows a 400 during an agent-only turn, capture the provider response body before treating it as the same class of issue.

AGENT.2 low-confidence routing note, 2026-05-06: low-confidence news references now route through the agent when `candidate_confidence='low'` and at least one load-bearing field is populated. This replaces the old Pass 3a low-confidence re-extraction posture with deterministic fallback plus agent audit. If low-confidence appears with `new_candidate` or `possible_multi_candidate`, the existing trigger's verdict contract governs and low-confidence fields are extra reasoning context. If low-confidence is the only trigger, the agent can `no_change`, `escalated`, or `promote_existing_project` when a deterministic low-confidence discard should actually match an existing project. Code/tests and controlled live smoke are complete.

AGENT.2 low-confidence live-smoke success, 2026-05-06: controlled synthetic fixture `d14149b95c` created project `8e60e0e2-712b-4536-908e-56b78f0c523e`, article `870182ba-bc14-440a-b756-e48584ec99f0`, extraction `e3d680ad-0040-4859-8897-eb955be4e784`, and low-confidence reference `509a6e02-3651-4866-884d-fc9aded94286`. The deterministic matcher confirmed the project via `address_composite`; the live agent ran with `triggered_by=['low_confidence']`, called `get_project_state`, returned `decision=no_change`, recorded cost `$0.071788`, wrote evidence `5ba99151-8209-415e-8b51-ec9130cd90ad`, and linked agent run `3ea8ba88-47aa-4614-8871-c86366098b73` to two low-priority status-change review items (`total_units` and `developer`). This validates the low-confidence confirmed-reference path and agent-to-review linkage while keeping deterministic fallback semantics.

AGENT.2 low-confidence reproducibility note, 2026-05-06: organic `candidate_confidence='low'` output is model-dependent, so repeatable low-confidence smokes should use fixture-based rows when needed: create or reuse a news extraction/reference with populated load-bearing fields and `candidate_confidence='low'`, then run integration/agent routing against that reference. Prompt-tuned articles are useful for live realism, but fixture rows are the stable regression recipe.

AGENT.2 output-quality retry note, 2026-05-06: `extract_retry_v1` is the non-agent replacement for the old output-quality Pass 3a branch. Initial `parse_error`, `schema_invalid`, `refused`, and `truncated` extraction results get up to two retry attempts with stronger formatting guidance, persisted as `pass='extract_retry'` rows and costed under the distinct `extract_retry` capability. Successful retries become the article's current extraction; exhausted retries preserve the failure path for review/escalation instead of entering the agent loop. Live smoke pending: the retry prompt has not yet been observed under a real LLM response, so its behavior under live response shaping is unknown. A synthetic-failure smoke (e.g., temporarily lower `EXTRACTION_MAX_TOKENS` to force a `truncated` initial extraction, then observe retry recovery) should validate the prompt before the AGENT cutover.

AGENT.2 interim Pass 3a low-confidence gate, 2026-05-06: default extraction no longer runs legacy Pass 3a re-extraction for `pass2_low_confidence` when `news_use_legacy_pass3=false`; integration handles low-confidence references through the audited `news_v1` agent path with deterministic fallback. Pass 3a structural-conflict re-extraction remains active until the harder trigger / contradiction rewrite step, and `news_use_legacy_pass3=true` still restores legacy low-confidence re-extraction for emergency fallback.

AGENT.2 sub-sequence, 2026-05-06: the AGENT.2 row above describes total scope as a paragraph; the build sequence below is the authoritative ordering. Steps already done are marked. Steps not yet done preserve deterministic fallback while behind the existing kill switches.

1. `promote_existing_project` paste-a-link smoke. **Done** (Rosa's Place, 2026-05-06).
2. First scheduled cron run unattended on the agent path + deployed RQ-worker smoke. **Done** (RQ smoke validated 2026-05-06; ongoing scheduled-cron observation continues in same-day monitoring per the smoke-monitoring note).
3. **`possible_multi_candidate` trigger.** **Done** (code/tests + live smoke, Rosa's Place, 2026-05-06). Same Type 3 mutation pattern as `new_candidate`, but constrained to the matcher's candidate list: agent picks one listed project, leaves deterministic possible-match review in place, or escalates. Historical trigger name is kept for audit continuity even though the gate covers single-candidate possible matches too. Per the AGENT.2 possible-match routing note above.
4. `low_confidence` trigger. **Done** (code/tests + live synthetic smoke, 2026-05-06). Same Type 1 shape as `new_candidate` but fires when Pass 2 candidate_confidence is low on populated fields.
5. Output-quality retry path (`extract_retry_v1`). **Done** (code/tests, 2026-05-06). Routes parse_error / schema_invalid / refused / truncated extractions to a cheaper retry-with-stronger-guidance prompt; not the agent. Records as `pass='extract_retry'` in `news_extractions`, with at most two attempts before preserving the deterministic failure/review path.
6. **`workforce_units` integration end-to-end.** **Done** (code/tests/docs, 2026-05-06). Added nullable schema, Pipedream/backfill mappings, resolver, `extract_v2` schema/prompt support, news integrator changes, UI surfaces, and focused tests. See E.6 for canonical scope. Phase R re-seeds projects from fresh CoStar (which does not track workforce) and the most recent Pipedream export (which also does not), so post-reset workforce_units stays NULL across the board until news / permits / researcher overrides populate it forward.
7. **Semantic interpretation layer (single build, all five field domains)** - **Done** (code/tests/docs + live smoke/cutover, 2026-05-07). Status, product_type, age_restriction, delivery_date, and unit_buckets now flow through the `interpret_v1` Pass 2c LLM-backed semantic interpreter for news. Pass 2c is a single-shot Opus 4.7 structured-output call downstream of Pass 2b extraction, with no tools and rich `InterpreterContext` input. Per-source strategy: news -> LLM (Pass 2c); LADBS -> deterministic port (source already structured); CoStar / Pipedream / future structured permit feeds -> deterministic when those source profiles ship. Multi-market glossary architecture and glossary-gap monitoring remain owned by `docs/specs/semantic_interpretation_layer_design.md`.
8. Move Pass 3a/3b code to `news/extraction_legacy.py` behind `news_use_legacy_pass3`. **Done** (code/tests/docs, 2026-05-07). Code-only move; the synthetic-`reasoning_trace` data backfill from the original cutover migration drops out because Phase R wipes legacy reextraction rows.
9. Harder triggers (`pass1_pass2_conflict`, `material_contradiction`, `override_contradiction`) + contradiction-detection rewrite (Q5 - agent owns contradiction reasoning for both pre-resolve "article-vs-state" and post-resolve "newer-evidence-vs-override" cases). **Done** (code/tests/live smokes through Case B, 2026-05-08). Pass 1 vs Pass 2 structural conflicts route into the news agent when the live agent path is intentionally enabled; otherwise the legacy structural re-extraction fallback remains active. `material_contradiction` can downgrade a confirmed match to possible-match review when article-vs-state attribution is suspect. `override_contradiction` lets the agent create C.i-compatible proposed-alternative review items for newer evidence vs active researcher overrides, linked through `agent_run_review_items`; deterministic C.i skips those agent-created item IDs on the same resolve pass. 2026-05-08 cleanup: raw `pipeline_status` news signals are excluded from pre-resolve override-trigger detection because Pass 2c and post-resolve C.i own canonical status comparison; the override prompt now exposes one recommendation for the full reference payload rather than a per-field verdict that integration does not consume.
10. Worker model split: existing `news_scrape` for fetch+Pass1+triage+default-extraction, `news_agent_integrate` as one job per article needing agent escalation with 300s wallclock cap. **Done** (code/tests/docs, 2026-05-08). The existing scheduled `news_scrape` job remains the compatibility discovery/fetch/triage/extraction job. After Pass 2b, it preflights agent triggers without running the agent; articles with triggers enqueue child `news_agent_integrate` jobs, while no-trigger articles keep inline integration. The child job reloads article/extraction state, runs integration + agent loop under the `news_v1` 300s wallclock cap, writes evidence/review items, and increments the parent `source_runs.new_matches` count with an atomic SQL update. One queued/running `news_agent_integrate` child is active per article; parent retries reuse the child row, Redis-unavailable rows can be enqueued later, and worker-level failures before integration starts remain ops-visible retryable scrape-job failures rather than writing fallback review items outside the integration path.
11. **Activity / Audit Log view (subsumes spot-check sampler dashboard, Q12).** One cross-project chronological feed of resolver-applied changes, sourced from `change_log` joined with `resolution_log` (rule + confidence + evidence IDs), `agent_runs` / `agent_run_review_items` (agent linkage + reasoning), and source-specific evidence detail. Filters by source type, market/jurisdiction, project, field, actor, date range, and change type; click-through to project + evidence + agent reasoning with the same hover/popover affordances as the Review Queue. Expose both `news_articles.fetched_at` and `agent_runs.created_at` for news-agent rows so researchers can distinguish "article arrived" from "agent decision completed" when child-job queue depth causes lag. Saved-view filter presets must include: (a) **non-escalated agent decisions** for researcher verification (the original spot-check sampler scope), with an explicit slice for `low_confidence` runs where a deterministic `discarded` reference was promoted to an existing project because that is the highest-authority news-agent override path; (b) **auto-applied without review** — every resolver-driven field change that bypassed the Review Queue (LADBS-driven status updates, CoStar/Pipedream baseline updates, semantic Pass 2c auto-promotions on strong physical signals, agent `confirm_existing_project` / `promote_existing_project` / `no_change` decisions); (c) **semantic auto-promotions** alone; (d) **per-market reason-code distribution** with gap-rate / unmappable-rate / reviewer-rejection-rate metrics (per the 2026-05-07 multi-market glossary design, default thresholds: gap rate > 15%/field, unmappable rate > 5%/field, rejection rate > baseline + 2σ; auto-alerts when a market exceeds the established-markets baseline). Researcher uses the per-market alerts to drive per-market `semantic_glossary.yaml` updates. **Good slot for `UI.QA` once this view ships — every researcher-facing surface is now live.**
   2026-05-08 first implementation slice: `/activity` now has a server-rendered Activity page backed by `GET /activity/events`. The endpoint combines `change_log`, `resolution_log`, and `agent_runs` / `agent_run_review_items`, with saved presets for agent decisions, auto-applied changes, and semantic status activity. News-agent rows expose both `article_fetched_at` and `agent_created_at`. Follow-up filter hardening preserves preset query params, hides no-op resolution rows, sorts by parsed timestamps, makes failed-agent titles outcome-aware, and lets publisher-source filters include linked news-agent rows; remaining step 11 work is market/jurisdiction slicing, richer evidence drill-through, and direct `news_semantic_interpretations` reads for per-market reason-code/gap-rate distribution metrics.
12. Pipedream coverage compare job (Q10/Q11) — built here; first run is `AGENT.reset` step against the June Pipedream refresh.
13. AGENT.3 permits — same runner, permit source profile, narrow trigger set (`new_candidate`, >10% unit delta, product-type change), permit-specific tools, scoped permits cost-cap bucket, `agent_enabled_for_permits` kill switch.
14. AGENT cutover event — single ship moment for AGENT.2 + AGENT.3.
15. `AGENT.reset` — see roadmap row below.

### Cross-cutting: UI.QA visual inspection
> **Goal:** Holistic walkthrough of every researcher-facing surface on real data — Review Queue, Project Detail (all tabs), Coverage, Pipeline list/map, Dashboard, Activity / Audit Log — to catch layout, flow, design, copy, affordance, and provenance issues before UI patterns lock in for second-market onboarding.
> **Priority:** Flexible. This step is on the radar but never gates development. The user must be at the computer for an extended manual session, so timing is researcher-driven.
> **Depends on:** AGENT.2 step 11 (Activity / Audit Log view) ship at minimum.
> **Recommended completion:** Before Phase H (Santa Monica) starts — once a second market goes live, layout changes become cross-market migrations.

| Step | Task | Status | Notes |
|------|------|--------|-------|
| UI.QA | Researcher-driven visual inspection pass | `flexible` | Holistic walkthrough on real data of every researcher-facing surface (Review Queue with consolidated decision cards + hover/click snippets, Project Detail tabs, Coverage news-source health, Pipeline list/map, Dashboard, Activity / Audit Log). Looks for layout, flow, design, copy, affordance, and provenance issues. No predetermined work product; outputs are issues filed against specific surfaces. **Three natural slots, in escalating recommendation strength:** (1) ship of AGENT.2 step 11 (Activity / Audit Log view) — first opportunity where every researcher-facing surface is live; (2) every `AGENT.reset` stabilization cycle — each cycle's clean post-reset state is a fresh test surface, and the final-settle cycle is the natural completion point; (3) start of H.0 (Santa Monica market-agnostic hardening audit) — strongly recommended before Phase H, since UI patterns lock in once a second market is live. Until this step is `done`, those three slots carry "good slot for `UI.QA`" annotations; once done, the annotations drop out. A lighter pre-pass after AGENT.2 step 9 (`material_contradiction`) is optional but recommended to catch structural review-item layout issues before they compound. |

### Phase E: Resolution Engine Refinements
> **Goal:** Implement remaining spec features that improve data quality.
> **Priority:** Medium — improves quality but system works without these.
> **Depends on:** Phase A. Can be built in parallel with other phases.

| Step | Task | Status | Notes |
|------|------|--------|-------|
| E.1 | Delivery year freshness threshold | `not_started` | Source-provided dates older than 6 months should be treated as stale. UC projects exempt from freshness check. See EVIDENCE_LAYER_DECISIONS.md for full rules. |
| E.2 | Auto-stall detection | `not_started` | Flag projects with 12+ months of no new evidence as potentially stalled. Generate review items, don't auto-change status. |
| E.3 | Externalize likelihood config to YAML | `not_started` | Move base rates and signal weights from `likelihood.py` to `config/likelihood.yaml`. Makes tuning easier without code changes. |
| E.3a | Developer registry + apply-script hardening | `superseded` | Moved forward to C.a.1 so registry pollution hardening lands before Phase D news ingestion and before the first Santa Monica Phase-A-style apply cycle. |
| E.4 | Wire seed ingesters to write evidence | `not_started` | Modify `db/seed.py` so future Pipedream/CoStar seed imports also create evidence rows. Currently only the collector path and backfill scripts write evidence. |
| E.5 | Delivery year size adjustment | `not_started` | The estimation formula supports a size adjustment but it's hardcoded to 0. Implement: large projects (500+ units) add 1 year to estimate. |
| E.6 | Workforce units as distinct unit bucket | `done` | 2026-05-06: Added nullable `projects.workforce_units` and `news_project_references.candidate_unit_workforce`, plus extraction schema/prompt support, news evidence integration, resolver/differ/review/override support, Pipedream/backfill mappings, agent retrieval context, project-detail UI exposure, docs, and focused tests. Workforce units are a component of total units, distinct from both affordable and market-rate units; unknown remains `NULL`. |

### Phase F: Additional Collectors
> **Goal:** Expand LA source coverage beyond LADBS.
> **Priority:** Lower — LADBS is the primary lifecycle source and it's already working. These add incremental coverage.
> **Depends on:** Phase A. Independent of frontend work.
> **Required reading:** `ARCHITECTURE.md` Section 4b (source-by-source analysis — access methods, endpoints, field coverage, SoQL filters, rate limits for every source), Section 4c-4d (source role summary and collector type mapping), Section 5 (collection workflow — pull vs. lookup pattern, execution order, cycle timing). Critical: ZIMAS is lookup-only (no bulk export) — case numbers must come from other sources first. LA Case Reports drives the biweekly cadence.

| Step | Task | Status | Notes |
|------|------|--------|-------|
| F.1 | LAHD affordable housing collector | `not_started` | Socrata mymu-zi3s / an7z-aq2k. Reuses Socrata collector + new source adapter. Fills affordable housing gap. |
| F.2 | LA Case Reports PDF collector | `not_started` | Biweekly PDF API: `planning.lacity.gov/dcpapi/general/biweeklycase/doc/{id}`. Parser with pdfplumber. Early warning for large projects in entitlement. |
| F.3 | ZIMAS/PDIS scraper (enrichment) | `not_started` | Accepts case number → scrapes PDIS page → returns structured fields. NOT for bulk discovery. Depends on case numbers flowing in from other sources. |
| F.4 | Case number chaining | `not_started` | Chain from LA Case Reports → ZIMAS, from Pipedream Site1 URLs → PDIS, from LADBS permits → case lookups. |
| F.5 | CEQAnet collector | `not_started` | Custom scraper — early warning for large projects with environmental review. |
| F.6 | LA County planning collector | `deferred` | Socrata ccmr-xemc. Don't wire in until a county or unincorporated market exists. |

### Phase G: Output Formats
> **Goal:** Export pipeline data for researcher consumption and client deliverables.
> **Priority:** Lower — useful once the data is validated and the workflow is proven.
> **Depends on:** Phase A + Phase C (need validated data + researcher review workflow established).

| Step | Task | Status | Notes |
|------|------|--------|-------|
| G.1 | Excel template export | `not_started` | Export project data to the format researchers are used to (Pipedream-like layout). |
| G.2 | Summary tables / CMA block | `not_started` | Market-level summary statistics in tabular form. |
| G.3 | Exhibit appendix generation | `not_started` | Formatted project-by-project appendix for client reports. |
| G.4 | Evidence provenance export | `not_started` | Per-project source audit: which sources contributed, when, what they said. |

### Phase H: Santa Monica Market
> **Goal:** Prove multi-market by standing up SM with its distinct source types (PDF-heavy + Socrata + Accela).
> **Priority:** After LA is stable.
> **Depends on:** Phases A-C minimum, ideally F.2 (PDF parser reusable for SM).
> **Required reading:** `ARCHITECTURE.md` Section 4b sources 4-6 (SM Dev Tracking PDF, SM Ministerial PDF, SM Active Permits Socrata — access methods, URL patterns, field coverage). SM Dev Tracking is SM's "gold standard" discovery source (457 projects, weekly PDF). Accela API is optional — Socrata covers most needs.

| Step | Task | Status | Notes |
|------|------|--------|-------|
| H.0 | Market-agnostic hardening audit + address context foundation | `not_started` | Before adding SM-specific collection logic, audit procedures built before the market-agnostic principle was explicit: collectors, source routing, matchers, address normalization, news structural extraction, Pass 3 re-extraction conflict checks, resolvers, CLIs, API writes, and UI filters. Replace hardcoded LA/SM branches with market/jurisdiction/source config where practical; where not practical, document the extension point. Add a market/jurisdiction address context layer (default state/city/county where applicable, city aliases, jurisdiction aliases, boundary/coverage notes) and update current address-conflict/structural-extraction code to consume it instead of maintaining code fallback maps. Produce the first living new-market onboarding checklist from this audit. **Strongly recommended slot for `UI.QA` if not yet complete — UI patterns lock in once Santa Monica goes live, so this is the latest-good moment to catch layout/flow/design issues before they become cross-market migrations.** |
| H.1 | Write SM market config | `not_started` | Use the H.0 onboarding checklist and address context layer. Santa Monica should prove that a second market can be added primarily through config/source docs, not new hardcoded app conditionals. |
| H.2 | Configure SM Dev Tracking PDF collector | `not_started` | Reuses PDF parser. Predictable monthly URL pattern. |
| H.3 | Configure SM Ministerial PDF collector | `not_started` | Cross-ref with Dev Tracking on address + permit number. |
| H.4 | Configure SM Active Permits (Socrata kpzy-s8rg) | `not_started` | Reuses Socrata collector. |
| H.5 | (Optional) Build Accela collector for SM ePermit detail | `not_started` | OAuth required. Lower priority. |
| H.6 | Seed SM with CoStar + run full cycle | `not_started` | **Blocked on C.a.1** — the Phase A apply script (`scripts/apply_phase_a_decisions.py`) currently hard-codes architecture-firm overrides by raw_value only. Before reusing it for SM, refactor to address-scoped or per-row overrides per C.a.1 item (2). |

### Phase I: Generalization & Additional Markets
> **Goal:** Template for standing up any new market quickly.
> **Priority:** After SM proves the pattern.
> **Depends on:** Phase H.

| Step | Task | Status | Notes |
|------|------|--------|-------|
| I.1 | Formalize new-market onboarding process | `not_started` | Graduate the H.0 checklist into a repeatable process: required market/jurisdiction config, address context, source docs, source registrations/news sources, resolver/source-tier mapping, boundary assumptions, geocoder behavior, seed/backfill plan, review/coverage expectations, and operational runbook. For news sources, the researcher selects a per-source backfill window during onboarding: mature markets with robust existing data can use short windows (for example, 8 weeks for LA), while new markets without pipeline history may need 12-24 months to bootstrap project awareness. Record the window as `news_sources.config.backfill_window_days`; the D.B-style backfill CLI must price that chosen window before approval. **After AGENT sprint:** any structured intake source (news, permits, CoStar, Pipedream, future X) introduced by a new market also requires an **agent source profile** as a required onboarding artifact (per `agentic_escalation_design.md` §5.9). The profile declares the source's agent triggers, allowed tools, prompt template, cost-cap bucket, and kill switch. **Per `docs/specs/semantic_interpretation_layer_design.md` §5.15 (revision 4):** any new market with news scraping also requires a **per-market semantic glossary addendum** at `config/markets/<slug>/semantic_glossary.yaml` before going live. Process: collect 50-100 representative articles, run through the existing `interpret_v1` prompt with no addendum, identify recurring `glossary_gap_observed` and `*_unmappable` outputs, draft canonical mappings for the recurring unfamiliar phrases (with `reason_code_extension` entries that register against `(news_v1, <field_name>)`), iterate until gap-rate metrics drop below the §5.14.3 alert thresholds, and ship via git/deploy. Record the onboarding evaluation (sample URLs, pre/post gap rate, top phrases addressed, residual gaps accepted) at `config/markets/<slug>/onboarding_evaluation.md`. |
| I.2 | Build market config template + validation | `not_started` | Validate required address context and source registration metadata so missing market-specific assumptions fail during onboarding rather than surfacing as hidden LA defaults at runtime. |
| I.3 | Formalize source registry + adapter contract | `not_started` | |
| I.4 | Plan `ProjectMarketMembership` migration for overlapping markets | `not_started` | Required before any city + county overlap goes live. |
| I.5 | Select and stand up third market (different metro) | `not_started` | True generalization test. |

### Phase J: Platform Admin Console
> **Goal:** Give administrators in-app control over operational settings that should not require code deploys or direct database edits.
> **Priority:** Later-stage platform hardening. Build after the Phase D/D.M news ops path proves the first concrete admin workflows, but design new LLM and scrape features so they can register into this console later.
> **Depends on:** Phase C write boundary/auth, D.M for the first news run ledger and cost controls, and Phase I for stable market/source onboarding concepts.

| Step | Task | Status | Notes |
|------|------|--------|-------|
| J.0 | Admin shell, permissions, and audit contract | `not_started` | Create a dedicated `/admin` area guarded by an explicit admin role, not only the current allowlisted-email pattern. Define shared admin API guardrails, mutation audit requirements, and navigation rules before adding write-capable controls. |
| J.1 | LLM model configuration console | `not_started` | Add an in-app registry for every LLM capability: news triage, default extraction (per AGENT.1 model A/B outcome), semantic interpretation (`semantic.news_v1`), the agent runner's per-source profiles (`agent.news_v1`, `agent.permit_v1`, future `agent.costar_v1` / `agent.pipedream_v1`), output-quality retry, future corroboration/resolver helpers, and any later LLM-assisted workflow. Admins should be able to choose provider/model and bounded model parameters per capability with safe defaults, environment-aware validation, and audit history. New LLM call sites should register a stable capability key and persist the model/config version used so this console can switch models without code rewrites. Per the AGENT sprint, each source profile already registers its capability key; J.1 generalizes the existing `agent_runs.profile_name` pattern into a UI surface. |
| J.2 | Cross-app scrape and source-run ledger | `not_started` | Generalize the D.M news run table into a platform-wide newest-first operations table for all scrapes/imports/backfills. Include filter/sort by source, market/jurisdiction, trigger, status, worker, date range, cost, and failure category; show records pulled, items extracted, article reviews/re-reviews/conflicts/new candidates where applicable, token/cost totals, alerts, and links to source runs, scrape jobs, articles, review items, and raw run diagnostics. |
| J.3 | User and role management console | `not_started` | Admin UI to view users, assign roles (for example admin, researcher, viewer), deactivate access where supported, and audit every role change. Align with Supabase Auth, PostgREST RLS, and FastAPI JWT enforcement so UI roles and backend permissions do not drift. |

---

## 5. Completed Work — Historical Build Phases

> These sections record what was built before this roadmap was created, for reference. They correspond to the original build plan in ARCHITECTURE.md Section 7.

### Original Phase 1: Foundation (Complete)

All steps done. Database schema, Pipedream + CoStar ingesters, address normalization, seed persistence. See ARCHITECTURE.md Section 7 Phase 1 for details.

### Original Phase 2: First Public Source + Matching (Complete)

Socrata collector, LADBS permit adapters, matcher, differ, status rules, collect+persist pipeline, incremental cursor, source-row metadata. See ARCHITECTURE.md Section 7 Phase 2 for details. Note: steps 2.4, 2.5, 2.10 were marked `in_progress` in the original doc but are functionally complete — the remaining gaps (broader field-level diffing, reconciliation reporting) are now handled by the evidence layer.

### Original Phase 3: Discovery + Enrichment (Partially Complete)

Steps 3.0 through 3.0f (LADBS source bundle expansion, recall audit, Socrata coverage diagnosis, source rewire) are all done. Steps 3.1–3.5 (PDF parser, LA Case Reports, ZIMAS, case number chaining, discovery cycle) are not started and have been moved to Phase F of this roadmap.

### Evidence Layer Retrofit (Complete — Phases 1-4 of retrofit plan)

This was a major architectural addition not in the original build plan. Documented in `docs/specs/evidence_layer_retrofit_plan.md`. All four core phases are complete:
- **Retrofit Phase 1:** Schema + backfill infrastructure (migration, evidence table, developer tables, new project columns, backfill scripts)
- **Retrofit Phase 2:** Resolution engine + all field resolvers + likelihood + confidence
- **Retrofit Phase 3:** Collector refactor (evidence writes wired into collect.py, dual-write with PSR, resolve_project called after evidence insert)
- **Retrofit Phase 4:** Developer canonicalization (registry, alias management, fuzzy matching, CLI commands)

---

## 6. Tech Stack

### Infrastructure
- **Database:** Supabase (hosted PostgreSQL with PostGIS + pg_trgm; **`pgvector` added in AGENT.1** for article-chunk embeddings powering the agent's `search_articles_similar` retrieval tool)
- **Backend / collectors:** Python 3.11+
- **Frontend:** Next.js (React) on Vercel, Supabase JS client, Tailwind CSS
- **Hosting:** Render for backend workers/cron jobs, Vercel for frontend, Supabase for database

### Key Python Dependencies
`httpx`, `sqlalchemy` + `psycopg`, `alembic`, `geoalchemy2`, `usaddress`, `rapidfuzz`, `openpyxl`, `pydantic` + `pydantic-settings`, `typer`, `tenacity`

### Frontend Dependencies (Phase B+)
`next`, `@supabase/supabase-js`, `tailwindcss`, `react-map-gl`, `maplibre-gl`

### Frontend Write-Path Backend (Phase C+)
`fastapi`, `uvicorn`, `python-jose` (or `pyjwt`) for Supabase JWT verification. Thin HTTP layer over `src/tcg_pipeline/db/review_workflow.py`; deploys as an additional Render service. Frontend reads remain direct via Supabase PostgREST with RLS; only writes route through this API. See Phase C.a and the 2026-04-23 Decision Log entry.

### News / Research Dependencies (Phase D)
`httpx` + `trafilatura` for polite news fetch/extraction, `anthropic` for triage/extraction, `croniter` for schedules, and `dateparser`/`pyahocorasick` for structural extraction. Browser/advanced-fetch dependencies are deferred to D.late.ADV / D.late.C.

### Agent Layer Dependencies (AGENT sprint)
- **LLM providers:** Direct provider APIs are the current policy for all built/current AGENT work unless explicitly revised: `anthropic` for Claude/agent calls and `openai` for GPT candidates. **Multi-provider abstraction added in AGENT.1** so the default-extraction A/B harness can measure GPT-5.4/GPT-5.5 alongside Anthropic models. Vercel AI Gateway support exists as a deferred operational option; it requires `AI_GATEWAY_API_KEY`, never falls back to `OPENAI_API_KEY`, and must not be used in production or future AGENT steps until a code/config sweep confirms every LLM call site is intentionally routed through Gateway. First harness routing convention: Claude candidates through native Anthropic, GPT candidates through native OpenAI.
- **Embeddings:** AGENT.1 uses direct OpenAI `text-embedding-3-small` (1536 dims, priced in code at `$0.02 / 1M input tokens` for cost-cap accounting). Alternatives such as Voyage/Cohere remain future model-registry options and would require a vector-dimension migration/sweep.
- **Vector storage:** `pgvector` extension on the existing Supabase Postgres. Article-chunk embeddings live in a new `news_article_chunks` table indexed via `ivfflat` or `hnsw`.
- **Geospatial:** Existing PostGIS + `geoalchemy2`, plus a new GIST index on `Project.location` (one-line migration in AGENT.1) to support `get_nearby_projects` at sub-50ms latency.
- **Default extraction model:** Opus 4.7 selected after AGENT.1 A/B data. Primary smoke run measured Opus 4.7 / Sonnet 4.6 / GPT-5.4; supplemental run measured Opus 4.7 / Opus 4.6 / GPT-5.5. Cost informed the choice but did not gate it.

See ARCHITECTURE.md Section 10 for the full stack listing.

---

## 7. Key Reference Documents

| Document | Location | Purpose |
|----------|----------|---------|
| Architecture & Design Spec | `ARCHITECTURE.md` | Detailed data model, source inventory, field inventories, matching strategy, collection workflow. The original design document. |
| Evidence Layer Integration Guide | `docs/specs/EVIDENCE_LAYER_INTEGRATION_GUIDE.md` | Comprehensive guide to the evidence layer: schema, resolution rules, integration points. Written for Claude Code / Codex agents. |
| Evidence Layer Decisions | `docs/specs/EVIDENCE_LAYER_DECISIONS.md` | Implementation decisions answering developer questions about edge cases, thresholds, and design tradeoffs. Includes review-protected override semantics (§22), superseded conditional override semantics (§21), STATUS_CHANGE rejection (§21a), observation ordering (§21b), and delivery-date override provenance (§21c). This is a living document — check for recent additions. |
| Evidence Layer Retrofit Plan | `docs/specs/evidence_layer_retrofit_plan.md` | Initial retrofit assessment: repo audit, schema changes, migration strategy, effort estimates. |
| News Research Design | `docs/specs/news_research_design.md` | Phase D implementation contract for Pass 0/1, triage, source registry, scheduler, and source-strategy discipline. **Pass 3a/3b sections of this doc are superseded by `agentic_escalation_design.md` once the AGENT sprint ships.** D.2-docs revises it for the Urbanize-first polite collector, deferred advanced fetch, per-source backfill windows, and source-doc discipline. |
| Agentic Escalation Layer Design | `docs/specs/agentic_escalation_design.md` | Implementation contract for the AGENT sprint. Covers the source-agnostic agent runner, `agent_runs` audit table, source profile abstraction (§5.9), shared TCG semantic field interpretation layer (§5.1.1), AGENT.1 default-extraction A/B results (primary Opus 4.7 / Sonnet 4.6 / GPT-5.4 plus supplemental Opus 4.6 / GPT-5.5), retrieval infrastructure (`pgvector` + GIST index), output-quality retry path, contradiction-detection move (§5.5 with mandatory C.i impact assessment), worker-model decision (one job per article needing escalation), cost guardrails (scoped daily caps + runtime kill switches per §5.8), and eval methodology (three-layer + spot-check sampler). Living document — check the §13 revision history for recent updates. |
| C.i Contradiction Impact Assessment | `docs/specs/ci_contradiction_impact_assessment.md` | **Pre-build deliverable for AGENT.2** — created and researcher-reviewed 2026-05-04. Concise table-format enumeration of every code path that depends on the current `detect_project_contradictions` outputs, with `⚠ HUMAN REVIEW` markers on uncertain rows. Researcher decisions on all open items recorded in §I of that doc. AGENT.2 is cleared to proceed once D.6 closes out and AGENT.1 stabilizes. |
| Semantic Interpretation Layer Design | `docs/specs/semantic_interpretation_layer_design.md` | Implementation contract for AGENT.2 sub-sequence step 7. Defines the per-source semantic interpreter framework, output schema, reason-code registry, LLM-backed news Pass 2c interpretation (`interpret_v1`, single-shot Opus, no tools), other field interpreters (product_type, age_restriction, date_delivery, unit_buckets), multi-market glossary addenda, glossary-gap handling, jurisdiction permit-data-quality policy schema with operational upgrade criteria, resolver corroboration logic, and the deterministic LADBS port that establishes the structured-source pattern. Pre-Leasing detection (AGENT.6) is explicitly out of scope but the doc defines the signal-flag contract AGENT.6 consumes. |
| News Source Strategy Docs | `docs/sources/news/<slug>.md`, `docs/sources/README.md` | Required runbook/context docs for every `news_sources` row: status, access, discovery, fetch path, robots/rate limits, quality observations, operational history, open issues, and code references. |
| LADBS Source Audits | `docs/audits/` | Recall audits, coverage analysis, source rewire verification for LADBS Socrata datasets. |
| Market Config (LA) | `config/markets/los_angeles.yaml` | Source definitions for the Los Angeles market. |
| Source Tier Config | `config/source_tiers.yaml` | Source type → tier mapping for the evidence layer. |

---

## 8. Decision Log

> Append-only. Record significant decisions with date, what was decided, and why. Don't edit old entries — add new entries that supersede them.
>
> **Historical decisions:** `ARCHITECTURE.md` Section 8 contains the full decision log from project inception (2026-04-15 onwards) — approximately 90 entries covering source-specific semantics, ingestion rules, matching thresholds, schema design choices, and LADBS evidence type rules. This ROADMAP carries forward the most architecturally significant entries below. For source-specific implementation decisions (e.g., "only final CofO with real date emits Complete evidence," "only recent substantive inspections emit UC evidence," "CoStar maps by header name not column number"), always consult ARCHITECTURE.md Section 8.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-04-15 | CoStar + Pipedream as seed sources, public data for updates/discovery | CoStar gives breadth, Pipedream gives depth. Public sources fill gaps and keep things current. |
| 2026-04-15 | Pipedream/TCG data takes priority over CoStar where both exist | Human-verified data is more reliable than CoStar's automated collection. |
| 2026-04-15 | Researcher overrides are protected from automated updates | Prevents the system from clobbering human judgment. |
| 2026-04-15 | Start with LA as proof of concept, design for multi-market | Validates architecture on a real market without over-engineering. |
| 2026-04-15 | Supabase (hosted PostgreSQL) from day one, no SQLite phase | PostGIS, dashboard, auth, REST API, realtime for future UI. |
| 2026-04-16 | LADBS treated as a source bundle, not a single endpoint | Multiple datasets needed for full lifecycle coverage: permits, inspections, CofO. |
| 2026-04-17 | Rewire LADBS sources from frozen hbkd-qubn/cpkv-aajs to live pi9x-tg5x/9w5z-rg2h | Frozen datasets were causing false not-found results. Live replacements recover ~50 of 71 missing UC projects. |
| 2026-04-20 | Add evidence layer — append-only evidence store with resolution engine | ProjectSourceRecord upsert model loses history. Evidence layer preserves all observations, enables confidence scoring, supports per-field resolution rules. Major architectural addition. |
| 2026-04-20 | Keep ProjectSourceRecord as "current state cache" alongside evidence | Preserves backward compatibility with matcher during incremental migration. Can be removed later. |
| 2026-04-20 | Source tiers: Tier 0 (researcher), Tier 1 (government + Pipedream), Tier 2 (news), Tier 3 (CoStar), Tier 4 (social) | Establishes hierarchy for resolution rules. Researcher overrides are sacred Tier 0. |
| 2026-04-20 | Developer canonicalization: rapidfuzz token_set_ratio, >=90 auto, 75-89 flag, <75 new | Balances automation with human review for ambiguous matches. |
| 2026-04-20 | Hash-based evidence dedup | Only insert new evidence when raw_data_hash changes. Prevents table bloat from incremental overlap re-pulls. |
| 2026-04-22 | Frontend prioritized before additional collectors and Excel outputs | Need visual understanding of data to inform researcher workflow design. Can't design the tool without seeing the data. |
| 2026-04-22 | News scraping (BizJournals) prioritized before remaining collectors | Tier 2 news fills intelligence gaps (developer announcements, timelines, opposition) that government sources miss. Higher value per source than incremental Tier 1 coverage. |
| 2026-04-22 | De-prioritize LAHD, LA Case Reports, ZIMAS, Excel outputs | These are incremental improvements. The core pipeline (LADBS → evidence → resolution → review) is already working. Circle back after frontend + news are proven. |
| 2026-04-22 | Build plan moved from ARCHITECTURE.md to ROADMAP.md | ARCHITECTURE.md is the design reference. ROADMAP.md is the living build plan. Separation prevents the design doc from becoming stale as priorities shift. |
| 2026-04-22 | Researcher overrides are conditional by default (`until_newer_evidence`), not unconditionally sacred | Refinement of earlier "Tier 0 never clobbered" rule. Review-driven overrides hold until genuinely newer evidence appears. Sticky mode still available for explicit permanent locks. Legacy overrides without `mode` treated as sticky for backward compat. See EVIDENCE_LAYER_DECISIONS.md §21. |
| 2026-04-22 | STATUS_CHANGE rejection writes a conditional override and re-resolves | Rejecting a status change blocks that evidence from re-applying but doesn't block newer evidence. Override baseline is copied from current resolution. See EVIDENCE_LAYER_DECISIONS.md §21a. |
| 2026-04-22 | Observation ordering: evidence_date → collected_at → source priority → source_tier | Closes bug where older high-priority source could beat newer lower-priority source. Re-running resolve-all may shift some developer values. See EVIDENCE_LAYER_DECISIONS.md §21b. |
| 2026-04-22 | delivery_year_provenance set to `researcher_override` when date_delivery comes from override | Returns to evidence-derived provenance only after the override is explicitly cleared or an `override_contradiction` accept-new decision clears it; newer evidence no longer silently supersedes the override. See EVIDENCE_LAYER_DECISIONS.md §21c/§22. |
| 2026-04-23 | Ignore polluted generic developer registry names and do not recreate them automatically | Production had a `Category` registry row with unrelated aliases. Matching and persistence now block ignored generic names, and the polluted row was deleted from prod so future sweeps do not recreate it. |
| 2026-04-23 | Fuzzy-review developer canonicalization stays shadow-only even under `canonicalize-developers --apply` | 75-89 similarity matches remain ambiguous. Auto-merging registry rows or rewriting project developer values at that threshold can make bad matches sticky, so apply mode now skips those writes until reviewed. |
| 2026-04-23 | `estimated_calc` may fill blank `date_delivery` values for Proposed / Pending / Approved projects | Phase A validation spot-checked 10 representative null-to-estimate rows and accepted the full 218-row bucket. The value remains explicitly tagged by `delivery_year_provenance = estimated_calc` so downstream consumers can filter or down-weight it. |
| 2026-04-23 | Low-tier `total_units` overwrites are allowed when absolute delta is `<= 5`; larger deltas require researcher review | Phase A review showed small deltas behave like measurement noise while larger deltas often reflect phase splits or project-identity ambiguity. The Phase A apply profile therefore accepted 7 small deltas and wrote conditional overrides for 20 larger deltas to preserve current values until newer evidence arrives. |
| 2026-04-23 | Recent article evidence can outrank CoStar for `date_delivery` when the article is within the last 6 months | Forward-looking requirement for Phase D. Articles often capture operator-stated timeline updates that lag in CoStar. This is a field-specific source-priority override for delivery dates only, not a general tier change. |
| 2026-04-23 | `canonicalize-developers --apply` must not rewrite a project developer field that is currently protected by a researcher override | Phase A apply surfaced one override-clobber case (`Lake on Wilshire`). Future sweeps now preserve project developer values whenever an active `researcher_overrides.developer` row is present; canonical registry maintenance can still proceed independently. |
| 2026-04-23 | Frontend write path will be a Python FastAPI service over `review_workflow.py`, not Supabase Edge Functions or PL/pgSQL | `review_workflow.py` is ~500 lines of orchestration (accept/reject/defer, orphan evidence linking, cross-project conflict detection, override writing, resolution re-run). Porting to TypeScript duplicates logic across two stacks with guaranteed drift; PL/pgSQL makes testing and control flow painful. FastAPI reuses existing Python, deploys on the Render stack already in use, and keeps the same code callable from CLI/cron. Reads continue to go direct through Supabase PostgREST with RLS; only writes route through the API. RLS policies must land with B.1 — mutable tables locked to service role so the API boundary is not bypassable. Affects Phase C.a and the B.1/B.2 scaffolding choices. |
| 2026-04-23 | Review-protected override semantics supersede sticky/until_newer_evidence | Researcher inputs never silently rot (sticky problem) and never silently yield (until_newer_evidence problem). Every override holds its value until explicitly reviewed; new contradicting evidence generates a review item at minimum MEDIUM priority. See `docs/specs/EVIDENCE_LAYER_DECISIONS.md` §22. |
| 2026-04-23 | Review Queue uses batch-commit model, not immediate-apply | Researchers stage decisions as they work; commit applies all staged decisions atomically. Staged state persists across sessions per-user. Decisions can be revised before commit. Commit allows partial — undecided rows stay in queue. Rationale: matches researcher workflow (scan through changes, decide, act in batch). Avoids half-applied updates if a session is interrupted. |
| 2026-04-23 | Deferred review items stay at the bottom of the same queue, never auto-expire | "Queue cleared" requires deferred items also decided. Deferred count surfaced in Coverage so backlog is visible, not hidden. A separate Deferred tab would risk becoming a forgotten graveyard. Choice is explicit trade-off: less disciplined researchers will have non-empty queues; more disciplined researchers will clear them. |
| 2026-04-23 | Optimistic last-click-wins concurrency; no row-level locking | At 1-3 users, row-level locking is ceremony without payoff. When User A stages a decision, User B sees the row removed from their queue next refresh. If B tries to stage concurrently, B's request returns 409 with a "just decided by A" banner. Revisit if team grows. |
| 2026-04-23 | Coverage is the name of the jurisdictions view | Analyst-friendly term: "I cover LA, Phoenix, Seattle." Beats "Jurisdictions" (literal), "Scope" (technical), "Territories" (sales-y). |
| 2026-04-23 | Badge palette: eight source categories with color + text | Blue = live user input (current user and other TCG researchers, distinguished by initials). Teal = Pipedream (legacy spreadsheet, distinct from live input). Green = Gov. Amber = News. Purple = CoStar (per TCG internal convention). Slate = Web (developer websites). Gray = Computed. Small, subtle, paired with shape/icon for accessibility. See `docs/specs/ui_requirements.md` §9. |
| 2026-04-23 | Keyboard shortcut layout uses asdf for decisions | Homerow-left, one-handed, adjacent keys. Keycaps on buttons show the shortcut — labels don't have to match initials. Accept new = a, Keep old = s, Defer = d, Custom = f. Navigation: jk, hl. Bulk: shift-a/s/d. Goto: g+letter. |
| 2026-04-23 | Pipedream replacement is the Phase C target, not Phase B | Phase B ships read-only for expectation-setting and data validation. Phase C-early (inline editing, new project creation) is the point at which researchers stop using Pipedream. Structuring Phase C into early (edit) and late (review queue) ordering lets researchers use the tool in a useful way earlier. |
| 2026-04-23 | Web scraping is strictly developer_website for now | The Web badge category maps to `developer_website` source_type only. No general-web scraping planned for MVP. Category is reserved for future expansion. |
| 2026-04-24 | Take a DB snapshot before Phase B schema migrations | Markets, jurisdictions, and source-run backfills touch core project scoping and freshness metadata. A Supabase point-in-time backup or `pg_dump` immediately before B.0b/B.0c gives a cheap recovery path if backfill logic is wrong. |
| 2026-04-24 | B.1 must ship one real authenticated read path | Scaffolding alone can hide Supabase client, RLS, session, and Server Component integration bugs until B.2. `/coverage` must load real jurisdiction rows through Supabase PostgREST with RLS before the next UI surface builds on it. |
| 2026-04-24 | Source-populated direct fields are read-only for MVP | Fields populated directly by CoStar/Pipedream/collectors but not currently owned by the resolution engine (`rent_or_sale`, physical attributes, unit mix, owner, zoning, etc.) should not be editable until the backend either promotes them to evidence-derived fields or teaches ingesters to respect overrides. Avoids researcher edits being overwritten by the next source refresh. |
| 2026-04-24 | Vercel previews use production Supabase during read-only Phase B | Previews stay behind the same `ALLOWED_EMAILS` allowlist and read from the same Supabase project while no writes exist. This keeps review simple and avoids premature staging infrastructure. Before Phase C writes, revisit and likely split previews to staging Supabase/FastAPI. See `docs/ops/frontend_deployment.md`. |
| 2026-04-24 | Coverage may read `review_items` but cannot write them in Phase B | B.2 needs pending/deferred queue counts per jurisdiction. Authenticated users get SELECT on `review_items` under RLS; no INSERT/UPDATE/DELETE grants are added. Review decisions and queue mutation still wait for the Phase C FastAPI write path. |
| 2026-04-25 | Use MapLibre and configurable raster tiles for the Pipeline map | Mapbox GL JS v3 introduces token/licensing/telemetry concerns for this tokenless internal map, so Pipeline now uses `react-map-gl/maplibre` + `maplibre-gl`. Tile URL and attribution are environment-configurable. The default remains OpenStreetMap only as a short-term internal fallback; choose a paid or self-hosted provider (MapTiler, Stadia, Protomaps, etc.) before routine/heavier usage. |
| 2026-04-25 | Use Postgres read views for UI evidence summaries | Phase B reads should avoid shipping whole append-only tables to Next.js just to compute UI summaries. Pipeline now reads `project_latest_evidence` (`DISTINCT ON project_id`) through PostgREST/RLS. B.4 should add a richer per-field provenance read model from `resolution_log` rather than extending client-side reductions. |
| 2026-04-25 | Project Detail Snapshot uses `resolution_log` as the field provenance read model | B.4 source badges and hover provenance should reflect the field-level resolution trail, not just the newest evidence row for the project. Added `project_field_resolution` (`DISTINCT ON project_id, field`) for latest rule, confidence, and evidence IDs per field. B.5 can still fetch full project evidence timelines separately. |
| 2026-04-25 | Snapshot field badges must be honest about provenance gaps | Do not infer field provenance by matching canonical UI names against source-native `extracted_fields` keys. If `resolution_log` does not link evidence IDs for a displayed field, show it as unlinked/system-sourced rather than biasing toward whichever evidence happens to use canonical names. A source-key alias mapper or expanded resolver logging can be added later. |
| 2026-04-25 | Evidence tab reads full project evidence rows directly | The Evidence tab is an audit timeline, not field provenance. It can fetch all evidence rows for the selected project, merge source URLs from `project_source_records`, and show source-native extracted fields/raw JSON honestly. Field-level provenance remains owned by `resolution_log` and Snapshot badges. |
| 2026-04-25 | Evidence field filters use resolver-linked evidence IDs | Snapshot-to-Evidence drill-down must not match canonical UI field names against source-native `extracted_fields` keys. A field filter like `field=total_units` now shows rows whose evidence IDs are linked by `project_field_resolution` for that canonical field. Raw source keys remain visible only as source detail. |
| 2026-04-26 | B.6 tabs are read-only projections over current storage | Resolution uses latest `resolution_log` rows; Changes uses `change_log`; Overrides initially used legacy `projects.researcher_override` JSONB and moved to active `researcher_overrides` rows in C.tail.2. Because the current backend does not store alternatives considered or superseded override history, Phase B surfaces those gaps instead of inventing data. |
| 2026-04-26 | Resolution tab shows resolver output, not only discrepancies | Current `resolution_log` rows are latest resolver outputs for tracked fields, not a discrepancy-only table. B.6 groups changed rows first, collapses unchanged tracked rows, and only exposes Evidence drill-through when linked evidence rows exist. The Changes tab also includes `status_history` so Phase B shows lifecycle activity before Phase C review commits start writing `change_log`. |
| 2026-04-26 | Dashboard tiles stay read-only in Phase B | B.7 provides situational awareness from existing read models and tables, without creating new review items or write-side jobs. Stalled Candidates is a heuristic count for Approved/U/C projects with real evidence dates older than 12 months, not the Phase E auto-stall workflow. Contradictions stays in a Phase C inactive state until contradiction-type review items exist. Needs Attention links to Coverage because `/review` remains a placeholder until the real Review Queue ships. |
| 2026-04-26 | Developer registry hardening moved from Phase E to Phase C | E.3a is now C.a.1 and should run after the write API scaffold/staging gate work, before source snippet/rendering work if possible. The work is self-contained, has no frontend dependency, and unblocks D.4 article evidence writes by preventing news NLP from recreating polluted developer registry rows. Run the long-tail registry audit in shadow mode against prod first, matching the Phase A apply pattern. |
| 2026-04-26 | Researcher overrides table migration kept legacy JSONB during transition | C.c promoted overrides into `researcher_overrides` for per-field history and future contradiction FKs while `projects.researcher_override` remained synchronized. C.tail.2 completed the transition on 2026-04-28: active `researcher_overrides` rows are now the source of truth and the legacy JSONB column is dropped by Alembic `202604280017`. |
| 2026-04-26 | Legacy override mode remains load-bearing until contradiction review lands | Superseded by the 2026-04-27 C.i implementation. `mode` and `baseline` remain stored for audit and contradiction context, but newer evidence no longer silently clears or overrides researcher values; it produces `override_contradiction` review items. |
| 2026-04-27 | Researcher override migration verified before C.d writes | The configured database was snapshotted before applying Alembic `202604260010`. Post-migration verification passed row parity, metadata parity, RLS/index/policy/grant checks, and resolution snapshot comparison. The verifier now builds compare field sets from raw legacy/table pair keys and reads table rows column-only, so migration metadata inspection does not preload override ORM objects into the resolver session. |
| 2026-04-27 | Preview writes are blocked by default for Phase C | Vercel previews may continue read-only Phase B access through Supabase/PostgREST, but must not use a production FastAPI write service for mutation routes by default. Preview write testing requires an explicit target decision: staging FastAPI/Supabase, or a temporary approved production write session with a narrow `ALLOWED_EMAILS` list. C.d write handlers must not rely on Render `APP_ENV` alone because preview/prod identity comes from the caller's Vercel deployment and configured API base URL. |
| 2026-04-27 | Core inline edits use FastAPI override writes | Project Detail Core-field edits are the first real Phase C write path. The browser submits to a Next.js server action, which forwards the user's Supabase access token to FastAPI; FastAPI validates the token/allowlist, writes or clears `researcher_overrides`, runs resolution, and records `change_log` audit rows. Source-populated direct fields remain read-only for MVP, and contradiction detection stays in C.i. |
| 2026-04-27 | Core inline edits are review-protected, not sticky | Manual Core-field values remain current until reviewed, but they are not permanent locks: materially different newer auto-resolved values create `override_contradiction` review items instead of silently overwriting the manual value or silently ignoring the new evidence. New C.d writes use `mode = review_protected`; C.i extended the same review-protected behavior to legacy `until_newer_evidence` overrides and stale contradiction invalidation. |
| 2026-04-27 | Identity fields write directly; notes append history | C.e keeps the write-boundary split explicit: evidence-derived Core edits remain researcher overrides, while researcher-authored Identity/workflow fields update the `projects` row directly through FastAPI. Project notes are append-only in `project_notes`; C.tail.3 moved latest-note display reads to `project_notes` and drops the legacy latest-note columns. Authenticated PostgREST clients receive SELECT only for `project_notes`, with writes reserved for the FastAPI service connection. |
| 2026-04-27 | Relationship links started create-only, then gained maintenance actions | C.f brought Project Detail relationship linking online with read-only Supabase project search and FastAPI-backed link creation/non-empty note updates. C.tail.5 added outgoing unlink, retype, and explicit note clearing through FastAPI `PATCH`/`DELETE`, with `change_log` audit rows for all relationship mutations. Direct authenticated PostgREST writes remain blocked by RLS. |
| 2026-04-27 | Manual new-project creation uses conservative duplicate gating | C.g creates projects through FastAPI, not direct PostgREST writes. The initial duplicate gate reuses the existing matcher with normalized address only because the manual form does not collect deterministic source identifiers yet. If a candidate exists, the API returns it without writing until the researcher explicitly chooses `force_create`; opening or merging into an existing project stays separate from creation. |
| 2026-04-27 | Manual new-project creation is not geocoding or identifier capture yet | C.g intentionally keeps creation narrow: project row, initial status history, duplicate gate, and audit row. Manually created projects may not appear on the map until a geocoder or editable lat/lng path lands, and creation does not yet collect APNs, permit/case identifiers, or source URLs. Add those as a follow-up before relying on manual creation for news-driven project intake at scale. |
| 2026-04-28 | Phase D design committed as `docs/specs/news_research_design.md` | Implementation contract for items D.1–D.B with v2/v3 senior-review revisions baked in. Phase D items reordered for vertical-slice delivery: paste-a-link first (D.7a), BizJournals deliberately late (D.2). New rows added for the Render-worker extension (D.W), §21f resolver implementation (D.4-resolver), monitoring (D.M), graveyard (D.G), and BizJournals backfill (D.B). |
| 2026-04-28 | Phase D extends C.tail.1 RQ/Redis worker; no second queue | The C.tail.1 worker already consumes `scrape_jobs` via Redis/RQ. Phase D registers new task functions (`news_paste_a_link`, `news_scrape`, `news_reextract`, `news_backfill_chunk`) and adds a daemon-thread heartbeat plus an in-process scheduler tick gated by `NEWS_SCHEDULER_LEADER=true`. Concurrency comes from worker instance count, not in-job fan-out. |
| 2026-04-28 | Article auth via session-state injection, not auto-login Playwright | Researcher logs into BizJournals once in a real Chromium window via `tcg-pipeline news auth-bizjournals`; Playwright dumps `storageState` to an encrypted `service_credentials` row; the worker uses the stored cookies for headless fetches. Eliminates the most fragile component of any scraper (login automation). On expiry, an email + Coverage banner prompts the researcher to re-auth. |
| 2026-04-28 | Articles stored as a four-table quartet | `news_articles` (URL, body, metadata) + `news_extractions` (append-only LLM output, prompt-versioned) + `news_project_references` (per-project structured refs from each extraction) + `evidence` (the consumer side, source_type=news_article). Re-extractability, multi-project mentions, and the discarded-article graveyard all depend on this separation. |
| 2026-04-28 | Phase D uses post-apply review semantics consistent with existing collectors | Confirmed-match article evidence updates project fields synchronously via `resolve_project(apply=True)`; the review queue surfaces those changes as `STATUS_CHANGE` / `OVERRIDE_CONTRADICTION` items; reviewers revert via Keep-old (writes a `researcher_override` of the prior value). Not "no auto-apply" — that v1 phrasing was wrong. The status resolver remains conservative (single Tier 2 won't auto-promote to Under Construction). |
| 2026-04-28 | News integrator emits one STATUS_CHANGE review item per changed field | The frontend's `firstChange()` helper at `lib/review/payload.ts:142` reads only `payload.changes[0]`; multi-change items lose Keep-old/Custom decisions on fields 2+. News articles often imply 3-7 field changes, so Phase D iterates the differ and writes one review item per `(project_id, field_name)` with a single-element `payload.changes` list. The existing `(project_id, field_name, item_type)` dedup naturally collapses repeat extractions. |
| 2026-04-28 | `evidence.superseded_at` added so re-extractions deterministically supersede prior rows | Re-extracted evidence rows share `evidence_date` (article published_at) with their predecessors. Without an explicit supersession flag, the resolver's tiebreak couldn't disambiguate. The new column plus a partial index `WHERE superseded_at IS NULL` scopes resolver reads to active rows; superseded rows remain queryable for audit. |
| 2026-04-28 | News source slugs added to `LOGICAL_SOURCE_TYPE_BY_SOURCE_NAME` | `bizjournals_la`, `news_paste_a_link`, `news_backfill`, `news_reextraction` → `news_article`. Without these entries, `_link_orphan_evidence` (`review_workflow.py:1447`) would query `evidence.source_type = 'bizjournals_la'` (the unmapped pass-through) and find nothing, breaking orphan accept for news. Future publishers each get one entry. |
| 2026-04-28 | §21f recent-article delivery-date rule moved from intent to implementation | `EVIDENCE_LAYER_DECISIONS.md` §21f ("article within 6 months outranks CoStar for delivery dates") was forward-looking; no resolver code applied it. Phase D ships it in build step D.4-resolver concurrently with the matcher because the rule can't be validated without article evidence flowing. |
| 2026-04-28 | Auto-apply for high-confidence article matches deferred until reviewer metrics justify it | Shipped as `D.late.A`. Activates after Phase D shows ≥80% reviewer acceptance over 3 months on confirmed-match news evidence with `candidate_confidence='high'` and clean Pass 1/2 agreement. Until then every article-derived change goes through the review queue. |
| 2026-04-28 | Cross-source LLM corroboration ("deep research") routed to Phase E | Roadmap names "Deep Research" but defines no items. Corroboration is a resolver concern — given evidence from multiple sources, how does the resolver weight them — not a producer-side problem. Building it on Phase D would entangle two unrelated risk surfaces. Phase E gains a `corroboration_pass` resolver step; tracking note added there when implemented. |
| 2026-04-28 | PostgREST RLS for Phase D excludes raw HTML, body text, and full LLM payloads from authenticated reads | Authenticated PostgREST gets SELECT only on definer-security summary views (`news_articles_summary`, `news_extractions_summary`) owned by a dedicated `news_summary_reader` `NOLOGIN` role. Raw bodies and full extraction outputs are admin-only via FastAPI after JWT + `ALLOWED_EMAILS`. v1 used `security_invoker = true` which would have failed because authenticated users have no SELECT on the underlying tables. |
| 2026-04-28 | Cost cap race protection via `pg_advisory_xact_lock` | With multiple worker instances, two workers could each see headroom before either's call lands in the rollup, overshooting the cap. The LLM-call wrapper acquires `pg_advisory_xact_lock(NEWS_COST_CAP_LOCK_KEY)` before the cap check + cost reservation, releasing on transaction commit. Maximum overshoot is bounded at `(workers - 1) × max_call_cost`. |
| 2026-04-28 | BizJournals authenticated subscription scraping is acceptable for this use case | The user confirmed paid-subscription credentials are owned and authorized for use in this internal tool. Articles are never redistributed or shared outside the tool. No TOS gating remains for Phase D. |
| 2026-04-28 | LLM-assisted extraction is the chosen approach (Claude API direct, not via AI Gateway) | Cost is not the primary constraint per the user; quality is. Pass 2a triage uses Haiku 4.5 (broad-net, "always err toward inclusion"); Pass 2b extraction uses Opus 4.7. Direct `anthropic` SDK chosen over Vercel AI Gateway for prompt-cache control, transparent per-token cost tracking, and simpler cap enforcement. Re-evaluate if multi-provider A/B becomes a need. |
| 2026-04-28 | News scrape cadence: daily at 06:00 PT for BizJournals, monitor and adjust | Articles are not 1-hour-grained time-critical. Daily covers expected publication cadence with headroom. Backfill uses an 8-week horizon at launch; subsequent runs are incremental. |
| 2026-04-29 | Phase D scheduled-source pilot pivots from BizJournals LA to Urbanize LA | Researcher citation patterns favor Urbanize/LA YIMBY/The Real Deal over BizJournals, and Urbanize is the lowest-friction free source for an end-to-end demo. BizJournals stays mapped as `news_article` but becomes inactive/unscheduled until paid-source capability ships in D.late.C. |
| 2026-04-29 | Phase D collector work builds a generic polite collector, not an Urbanize-only scraper | D.2a implements a config-driven polite NewsCollector with robots compliance, rate limits, feed/archive discovery, source routing, source docs, and Urbanize as the only live Phase D consumer. LA YIMBY fixtures keep the abstraction honest, while real LA YIMBY activation waits for D.late.E1. |
| 2026-04-29 | Advanced fetch implementation deferred; routing interface retained | D.2b adds `fetch_path = polite | advanced`, audit fields, source-doc justification, and hard failure if `advanced` is selected before implementation. cloudscraper/FlareSolverr/TLS impersonation/browser automation/residential proxy work waits for a real approved source need in D.late.ADV/D.late.C. No automatic polite-to-advanced escalation without human approval. |
| 2026-04-29 | Urbanize backfill horizon initially set to 12 months | Superseded 2026-05-01 for LA by the configurable 8-week Urbanize backfill window after Nate's direction and D.6 smoke cost findings. The lasting decision is that D.B must dry-run URL count, estimated relevance, projected LLM cost, and runtime before enqueueing work, then respect robots/rate-limit/source-pause constraints. |
| 2026-04-29 | Source strategy docs are mandatory for news source onboarding | Every `news_sources` row requires `docs/sources/news/<slug>.md` plus an index entry in `docs/sources/README.md`. PRs that add or change source behavior must update the strategy doc with access, discovery, fetch path, quality, operational history, open issues, and code references. |
| 2026-04-29 | Phase D pivot details tightened after senior review | D.2a now specifies host-routing cache TTL, source failure semantics, and transient 5xx retry behavior. D.6 owns scheduled-scrape jitter. D.B reports backfill cost/runtimes as ranges and asks Nate for approval at run time. D.M owns the concrete human-approved advanced-fetch request flow. D.2-docs makes the design-doc revision an explicit task. |
| 2026-04-30 | Market-agnostic behavior becomes an explicit engineering principle | New source, matcher, extractor, resolver, and UI work should be market-agnostic by default. Market-specific behavior belongs in market/jurisdiction/source config with clear extension points. Phase H now starts with a hardening audit to find older LA-specific assumptions and convert them before Santa Monica becomes the second-market proof point; Phase I formalizes the resulting new-market onboarding checklist and validation. |
| 2026-05-01 | Urbanize LA is a market-unscoped scheduled source | D.2a seeds `urbanize_la` with `market_id = NULL` and `jurisdiction_id = NULL`; matcher relevance decides whether each reference belongs to LA, Santa Monica, another active market, or the graveyard. D.B prices the configured 8-week Urbanize LA window (`backfill_window_days = 56`, roughly 150 URLs at current cadence), and D.6 must smoke-test triage/extraction in staging with an Anthropic key before turning on cron. |
| 2026-05-02 | Admin capabilities become first-class platform work | D.M owns the immediate Phase D news ops dashboard, cost controls, and source-run table needed for scheduled-news rollout. Later Phase J owns the cross-app admin console: LLM capability/model configuration, a platform-wide scrape/source-run ledger, and user/role management. New LLM call sites should register stable capability keys and persist the model/config used so model switching can move into the app without rewriting each pipeline. |
| 2026-05-04 | Adopt agentic escalation layer to replace Pass 3a/3b deterministic re-extraction | Pass 3a/3b cannot consult project state, prior articles, or developer history — they re-prompt with the same article. The dense-submarket marketing-name-collision problem (real source of duplicate creation) is structurally unsolvable by better re-extraction; it requires cross-article and cross-project reasoning that an agent loop with tool access can do. Also addresses the 25-market scaling blocker (the in-prompt glossary is ~103k tokens at LA scale and grows unboundedly). See `docs/specs/agentic_escalation_design.md` §1. |
| 2026-05-04 | Agent layer is source-agnostic from day one | Architectural framing: the agent is the project-attribution decision layer for *any* structured intake, not a news-only or permit-only feature. Initial sprint ships news + permits; CoStar (AGENT.4) and Pipedream (AGENT.5) plug into the same agent runner via per-source profiles without runner changes. Sprint scope grows by ~5–10% to support the source-profile abstraction; the alternative (retrofit later) is much more expensive. See `docs/specs/agentic_escalation_design.md` §5.9. |
| 2026-05-04 | Drop the in-prompt developer/project glossary entirely (option 3) | Today's `render_news_glossary` ships every developer (~319) plus every market-filtered project (~1,362 for LA) into the cached system prompt — ~103k tokens, dominating per-article cost and blocking 25-market scaling. AGENT.1 removes this; default extraction emits raw `candidate_name`/`candidate_developer` text without registry hints; matcher continues with fuzzy registry matching; agent's tools (`get_developer_projects`, `search_articles_similar`, `get_nearby_projects`) provide registry knowledge on demand. Resolves the deferred D.B "glossary slicing" sub-decision. |
| 2026-05-04 | Marathon sprint with single cutover; cost guardrails replace staged observation | Researcher direction. Build all of AGENT.1/2/3 in one continuous workstream, ship as one cutover event with permits live from day one. Replaces the originally-considered staged-rollout plan. Safety mechanism is the §5.8 operational guardrails: scoped daily cost caps (separate news vs permit buckets) plus runtime kill switches (`agent_enabled_for_news`, `agent_enabled_for_permits`) flippable in minutes without a deploy. Trade-off accepted: weeks of staged observation give way to bounded daily cap + minutes-to-flip kill switch. |
| 2026-05-04 | Default extraction model is decided by measured A/B, not pre-selected | AGENT.1 builds a three-way harness (Opus 4.7 vs Sonnet 4.6 vs GPT-5.4) against the D.6 smoke article set, with supplemental runs allowed for newly available models. Researcher picks based on quality + cost data. Sonnet/GPT quality is unvalidated against TCG articles, so candidates are treated as hypotheses the harness validates. No hard cost gate — even staying on Opus 4.7 (status quo) is a cost improvement once the agent fires on a fraction of articles instead of every article. |
| 2026-05-04 | Contradiction detection moves into the agent layer; impact assessment gates the rewrite | Today's `detect_project_contradictions` (C.i, shipped 2026-04-27) runs post-resolve only against researcher overrides. AGENT.2 moves contradiction reasoning into the agent for both pre-resolve "article doesn't fit" cases and post-resolve "newer evidence vs override" cases. C.i shipped 8 days before this decision — too recent to rewrite without care. No feature flag; instead, a mandatory pre-build deliverable (`docs/specs/ci_contradiction_impact_assessment.md`, Claude Code-authored, researcher-reviewed) enumerates every code path that depends on C.i's outputs before any code is touched. See design doc §5.5. |
| 2026-05-04 | Eval methodology shifts to three-layer + spot-check sampler | Replaces the curated-golden-set framing that D.late.B originally implied. Layer 1: continuous reviewer-acceptance signal (free, scales). Layer 2: quarterly Pipedream auto-comparison against TCG state for projects within Pipedream's covered submarkets (San Diego, SF, Silicon Valley, Seattle, Denver, others as markets come online). Layer 3: targeted hand-grading on disagreement (~20–30 articles/month/market). Plus a spot-check sampler that surfaces N=10/week non-escalated agent decisions for researcher verification — addresses the silent-error blind spot. |
| 2026-05-04 | Worker model: one job per article needing agent escalation | Resolves design doc R8 (worker-timeout vs agent-runtime). Today's news scrape job runs discovery + Pass 0/1/2 + integration end-to-end in one ~900s job. Stage 2 splits this: `news_scrape_discovery` runs the batch fetch + extraction; `news_agent_integrate` is a separate job kind, one per article needing agent escalation, each with a 300s wallclock cap. Concurrency comes from RQ worker count, not in-job parallelism. Aligns with the §0.1 synchronous-dispatch commitment and Q7 safe-state semantics (single agent job's failure doesn't take down the discovery batch). |
| 2026-05-04 | Batch API for scheduled extractions deferred; architecture supports plug-in later | Anthropic Batch API offers ~50% cost discount on cron-driven extractions. Initial sprint stays synchronous via `client.messages.create` for simplicity — the cost win is real but not load-bearing for the sprint goals. Architectural commitment: extraction calls go through a thin `ExtractionDispatcher` interface so a future Batch path swaps backends without rewriting integration timing. Tracked as `D.late.AGENT.batch`. See design doc §0.1. |
| 2026-05-04 | All user-facing dates render in `m/d/yy` format | Project-wide UI convention. Researcher direction: short, easy-to-read display format for every date shown to users — review queue, project detail, dashboard, Coverage, change log, agent reasoning detail, etc. New code uses `m/d/yy` from the start. Existing date displays get rolling cleanup as each surface is touched (no dedicated cleanup item; touch-and-fix pattern). ISO formats remain in API payloads, audit logs, and machine-readable outputs — this convention is display-only. |
| 2026-05-04 | Senior-developer review absorbed into AGENT sprint design | Six additive changes that protect "build fast" with stronger operational controls: (1) Pass 3a/3b code preserved as `extraction_legacy.py` for one release with emergency `news_use_legacy_pass3` flag — keeps a stronger fallback than default-only; (2) generalize `news_cost_caps` to `cost_caps(bucket, ...)` so permits/costar/pipedream buckets plug in without schema changes; (3) `agent_runs` schema expanded with full observability (provider/model/profile_version/prompt_version/triggered_by-list/token-counts/latency/source_run_id/scrape_job_id/error_text/tool_calls_count/wallclock_seconds) — observability is the safety mechanism if sprint moves fast; (4) A/B harness reframed end-to-end (parse outcomes, reference counts, matcher outcomes, agent trigger rate, review item counts, all-in cost, payload quality spot-grade) — product impact is attribution + review workload, not extraction JSON only; (5) hard tool output budgets (§5.4.1) with truncation contract — per-run cost cap meaningless if tools dump unbounded context; (6) per-reference indexing contract tightened with explicit gates for committed-accept reviews vs auto-applied confirmations vs re-extraction supersession. Researcher accepted all six. Sprint scope grows ~1-2 days. See design doc §13 revision 6 entry. |
| 2026-05-05 | AGENT.1 provider/pricing hardening before A/B harness | Senior review of the multi-provider LLM slice identified two harness blockers: Vercel AI Gateway must require `AI_GATEWAY_API_KEY` rather than silently falling back to `OPENAI_API_KEY`, and Sonnet 4.6 pricing must exist before the harness can reserve/true-up cost. The fix also updates Opus 4.7 pricing to current Anthropic pricing, adds Opus 4.6/GPT-5.5 pricing aliases once requested and verified, uses current OpenAI cached-input pricing for GPT-5.4/GPT-5.5, clears stale missing-key alerts after successful LLM calls, and documents first-harness routing as native Anthropic for Claude candidates plus native OpenAI for GPT candidates. Gateway connectivity is deferred until an explicit sweep/activation decision. |
| 2026-05-05 | Direct provider APIs are current LLM routing policy | For all already-built files and future AGENT steps until an explicit revision, use direct provider APIs: native Anthropic for Claude/agent calls and native OpenAI for GPT candidates. Vercel AI Gateway remains a later operational option for centralized routing/monitoring, not part of the current A/B or default build path. Before enabling Gateway, run a sweep of all LLM call sites, configs, pricing aliases, cost attribution, alerts, and deployment env vars to confirm routing is intentional and no direct-provider assumptions remain. |
| 2026-05-05 | Slim default extraction prompt promoted as `extract_v2` | Implemented the glossary-removal decision with a clean audit cut: active `render_extraction_prompt` now uses `extract_v2`, sending only the slim system template and signal-flag registry as cacheable system blocks. `extract_v2` tells the model to emit raw candidate names/developers and not infer registry IDs, and its schema no longer requires `registry_developer_id` / `registry_project_id`. `extract_v1` is restored as the legacy glossary prompt so historical rows tagged `extract_v1` keep one meaning. Legacy `reextract_v1` still includes the glossary until AGENT.2 moves Pass 3a/3b into `news/extraction_legacy.py`. |
| 2026-05-05 | AGENT.1 A/B harness scaffold is non-mutating by default | Added `tcg-pipeline news ab-extract` for Opus 4.7 / Sonnet 4.6 / GPT-5.4 comparison over the D.6 smoke fixture. The harness calls active `extract_v2`, parses with the production parser, runs matcher projections, estimates AGENT.2 trigger reasons, and runs review-item projection through the existing integration code on a rollback-only transaction so evidence/review/source-run rows are not persisted. Reports are written under `data/output/news/ab_extract_*.json` unless an explicit `--output` path is provided. |
| 2026-05-05 | A/B harness live-run guardrails added before credentials spend | Senior review flagged three live-run guardrails: prove rollback non-mutation, show the DB target before LLM calls, and fail fast on unreachable candidate models. Added a DB-backed row-count test over `news_articles`, `news_extractions`, `news_project_references`, `evidence`, `review_items`, and `source_runs`; CLI startup now prints the redacted DB URL, article count, candidate count, and planned LLM calls; harness preflights each provider/model before the article loop; and report metadata explicitly states harness spend is estimated from provider usage and does not write `llm_cost_usage` rows. |
| 2026-05-05 | AGENT.1 A/B smoke-set costs recorded | Primary post-tightening report `data/output/news/ab_extract_20260505_174623.json`: Opus 4.7 `$0.202427`, Sonnet 4.6 `$0.107957`, GPT-5.4 `$0.054534` adjusted to current OpenAI cached-input pricing (`$0.072966` in the original JSON generated before the pricing correction). Supplemental report `data/output/news/ab_extract_20260505_180014.json`: Opus 4.7 `$0.201802`, Opus 4.6 `$0.233970`, GPT-5.5 `$0.292948`. All supplemental candidates parsed 5/5 articles. Opus 4.7 remained the quality baseline and was selected as default in the following decision row. |
| 2026-05-05 | Default extraction model remains Opus 4.7 | Researcher decision after AGENT.1 smoke-set A/B. Keep Opus 4.7 as the default extraction model for quality. The slim `extract_v2` no-glossary prompt already reduces cost materially versus the old glossary prompt, and AGENT.2 should reduce total Opus use further by replacing deterministic Pass 3a/3b re-extraction with targeted agent escalation. No runtime code change required because Opus 4.7 is already the default. |
| 2026-05-05 | Article embeddings use direct OpenAI small embeddings for AGENT.1 | Implemented the accepted-reference indexing path with direct OpenAI `text-embedding-3-small` at 1536 dimensions, matching the AGENT.1 migration's `vector(1536)` column. The indexer reads only gated references (latest committed accept decision or `news_reference_auto_applied` audit rows), creates per-reference chunks from observed extraction fields and `passage_excerpts`, adds one whole-article chunk per accepted article, supersedes prior active chunks when text changes, skips unchanged active chunks before reserving cost or calling OpenAI, reserves/records spend in `llm_cost_usage`, and exposes `tcg-pipeline news index-articles` plus the `news_backfill_chunk` worker task. |
| 2026-05-05 | First-pass extraction prompt gets explicit evidence/status guardrails before A/B | `extract_v2` now tells the model to use only provided article inputs, avoid outside knowledge/memory/assumptions, and leave missing values null instead of guessing. Because `candidate_status_signal` becomes TCG `pipeline_status` evidence downstream, the prompt now includes the TCG status rubric: first mentions/conference ideas are `Conceptual`; `Proposed` requires stated application/planning/design-review activity or another concrete proposal beyond an idea. Structural status signals are treated as leads to verify against nearby article text, not binding canonical values. |
| 2026-05-05 | Shared TCG semantic interpretation layer belongs in AGENT.2/3 | The first-pass extractor can propose semantic fields for AGENT.1 A/B, but long-term TCG mapping should be a distinct, source-profile-owned layer before writing canonical evidence. Initial scope: `pipeline_status`, `product_type`, `age_restriction`, `date_delivery` projection (for example "end of 2026" → a documented normalized date convention such as `2026-12-15` with raw text anchored), and unit buckets including `workforce_units`. AGENT.2 builds it for news with deterministic phrase/rubric rules plus compact LLM interpretation only for unstructured or ambiguous article language. AGENT.3 wires permits into the same interface with deterministic LADBS/source-profile rules first; LLM/agent interpretation is reserved for ambiguous permit descriptions, conflicting signals, or cross-stream exceptions. |
| 2026-05-05 | Workforce units are a distinct unit component | `workforce_units` is a canonical Project/evidence field alongside existing `affordable_units` and `market_rate_units`. Workforce units are a component of `total_units`, but they are not affordable units and not market-rate units. Extraction/interpreter code must keep workforce counts in the dedicated bucket rather than silently folding them into another bucket. |
| 2026-05-05 | Plan a controlled rebuild/reset after AGENT cutover | Because the project currently has one Supabase database and AGENT development is using production-like data for smoke tests, exploratory review commits made during the sprint are not treated as permanent data-quality decisions. Add `AGENT.reset` as a planned rebuild/rehearsal: backup, reset or restore to a clean target, replay migrations, seed Pipedream/CoStar, run permit scrapes and approved news backfill, rebuild retrieval indexes, then smoke the app before routine use resumes. |
| 2026-05-06 | Same-day AGENT smoke monitoring replaces a 24-hour hold | Researcher direction is to keep making progress through the AGENT sprint. After enabling the first narrow news agent route, run a controlled single-run smoke and inspect `agent_runs`, linkage, review/evidence behavior, and cost immediately; schedule a same-day follow-up check while continuing implementation. Use Render kill switches to disable the live path within minutes if rows show unsafe mutation, bad linkage, malformed verdicts, or unexpected cost. |
| 2026-05-06 | Harden agent final-decision parsing after first live smoke | First live `news_v1` run successfully reached Anthropic and used retrieval tools, then failed safely because the final response was not strict bare JSON. The client should parse a JSON object from common LLM wrappers such as fenced JSON or a short leading/trailing sentence, while the prompt still demands one JSON object with no Markdown/prose. This keeps the audit/fallback posture but avoids spending a full agent call only to reject an otherwise structured decision. |
| 2026-05-06 | Preserve fetched article state on late news job failures | A production smoke hit a PostgreSQL SSL connection failure after article fetch/triage/extraction had already persisted. Generic job-failure handling must not rewrite an already-fetched article to `fetch_failed`; only pre-fetch failures should set that state. Late-stage failures remain visible on `scrape_jobs.status/error_text` while preserving the article's durable fetch/extraction state for follow-up integration or retry. |
| 2026-05-06 | First successful live news agent route validated | Controlled Urbanize smoke reached the live Anthropic/tool loop, completed with `decision=no_change`, linked the deterministic `new_candidate` review item through `agent_run_review_items`, and recorded expected `agent.news_v1` cost. Continue AGENT implementation while monitoring same-day agent rows instead of pausing for a 24-hour hold. |
| 2026-05-06 | Frontendless paste-link smoke helper is allowed | `tcg-pipeline news paste-link-smoke <url>` may be used from a trusted local/Render environment to create the same paste-a-link article/job rows as the API and run the worker inline. This reduces browser friction for article-by-article AGENT smoke tests, but deployed RQ queue dispatch still needs its own smoke when that boundary is the thing being validated. |
| 2026-05-06 | Check Render autodeploy before manual API deploys | Render autodeploy can already be building a pushed commit when a manual deploy is triggered through the API. Always list recent deploys first and wait for an existing deploy of the same commit if one is queued/building/updating; otherwise duplicate deploys create confusing canceled-deploy logs in the dashboard. |
| 2026-05-06 | Agent promotion needs bounded registry lookup | The news profile declared `search_projects`, but the registry did not yet register a handler. A `new_candidate` with no matcher candidate IDs cannot safely promote to an existing project unless a tool returns auditable project IDs from observed address/name/developer. Add `search_projects` and have accepted article retrieval fall back to evidence attribution when the reference row itself has no matched project ID. |
| 2026-05-06 | Deployed RQ worker boundary validated for news agent path | Local machines cannot resolve Render's private Redis hostname, so local DB enqueue is not enough for queue-boundary smoke tests. A Render one-off job can enqueue an existing DB `scrape_jobs` row from inside the worker service network, then the deployed worker processes it through the normal RQ queue. The first RQ smoke completed the same Rosa's Place promotion branch: `new_candidate` -> `promote_existing_project`, evidence attached, and no fallback review item. |
| 2026-05-06 | Next AGENT.2 route is possible-match attribution | After no-change, promote-existing-project, and deployed RQ smoke success on `new_candidate`, the next scoped AGENT.2 expansion is `possible` / ambiguous existing-project matcher outcomes. The agent may confirm one existing candidate, preserve deterministic possible-match review, or escalate; it should not yet perform broad contradiction replacement. This keeps the next slice focused on attribution quality and review-load reduction. |
| 2026-05-06 | AGENT.2 sub-sequence locked as 15 ordered steps | The AGENT.2 row's prose was a scope dump, not an ordering. Locked the build sequence as: (1) promote_existing_project smoke [done], (2) RQ-worker + scheduled-cron smoke [done], (3) `possible_multi_candidate` trigger [done], (4) `low_confidence` trigger [done], (5) output-quality retry path [done], (6) workforce_units integration [done], (7) semantic interpretation layer (single build, all five field domains), (8) Pass 3a/3b move to extraction_legacy.py, (9) harder triggers + contradiction-detection rewrite, (10) worker model split, (11) spot-check sampler, (12) Pipedream coverage compare job, (13) AGENT.3 permits, (14) cutover, (15) AGENT.reset. Sub-sequence note in the AGENT table is the authoritative ordering reference. |
| 2026-05-06 | workforce_units sequenced at AGENT.2 sub-sequence step 6 | Lands between trigger expansion (steps 3-5) and the semantic interpretation layer build (step 7), not earlier and not later. Reason: workforce_units is a precondition for the semantic layer covering unit buckets, but doing it before further trigger expansion gains nothing because Phase R wipes the whole DB anyway, while doing it after the semantic layer would force a slice A / slice B split. Migration default `NULL` with no backfill effort — Phase R re-seeds projects from fresh CoStar (which does not track workforce) and current Pipedream (which also does not), so post-reset projects start `NULL` across the board and populate forward via news, permits, and researcher overrides. |
| 2026-05-06 | Semantic interpretation layer ships as a single build, not slice A/B | Earlier plan split §5.1.1 into slice A (status, product_type, age_restriction, delivery_date) and slice B (unit buckets, blocked on workforce_units). With workforce_units sequenced at AGENT.2 step 6 directly before the semantic layer at step 7, the slice split collapses. Single coherent refactor covering all five field domains. Source-profile-owned mapping; deterministic-first; LLM-only for unstructured/ambiguous article language. |
| 2026-05-06 | AGENT.reset scope simplifies to truncate-and-reseed | Production has been the tuning environment since Phase A; nothing in the database has been authored under final-system conditions. Researcher overrides, Phase A apply decisions, project notes, accepted-review state, change_log, status_history are all dummy artifacts to drop. Reset preserves only config tables (markets, jurisdictions, source_registrations, news_sources, news_signal_flag_registry, cost_caps, cost_cap_overrides, alembic_version, developer canonical registry seed) and re-derives the rest from migrations + fresh CoStar export + most recent Pipedream + scheduled collector re-runs. AGENT.2 step 8's data backfill (synthetic `reasoning_trace` for legacy reextraction rows) becomes a no-op because Phase R drops those rows. AGENT.2 step 12 builds the Pipedream coverage compare job; first run is AGENT.reset step R.7 against the June 2026 Pipedream refresh as the inaugural post-reset eval baseline. |
| 2026-05-06 | Smoke-aggressively posture during AGENT.2 development | Because AGENT.reset wipes everything, the developer can write paste-link smokes, agent decisions, evidence rows, and review items during AGENT.2/3 development without worrying about cleanup. The only mutation discipline required is config-table conservatism — schema migrations to `markets`, `jurisdictions`, `source_registrations`, `news_sources`, `news_signal_flag_registry`, `cost_caps`, `cost_cap_overrides` must be non-breaking because those rows survive the reset. Everything else is fair game until R fires. |
| 2026-05-06 | `possible_multi_candidate` path live-smoked | AGENT.2 step 3 is done: deterministic `possible` matches with candidate project IDs now route through the `news_v1` agent, and the only mutating verdict is `confirm_existing_project` for one of the matcher-provided candidate IDs. `no_change`, escalation, off-list IDs, invalid confidence, and failures preserve the deterministic possible-match review item and link it through `agent_run_review_items`. Controlled Rosa's Place smoke confirmed one matcher candidate, wrote `agent_confirmed_possible_match` evidence, and created no fallback review item. |
| 2026-05-06 | Per-jurisdiction permit-data-quality policy gates news status promotion | Ambiguous news status signals ("broke ground", "started construction", "construction is underway") behave differently per `jurisdiction.permit_data_quality`. Default `low` (assumed for all new jurisdictions): news alone promotes to U/C with `confidence=medium` and an explicit `no_permit_corroboration` reason. Earned `high` (operationally validated, e.g., LA City after ≥3 months of clean LADBS coverage with daily feed cadence and ≤14-day inspection lag for ≥90% of records): news alone does NOT promote; resolver creates a `news_status_uncorroborated` review item recommending stay-current with the article cited; researcher accepts to override. When permits later arrive in either jurisdiction, the resolver re-runs and status auto-promotes through normal evidence flow with confidence upgraded and reason code citing both sources. New `ReviewItemType.news_status_uncorroborated` enum value added in AGENT.2 step 7 (additive migration). See `docs/specs/semantic_interpretation_layer_design.md` §4.2.3, §6, §7. |
| 2026-05-06 | CoStar and Pipedream are not status-corroboration sources | Both remain Tier 1 / Tier 3 evidence for physical attributes and seed data, but their status fields cannot corroborate news status signals because both are stale by design (CoStar manual upload cadence; Pipedream irregular refresh). Status corroboration is restricted to permits. CoStar agreement with a news status signal can be displayed as supporting context in the review-queue card and Resolution-tab metadata, but does not shift the algorithmic flow. See `docs/specs/semantic_interpretation_layer_design.md` §2 principle 5. |
| 2026-05-06 | Forward-looking news language never promotes pipeline_status | News interpreter classifies tense before emitting status signals. `past_concurrent` ("broke ground", "construction has begun", "is underway") promotes per jurisdiction policy. `forward_looking` ("plans to break ground", "is expected to break ground", "will begin construction", "set to break ground", "scheduled to start") writes a `groundbreaking_expected_at` signal flag and an anchor passage but never touches `pipeline_status`. `historical_dated` ("broke ground in spring 2025") promotes AND date-anchors for delivery-year estimation and stalled-project detection. See `docs/specs/semantic_interpretation_layer_design.md` §4.1, §4.2.4. |
| 2026-05-06 | Rental and for-sale projects are tracked as separate projects | When a development includes both rental and for-sale components (mixed-tenure mixed-use, master plans with one rental tower + one for-sale tower, phased developments switching tenure between phases), the system tracks each tenure component as its own project. The relationships table (`phase`, `master_plan`, `counterpart`) handles linkage. Rationale: `Project.rent_or_sale` is currently a single value; multi-tenure projects don't fit cleanly. Tenure carries different absorption curves, financing, and unit economics, so separate projects produce cleaner forecasting and reviewing. Pipedream and CoStar treat them as separate projects in practice, so seed data is consistent. Implications: news interpreter MUST NOT silently merge a mixed-tenure article into one project (see `docs/specs/semantic_interpretation_layer_design.md` §5.6.6). News interpreter MUST NOT default unstated tenure to for-sale — single-family rental / townhome rental (SFR / BTR / build-for-rent) is a real and growing asset class and silently classifying it as for-sale would contaminate downstream forecasting (see §5.6.2 — CRITICAL safety rule). |
| 2026-05-06 | Pre-Leasing defined as rent prices publicly listed for a specific community | Replaces the ambiguous reading of news "leasing office open" / "now accepting applications" / "leasing has begun" press releases (which now emit a signal flag only and do not promote status). Pre-Leasing detection is its own active capability (AGENT.6) with new logical source type `leasing_site`. Sources scanned: Apartments.com, StreetEasy, Trulia, community microsites, developer-portfolio leasing pages. Detection requires at least one specific unit listing with a specific monthly price (HIGH confidence); marketing-floor "starting at $X" without unit-level pricing is MEDIUM confidence; "sign up to be notified" is not Pre-Leasing. Cadence: bi-weekly sweep over U/C projects + trigger-driven immediate checks on `topped_out` / `framing_complete` / `concrete_pour` / `first_move_ins` / `ribbon_cutting` / `now_open` signals from the news interpreter. Sweep stops when project hits Complete (first move-ins or CofO). |
| 2026-05-07 | Semantic-layer review load policy | Researcher direction: reduce human work while staying accurate. Strong physical news signals ("foundation poured", "topped out", "first residents moved in") auto-promote with high confidence. Ambiguous early-construction news in high-quality permit jurisdictions creates one deduplicated `news_status_uncorroborated` review item per project/proposed status/reason; duplicate articles attach to the same item instead of creating parallel work. Three or more independent articles strengthen the recommendation/priority but still do not auto-promote without permit evidence or researcher action. Forward-looking status language creates signal flags only and no review item by default. See `docs/specs/semantic_interpretation_layer_design.md` sections 2, 4.2.1-4.2.4, and 7.3. |
| 2026-05-07 | Semantic feedback data feeds future agent improvement | Researcher decisions on status and other judgment-heavy semantic items must be recorded as structured feedback: source anchors, reason code, system recommendation, researcher decision, final value, and reviewer note when present. Initial use is rule tuning, prompt improvement, spot-check sampling, and eval slices by reason code; direct fine-tuning/model training is future scope after enough labeled decisions exist. See `docs/specs/semantic_interpretation_layer_design.md` section 2 principle 9, section 7.4, and section 11. |
| 2026-05-07 | ProductType expansion deferred for semantic v1 | Step 7 keeps the existing `ProductType` enum and records hotel / senior-care / student-housing / mixed-use specificity as signal flags with `OTHER` where necessary. Enum expansion remains a future schema/UI/export decision after the semantic layer produces enough examples. See `docs/specs/semantic_interpretation_layer_design.md` section 5.2.2 and section 12 question 3. |
| 2026-05-07 | Mixed-tenure review should suggest missing counterpart projects | Mixed rental/for-sale articles must not be merged into one project. If one tenure component already exists, the semantic/matcher path should create a `multi_tenure_review` item with a strong suggested action to create the missing counterpart project prefilled from article evidence and linked back to the existing project. If both linked components exist, write evidence to both; if neither can be matched, ask the researcher to split/create linked projects. See `docs/specs/semantic_interpretation_layer_design.md` sections 5.6.6 and 10. |
| 2026-05-07 | Vague delivery dates use mid-month projection | Semantic v1 uses the mid-month projection convention for vague delivery timing ("late 2026" -> `2026-12-15`, "Q3 2027" -> quarter midpoint). This is more semantically honest than first-of-month while still sorting predictably. See `docs/specs/semantic_interpretation_layer_design.md` section 5.4 and section 12 question 6. |
| 2026-05-07 | Semantic news interpretation pivots from deterministic phrase tables to LLM-backed Pass 2c | Phrase tables silently miss real signals (the failure mode is "did nothing," with no reason code or audit trail) and require exhaustive enumeration that can't keep up with publication and market language variation. Replaced with a single-shot Opus 4.7 call (`interpret_v1` prompt, capability key `semantic.news_v1`, structured-output schema, no tools) downstream of Pass 2b extraction. The TCG glossary, status rubric, jurisdiction policy, reason-code vocabulary, and tense-classification rules all live in the prompt rather than in Python phrase tables. Cost is tracked separately from extractor, retry, embedding, and agent calls via `llm_cost_usage.capability='semantic.news_v1'`; the shared bucket cap remains `news`. Estimated marginal cost is bounded (~$0.05-0.10/article incremental over Pass 2b; ~$2.50-15/day at LA scale; ~$12-125/day at 25-market scale). Quality-driven decision; cost is not the deciding factor. The deterministic-first principle is reframed: **LLM where the source is natural prose; deterministic where the source is already structured.** Source-profile-owned: news → LLM (Pass 2c); LADBS / CoStar / Pipedream / future structured permit feeds → deterministic. Both implementations conform to the same `SemanticInterpreter` Protocol and emit the same `SemanticInterpretation` output schema. See `docs/specs/semantic_interpretation_layer_design.md` revision 4. |
| 2026-05-07 | Pass 2c is a single-shot classifier, not an agent loop | The semantic interpreter is a focused structured-classification call with rich pre-computed `InterpreterContext` input (project state, jurisdiction policy, market glossary, recent evidence summary). It does NOT have tools. Cross-source reasoning, contradiction handling, and dynamic lookups remain the AGENT.2 escalation layer's job; the agent consumes Pass 2c output as one of its inputs. Keeping the layers separate keeps each prompt focused and each layer A/B-testable. The current `extract_v2` extraction prompt is heavily tuned for extraction quality; folding interpretation into Pass 2b would dilute that focus. Add narrow tools to Pass 2c only against documented failure modes — never speculatively. |
| 2026-05-07 | Multi-market semantic glossary architecture | The base TCG glossary in the Pass 2c system prompt is jurisdiction-agnostic (universal vocabulary: physical construction milestones, tenure language, age-restriction language, unit-bucket language, delivery-timing conventions). Locale-specific entitlement vocabulary (NYC's ULURP, Florida's DRI, Texas's MUD, Massachusetts's Chapter 40B, NY/MA "preliminary plat" vs CA "tentative map," etc.) lives in **per-market addenda** at `config/markets/<slug>/semantic_glossary.yaml`. The Pass 2c system prompt is composed at inference time as base + addendum, with the reason-code registry dynamically extended by per-market `reason_code_extension` entries. New markets default to no addendum (the base rubric still works; glossary-gap detection surfaces what's missing). Glossary-gap discipline: the interpreter handles unfamiliar terminology with best-fit + `glossary_gap_observed=true` signal flag at `confidence=low` rather than refusing, plus `*_unmappable` reason codes per field domain when even best-fit fails. Per-market reason-code distribution monitoring on the spot-check sampler dashboard auto-alerts when a market's gap-rate / unmappable-rate / reviewer-rejection-rate exceeds established-markets baseline (default thresholds: gap rate > 15%/field, unmappable rate > 5%/field, rejection rate > baseline + 2σ). New-market onboarding (Phase I.1) requires building the per-market addendum from a 50-100 article sample before going live. See `docs/specs/semantic_interpretation_layer_design.md` §5.13-§5.15 (revision 4). |
| 2026-05-07 | Pass 2c semantic cost measured at $0.198/call; tiered optimization sequenced as AGENT.7 | First live Pass 2c smoke against the configured staging DB (token `semantic-pass2c-20260507-4`, n=1) measured `$0.198341` for one call: single article reference matched to an LA project plus three candidate projects, `news_status_uncorroborated_high_quality_permit_jurisdiction` reason code emitted with `requires_corroboration=true` (the desired LA-high-jurisdiction behavior). Roughly 2x the original design-doc estimate of `$0.05-0.10`/call. Dominant cost is uncached per-article input (article body + project context + recent evidence + Pass 2b output), not output and not the cached system prompt. At LA scale (~150 Urbanize articles/day) ≈ `$30/day`; at projected 25-market scale ≈ `$200/day`. Cost is bounded and per the 2026-04-28 decision quality remains the primary constraint, but optimization headroom is real. Optimization sequenced as AGENT.7 in three tiers with measurement-first discipline: Tier 1 (no architectural change — pre-filter empty-extraction articles, compress `recent_evidence` and `project_state` representations, trim article body cap from 30K to 15K chars after verifying length distribution, slice Pass 2b output to candidate references only); Tier 2 (A/B evaluation of other LLMs — Sonnet 4.6, GPT-5.4, future releases — against Opus 4.7 on the existing A/B harness, with researcher hypothesis that Opus will likely remain best for ambiguous-prose interpretation; A/B is hypothesis-test with data, not speculative displacement); Tier 3 (tools-on-demand reserved for cases where Tiers 1+2 are insufficient, since it crosses the §2 principle 11 single-shot-classifier boundary). Pre-work: capture per-article cost distribution from broader §10 step 6 smoke + 30 days of production data; instrument prompt-cache hit-rate distribution; consider queueing/batching mitigations to improve cache effectiveness without code change. Sequencing: ship Tier 1 first, measure reduction, run Tier 2 A/B, only then evaluate need for Tier 3. See AGENT.7 row above. |
| 2026-05-07 | Semantic Pass 2c first live-smoke findings | One-article Urbanize LA staging smoke on local commit `9fdac9d` validated fetch -> triage -> extraction -> Pass 2c -> audit persistence -> integration. The first run exposed two operational corrections: `NEWS_SEMANTIC_MAX_TOKENS=2500` truncated the structured response, so semantic max tokens were raised to `5000`; and possible-match candidate projects were missing from `project_context`, so unscoped Urbanize articles fell back to low/default jurisdiction policy. Commit `5c9357d` now includes top-3 possible-match candidate projects, each with its own jurisdiction policy, and the prompt applies the most restrictive policy for a reference. The successful rerun emitted `news_status_uncorroborated_high_quality_permit_jurisdiction` with `requires_corroboration=true` for ambiguous LA construction language. Measured Pass 2c cost was `$0.198341` for the smoke article; use about `$0.20/article` as the near-term baseline (about `$30/day` at 150 LA articles/day; about `$200/day` at 1,000 articles/day) until broader smoke and dashboard telemetry provide a larger sample. Render `NEWS_EXTRACT_MAX_TOKENS` was also aligned from `2500` to the existing code/docs target of `5000`. |
| 2026-05-07 | Semantic Pass 2c broader controlled smoke passed | The broader controlled suite passed 10 checks over 9 live Pass 2c calls and cleaned all smoke articles/projects/source runs afterward. Covered strong `topped_out` auto-promotion, low/default-policy `broke_ground` auto-promotion, forward-looking status signal-only behavior, `first_move_ins` Complete promotion, mixed-tenure review creation, three-article high-jurisdiction dedup with priority escalation, and glossary-gap/unmappable handling. The suite exposed one normalization issue: Opus can return event tokens such as `topped_out` or `first_move_ins` in `canonical_value` while selecting the correct reason code. The integrator now maps status reason codes to canonical TCG statuses before writing project evidence or review proposed values. Measured suite cost was `$0.402036`; because the suite used short synthetic articles, keep `$0.20/article` as the scheduled-article planning baseline until production telemetry is larger. |
| 2026-05-07 | Semantic Pass 2c Render cutover | After the broader controlled smoke suite passed, Render API and worker move from the legacy semantic path to Pass 2c with `NEWS_USE_LEGACY_SEMANTIC=false` on both services. `NEWS_USE_LEGACY_PASS3=false`, `AGENT_ENABLED_FOR_NEWS=false`, and `AGENT_ALLOW_LIVE_LLM=false` remain unchanged; this cutover affects semantic interpretation only, not AGENT.2 tool-loop routing. Rollback is the Render env kill switch back to `NEWS_USE_LEGACY_SEMANTIC=true`. |
| 2026-05-07 | News LLM cost cap raised for semantic cutover | The shared `news` bucket cap was still warn `$25` / hard `$35`, which is below the semantic Pass 2c LA-scale planning baseline once extraction and triage are included. After logical backup `backups/supabase_pre_semantic_costcap_20260507_160340.dump` (SHA256 `bedef12c8529610b0f13999cd340570239ae0472f8501b37f83d37336280a29b`), inserted a new `cost_caps` row for `bucket='news'`, `effective_from='2026-05-07'`, warn `$75`, hard `$125`. Spend remains tracked per capability in `llm_cost_usage` (`semantic.news_v1`, extraction, triage, retry, agent), while enforcement remains shared at the `news` bucket until capability-specific caps are designed. |
| 2026-05-07 | Legacy Pass 3a/3b isolated before harder trigger rewrite | Moved deterministic Pass 3a decisioning/re-extraction and Pass 3b re-extraction runner code from active `news/extraction.py` into `news/extraction_legacy.py`. The active extractor now lazy-calls the legacy module only for the fallback path, while `news/integration.py` imports the Pass 3b fallback runner from the legacy module. Test fixtures default non-semantic integration tests to `NEWS_USE_LEGACY_SEMANTIC=true` so local `.env` LLM credentials do not spend tokens or alter legacy extraction assertions. This completes AGENT.2 sub-sequence step 8 and leaves step 9 (harder triggers + contradiction rewrite) as the next build slice. |
| 2026-05-07 | Added `UI.QA` flexible step; AGENT.2 step 11 broadened from spot-check sampler to Activity / Audit Log view | Researcher direction. **`UI.QA`** is a holistic visual-inspection pass on every researcher-facing surface, run on real data once major UI surfaces are populated. New `flexible` status marker indicates the step never gates development; researcher chooses when to do the manual session. Three natural slots are annotated in escalating recommendation strength: AGENT.2 step 11 ship (first opportunity), `AGENT.reset` (planned downtime + clean post-reset data), and start of H.0 (strongly recommended before second-market UI patterns lock in). Annotations drop out once `UI.QA` is `done`. **AGENT.2 sub-sequence step 11** expands from a narrow "non-escalated agent decisions for researcher verification" dashboard into a cross-project Activity / Audit Log view of all resolver-applied changes (sourced from `change_log` joined with `resolution_log` + `agent_runs`), with the original spot-check sampler scope, "auto-applied without review," "semantic auto-promotions," and "per-market reason-code distribution" as saved-view filter presets. Reason: a separate audit log built later would duplicate the same data joins; folding both into one view at step 11 keeps researcher-facing audit affordances consistent with Review Queue affordances and avoids superseding a narrow UI later. |
| 2026-05-08 | AGENT.reset reframed as iterative stabilization cycles; D.B sequenced at R.6.5 with 4-week stabilization window, 8-week final-settle window | Researcher direction. The reset is no longer a single-day cutover. Each stabilization cycle (backup → truncate → reseed → collectors → resolve → news cron live → backfill → eval) runs end-to-end so the team can review review-queue performance, UI/layout quality, and pipeline accuracy on data that flowed through every system. The reset data is treated as dummy data: review and commit decisions made during a cycle are part of the test, and any cycle can be wiped and re-run after UI/process improvements land. **D.B Urbanize backfill is sequenced as `AGENT.reset` step R.6.5** — 4 weeks (~75 URLs at current Urbanize cadence) per stabilization cycle for cost discipline; 8 weeks reserved for the final-settle cycle. Each cycle's organic backfill is supplemented with curated paste-a-link smokes that deliberately trigger rarer agent paths (`material_contradiction`, `override_contradiction`, structural conflicts) so the review queue exercises the full UI surface area regardless of what the 4-week organic window happens to contain. **`UI.QA` slots into every cycle**, not just one; mark `done` only after the final-settle cycle. **Final-settle criteria**: `UI.QA` complete, review-queue UX validated end-to-end, trigger fire rates / review-acceptance rates measured against the step 11 Activity / Audit Log view, cost telemetry within expected range per AGENT.7, and all known prior-cycle bugs/improvements addressed. After final-settle, the post-reset state becomes the new production baseline and Phase E/F/G/H/J unblock. **D.B repricing approval** now factors AGENT.2 agent-trigger fire rates in addition to the AGENT.1 default-extraction model choice. |
| 2026-05-08 | Final-settle reset backfill window sized by explicit cost-vs-comprehensiveness analysis, not a fixed 8 weeks | Researcher direction. 8 weeks is the initial planning baseline for D.B but may not be the right production-ready bootstrap window. Before the final-settle cycle, the operator runs a dry-run cost-coverage curve at multiple candidate windows (8 / 12 / 26 weeks at minimum) and picks an explicit point on the curve. Drivers include AGENT.7 cost optimizations shipped (Tier 1+ cuts per-article cost from the ~$0.20 Pass 2c baseline), 25-market scale projection (the chosen window applies to LA initially but informs new-market onboarding precedent), glossary-gap rates per market, project-attribution coverage targets vs the current Pipedream-derived baseline, and historical-context value for forecasting workflows. The chosen window applies only to the final-settle cycle; stabilization cycles stay at the 4-week default for cost discipline. Worth running the curve analysis once before AGENT.7 Tier 1 ships (sets a pre-optimization upper bound) and again after (informs the actual decision with post-optimization cost). |
| 2026-05-07 | Pass 1 vs Pass 2 conflicts start routing to the agent | AGENT.2 step 9 begins by moving structural extraction conflicts into the news agent path when `AGENT_ENABLED_FOR_NEWS=true`, `AGENT_ALLOW_LIVE_LLM=true`, and `NEWS_USE_LEGACY_PASS3=false`. If that live agent route is not fully enabled, legacy Pass 3a structural re-extraction remains active. This keeps current production behavior stable while enabling controlled smoke of the `pass1_pass2_conflict` trigger. |
| 2026-05-07 | Structural-conflict review backstop before material contradictions | Agent `no_change` on a confirmed `pass1_pass2_conflict` commits the original extraction as reviewed-by-agent. Agent `escalated` creates a field decision-card review item using the existing `status_change` review shape, linked through `agent_run_review_items`, so the disagreement is not buried only in `agent_runs`. A dedicated `news_structural_conflict_unresolved` enum/UI label and a consolidated context-gather pass are deferred until the `material_contradiction` rewrite clarifies the final review vocabulary and shared pre-pass shape. |
| 2026-05-08 | AGENT.2 Case B override contradictions integrate with C.i | `override_contradiction` now lets the news agent recommend accepting newer article evidence or keeping an active researcher override before deterministic C.i creates its own item. Agent-created review items use the existing `override_contradiction` shape with ordered `proposed_alternatives`, `system_recommendation`, `agent_run_id`, and `agent_run_review_items` linkage; `resolve_project(..., skip_contradiction_review_item_ids=...)` prevents deterministic C.i from duplicating or invalidating those same agent-created items during the same resolve pass. |
| 2026-05-08 | Override-trigger cleanup before final Case B review | Pre-resolve `override_contradiction` detection now skips raw news `pipeline_status` signals because values like `topped_out` are event tokens until Pass 2c canonicalizes them; real status override contradictions are handled by the semantic evidence path plus post-resolve C.i. The agent prompt no longer asks for a per-field `verdict.field` on override contradictions because integration applies one recommendation to the full reference payload. Dead pre-pass helpers were removed after the shared `_gather_agent_context` refactor, and news integration now reuses the shared active-override helper and `LARGE_UNIT_DELTA` constant from C.i. |
| 2026-05-08 | Scheduled news scrape splits agent integration into child jobs | AGENT.2 step 10 keeps the existing `news_scrape` kind as the discovery/fetch/triage/extraction job for compatibility, then preflights whether the extracted article has news-agent triggers. Articles with no agent triggers continue through inline integration. Articles with triggers enqueue a child `news_agent_integrate` job, one article per job, so the agent loop has its own bounded worker envelope while discovery can finish without waiting on multiple tool-loop runs. The child job reloads durable article/extraction state, runs integration + agent handling, writes review/evidence outputs, and increments the parent `source_runs.new_matches` audit count. |
| 2026-05-08 | `news_agent_integrate` child jobs are unique per active article and retryable | Follow-up hardening to AGENT.2 step 10. A partial unique index now allows at most one queued/running `news_agent_integrate` child per article, and the parent enqueue path reuses active child rows so scrape retries do not fan out duplicate agent work. `source_runs.new_matches` increments are atomic SQL updates, preventing concurrent children from losing review-item counts. Catastrophic child-worker failures before integration starts are treated as ops-visible retryable scrape-job failures; deterministic fallback review items are only written inside the integration path after agent-loop failures. Step 11's Activity / Audit Log view should show both article fetched time and agent decision created time so queue lag is visible. |
| 2026-05-08 | Activity / Audit Log first slice lands for AGENT.2 step 11 | Added a privileged `GET /activity/events` API and `/activity` frontend page that combine `change_log`, `resolution_log`, and `agent_runs` / `agent_run_review_items` into one chronological feed. Initial filters cover view preset, source, field, actor/profile, project, and date window. Saved presets cover agent decisions, auto-applied changes, and semantic status activity. Agent rows include project/article context, review-item links, reasoning trace, cost, and both `news_articles.fetched_at` and `agent_runs.created_at` so queue lag is visible. Remaining step 11 work: market/jurisdiction filters, richer evidence drill-through, and per-market semantic reason-code / glossary-gap metrics. |
| 2026-05-08 | Activity / Audit Log filter hardening before semantic metrics | Tightened the first Activity slice before expanding step 11: view-preset pills now preserve the rest of the active query string; event ordering parses timestamps rather than comparing strings; no-op resolution rows are hidden; failed agent runs render as failed outcomes rather than decisions; publisher-source filters include linked news-agent rows by matching `news_sources.slug`; and DB-backed tests cover auto-applied, semantic, source, date, actor, and failed-agent branches. The current `semantic` preset remains a status-news placeholder; the next semantic metrics slice should read `news_semantic_interpretations` directly so future sources like LA YIMBY are not missed. |
| 2026-04-16 | LADBS permit issuance = Approved evidence, not UC proof | TCG status definitions put first permit issuance inside Approved; UC requires visible vertical construction. Resolution engine flags permit-alone as requires_review. |
| 2026-04-16 | Only final CofO with real `cofo_issue_date` emits Complete evidence | Corrected/reactivated/superseded CofO rows remain source detail until explicitly modeled. |
| 2026-04-16 | Only recent, substantive inspections on active permits emit UC evidence | Adapter persists all inspection context but only emits `building_inspection_recorded` when recent + substantively positive + permit in active status. |
| 2026-04-16 | Periodic full reconciliation required for Socrata sources | Catches source-side corrections, row disappearance, and stale-logic bugs in our own earlier filters. |
| 2026-04-16 | Source-row disappearance tracked first, not auto-applied | If a Socrata row vanishes between runs, treat as source-state event until repeated absence or stronger evidence justifies changing project state. |
| 2026-04-15 | Pipedream status values adopted as canonical status enum | Well-defined and used by researchers daily. Public source statuses get mapped to these. |
| 2026-04-15 | Match conservatively, track liberally | When values change (address, units, name), that's a field change in ChangeLog, not a matching failure. Store all aliases in raw_addresses[] and previous_names[]. |
| 2026-04-15 | Auto-match threshold >=0.85, ambiguous 0.65-0.84, weak 0.40-0.64, no match <0.40 | Balances automation with researcher oversight. Most deterministic and address matches auto-link. Proximity + fuzzy matches get human review. |
| 2026-04-15 | Four-step execution order: Discovery → Status Updates → Enrichment → Review Queue | Discovery finds new projects, Status Updates tracks existing ones, Enrichment deepens via ZIMAS (triggered by new case numbers), Review Queue prioritizes researcher attention. |
| 2026-04-15 | ZIMAS is enrichment-only, not discovery | No programmatic address→case lookup exists. Case numbers must come from other sources (LA Case Reports, LADBS, Pipedream URLs). |
| 2026-04-15 | Scope includes all development types, not just residential | Pipeline tracks rental, for-sale, and commercial. Pipedream is residential-focused but system accepts commercial projects from CoStar and public sources. |

---

## 9. Open Questions

> Items to resolve as we build. Move to Decision Log when resolved.

- [x] ~~**BizJournals scraping legality/TOS**~~ Resolved 2026-04-28 — paid subscription is authorized; articles never leave the tool. See Decision Log.
- [x] ~~**Article extraction approach**~~ Resolved 2026-04-28 — LLM-assisted via Claude API direct (Haiku 4.5 triage + Opus 4.7 extraction + structural-pass pre-extraction with character offsets). See `docs/specs/news_research_design.md` §7-§8.
- [ ] **Pipedream ongoing sync:** After initial seed, will researchers continue to update Pipedream files? If so, need a recurring import/sync process. Or does this system replace Pipedream entirely?
- [ ] **Non-MF project handling:** CoStar provides 231 non-MF projects (offices, hotels, retail). How should they appear in the review interface? Separate section? Same queue with a property type filter?
- [ ] **Unit count threshold for new project candidates:** Minimum unit count for the system to flag a new LADBS project? Pipedream's smallest are ~10-20 units. Phase D matcher uses ≥10 units for news-derived `new_candidate` per `docs/specs/news_research_design.md` §9.4; LADBS path may want to align.
- [x] ~~**Update frequency** (news)~~ Superseded 2026-04-29 - BizJournals cadence deferred with D.late.C; Urbanize cadence is a D.2v/D.6 validation output. LADBS / Socrata cadences remain weekly per existing collectors.
- [x] ~~**Urbanize scrape cadence**~~ Resolved 2026-05-01 - initial candidate is `30 7 * * *` in `America/Los_Angeles`, roughly 75-90 minutes after observed RSS posts; D.6 adds deterministic per-source jitter and monitoring-based adjustment remains D.M/D.6 rollout work.
- [ ] **Advanced fetch approval criteria:** Define the human approval bar for moving a source from polite to advanced, including compliance/TOS notes and source-health evidence.
- [ ] **Next source after Urbanize:** Confirm whether LA YIMBY remains the D.late.E1 second-source pressure test after Urbanize validation, or whether researcher priorities change the ordering.
- [ ] **Source disappearance policy:** When a Socrata row vanishes, how many absent reconciliations before changing project state?
- [ ] **Stalled/inactive evaluator timing:** 12 months is the current spec. Is that the right threshold? Should it vary by status?
- [ ] **CoStar city name normalization:** CoStar MF exports use "Los Angeles", "Los Angeles CBD", "Downtown Los Angeles", "Hollywood", etc. Need a mapping table for MF city normalization. Non-MF is clean. Affects Phase A backfill data quality. (From ARCHITECTURE.md q. 2048)
- [ ] **Pipedream relationship ID format:** `CorrP`, `PCPart`, and `RelP1-6` normalize abbreviated numeric IDs by zero-padding. Verify against a real workbook whether relationship fields always use full 5-digit sequences or sometimes abbreviate. Affects Phase A backfill correctness. (From ARCHITECTURE.md q. 2051)
- [ ] **Pipedream "Pending" → public source status mapping:** Pipedream "Pending" means in entitlement. LADBS and ZIMAS use different terminology. Need a mapping table. Same for "Stalled" detection from absence of activity. (From ARCHITECTURE.md q. 2055)
- [ ] **Overlapping market support:** `Project.market` is sufficient while markets don't overlap. Before any city + county datasets go live, need `ProjectMarketMembership` model. (From ARCHITECTURE.md q. 2065)
- [ ] **Boundary-based routing:** Current city-scoped filtering relies on source coverage declarations + `--allowed-city`. Before county/metro expansion, define how to do boundary checks from parsed address, parcel, or geometry. (From ARCHITECTURE.md q. 2067)
- [ ] **Layered pipeline vs. end-to-end LLM reasoning:** Current architecture chose layered (Pass 0 / Pass 1 / triage / default extraction / matcher / agent escalation) for unattended-production-at-scale economics, reproducibility, and audit posture. Worth measuring after AGENT.reset whether end-to-end Opus on the full article (same tool registry, far less structured prompt) matches or beats layered output at acceptable cost. Suggested test: 10-15 fixture articles run through (a) the layered pipeline, (b) a layered-stripped "give Opus the article and tools" path, (c) a Claude Code session as a human-in-loop reference. Compare on attribution correctness, total cost, latency, and "surprising decisions" (cases where modes disagree are usually the most informative signal). If (b) wins on quality at acceptable cost, simplify the pipeline. If (c) wins by a wide margin, consider a separate human-driven operating mode for new-market onboarding distinct from steady-state monitoring. Natural execution point is paired with the AGENT.reset June Pipedream comparison.
